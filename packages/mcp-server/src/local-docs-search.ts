// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.

import MiniSearch from 'minisearch';
import * as fs from 'node:fs/promises';
import * as path from 'node:path';
import { getLogger } from './logger';

type PerLanguageData = {
  method?: string;
  example?: string;
};

type MethodEntry = {
  name: string;
  endpoint: string;
  httpMethod: string;
  summary: string;
  description: string;
  stainlessPath: string;
  qualified: string;
  params?: string[];
  response?: string;
  markdown?: string;
  perLanguage?: Record<string, PerLanguageData>;
};

type ProseChunk = {
  content: string;
  tag: string;
  sectionContext?: string;
  source?: string;
};

type MiniSearchDocument = {
  id: string;
  kind: 'http_method' | 'prose';
  name?: string;
  endpoint?: string;
  summary?: string;
  description?: string;
  qualified?: string;
  stainlessPath?: string;
  content?: string;
  sectionContext?: string;
  _original: Record<string, unknown>;
};

type SearchResult = {
  results: (string | Record<string, unknown>)[];
};

const EMBEDDED_METHODS: MethodEntry[] = [
  {
    name: 'list',
    endpoint: '/v1/models',
    httpMethod: 'get',
    summary: 'List Models',
    description:
      'List available models.\n\nRetrieve the complete list of models available to your organization, including\nmodels from OpenAI, Anthropic, Google, xAI, Mistral, Fireworks, and DeepSeek.\n\nReturns:\n    ListModelsResponse: List of available models across all supported providers',
    stainlessPath: '(resource) models > (method) list',
    qualified: 'client.models.list',
    response:
      "{ data: { id: string; created_at: string; provider: string; capabilities?: object; defaults?: object; description?: string; display_name?: string; provider_declared_generation_methods?: string[]; provider_info?: object; version?: string; }[]; object?: 'list'; }",
    markdown:
      "## list\n\n`client.models.list(): { data: model[]; object?: 'list'; }`\n\n**get** `/v1/models`\n\nList available models.\n\nRetrieve the complete list of models available to your organization, including\nmodels from OpenAI, Anthropic, Google, xAI, Mistral, Fireworks, and DeepSeek.\n\nReturns:\n    ListModelsResponse: List of available models across all supported providers\n\n### Returns\n\n- `{ data: { id: string; created_at: string; provider: string; capabilities?: object; defaults?: object; description?: string; display_name?: string; provider_declared_generation_methods?: string[]; provider_info?: object; version?: string; }[]; object?: 'list'; }`\n  Response for /v1/models endpoint.\n\n  - `data: { id: string; created_at: string; provider: string; capabilities?: { audio?: boolean; image_generation?: boolean; input_token_limit?: number; output_token_limit?: number; streaming?: boolean; structured_output?: boolean; text?: boolean; thinking?: boolean; tools?: boolean; vision?: boolean; }; defaults?: { max_output_tokens?: number; temperature?: number; top_k?: number; top_p?: number; }; description?: string; display_name?: string; provider_declared_generation_methods?: string[]; provider_info?: object; version?: string; }[]`\n  - `object?: 'list'`\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst listModelsResponse = await client.models.list();\n\nconsole.log(listModelsResponse);\n```",
    perLanguage: {
      typescript: {
        method: 'client.models.list',
        example:
          "import Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst listModelsResponse = await client.models.list();\n\nconsole.log(listModelsResponse.data);",
      },
      python: {
        method: 'models.list',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\nlist_models_response = client.models.list()\nprint(list_models_response.data)',
      },
      java: {
        method: 'models().list',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.models.ListModelsResponse;\nimport com.dedalus_sdk.api.models.models.ModelListParams;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        ListModelsResponse listModelsResponse = client.models().list();\n    }\n}',
      },
      kotlin: {
        method: 'models().list',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.models.ListModelsResponse\nimport com.dedalus_sdk.api.models.models.ModelListParams\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val listModelsResponse: ListModelsResponse = client.models().list()\n}',
      },
      go: {
        method: 'client.Models.List',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tlistModelsResponse, err := client.Models.List(context.TODO())\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", listModelsResponse.Data)\n}\n',
      },
      ruby: {
        method: 'models.list',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\nlist_models_response = dedalus.models.list\n\nputs(list_models_response)',
      },
      cli: {
        method: 'models list',
        example: "dedalus-sdk models list \\\n  --api-key 'My API Key'",
      },
      php: {
        method: 'models->list',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$listModelsResponse = $client->models->list();\n\nvar_dump($listModelsResponse);",
      },
      csharp: {
        method: 'Models.List',
        example:
          'ModelListParams parameters = new();\n\nvar listModelsResponse = await client.Models.List(parameters);\n\nConsole.WriteLine(listModelsResponse);',
      },
      http: {
        example:
          "curl https://api.dedaluslabs.ai/v1/models \\\n    -H 'User-Agent: Dedalus-SDK' \\\n    -H 'X-SDK-Version: 1.0.0' \\\n    -H \"Authorization: Bearer $DEDALUS_API_KEY\"",
      },
    },
  },
  {
    name: 'retrieve',
    endpoint: '/v1/models/{model_id}',
    httpMethod: 'get',
    summary: 'Retrieve Model',
    description:
      'Retrieve a model.\n\nRetrieve detailed information about a specific model, including its capabilities,\nprovider, and supported features.\n\nArgs:\n    model_id: The ID of the model to retrieve (e.g., \'openai/gpt-4\', \'anthropic/claude-3-5-sonnet-20241022\')\n    user: Authenticated user obtained from API key validation\n\nReturns:\n    Model: Information about the requested model\n\nRaises:\n    HTTPException:\n        - 401 if authentication fails\n        - 404 if model not found or not accessible with current API key\n        - 500 if internal error occurs\n\nRequires:\n    Valid API key with \'read\' scope permission\n\nExample:\n    ```python\n    import dedalus_labs\n\n    client = dedalus_labs.Client(api_key="your-api-key")\n    model = client.models.retrieve("openai/gpt-4")\n\n    print(f"Model: {model.id}")\n    print(f"Owner: {model.owned_by}")\n    ```\n\n    Response:\n    ```json\n    {\n        "id": "openai/gpt-4",\n        "object": "model",\n        "created": 1687882411,\n        "owned_by": "openai"\n    }\n    ```',
    stainlessPath: '(resource) models > (method) retrieve',
    qualified: 'client.models.retrieve',
    params: ['model_id: string;'],
    response:
      '{ id: string; created_at: string; provider: string; capabilities?: { audio?: boolean; image_generation?: boolean; input_token_limit?: number; output_token_limit?: number; streaming?: boolean; structured_output?: boolean; text?: boolean; thinking?: boolean; tools?: boolean; vision?: boolean; }; defaults?: { max_output_tokens?: number; temperature?: number; top_k?: number; top_p?: number; }; description?: string; display_name?: string; provider_declared_generation_methods?: string[]; provider_info?: object; version?: string; }',
    markdown:
      '## retrieve\n\n`client.models.retrieve(model_id: string): { id: string; created_at: string; provider: string; capabilities?: object; defaults?: object; description?: string; display_name?: string; provider_declared_generation_methods?: string[]; provider_info?: object; version?: string; }`\n\n**get** `/v1/models/{model_id}`\n\nRetrieve a model.\n\nRetrieve detailed information about a specific model, including its capabilities,\nprovider, and supported features.\n\nArgs:\n    model_id: The ID of the model to retrieve (e.g., \'openai/gpt-4\', \'anthropic/claude-3-5-sonnet-20241022\')\n    user: Authenticated user obtained from API key validation\n\nReturns:\n    Model: Information about the requested model\n\nRaises:\n    HTTPException:\n        - 401 if authentication fails\n        - 404 if model not found or not accessible with current API key\n        - 500 if internal error occurs\n\nRequires:\n    Valid API key with \'read\' scope permission\n\nExample:\n    ```python\n    import dedalus_labs\n\n    client = dedalus_labs.Client(api_key="your-api-key")\n    model = client.models.retrieve("openai/gpt-4")\n\n    print(f"Model: {model.id}")\n    print(f"Owner: {model.owned_by}")\n    ```\n\n    Response:\n    ```json\n    {\n        "id": "openai/gpt-4",\n        "object": "model",\n        "created": 1687882411,\n        "owned_by": "openai"\n    }\n    ```\n\n### Parameters\n\n- `model_id: string`\n\n### Returns\n\n- `{ id: string; created_at: string; provider: string; capabilities?: { audio?: boolean; image_generation?: boolean; input_token_limit?: number; output_token_limit?: number; streaming?: boolean; structured_output?: boolean; text?: boolean; thinking?: boolean; tools?: boolean; vision?: boolean; }; defaults?: { max_output_tokens?: number; temperature?: number; top_k?: number; top_p?: number; }; description?: string; display_name?: string; provider_declared_generation_methods?: string[]; provider_info?: object; version?: string; }`\n  Unified model metadata across all providers.\n\nCombines provider-specific schemas into a single, consistent format.\nFields that aren\'t available from a provider are set to None.\n\n  - `id: string`\n  - `created_at: string`\n  - `provider: string`\n  - `capabilities?: { audio?: boolean; image_generation?: boolean; input_token_limit?: number; output_token_limit?: number; streaming?: boolean; structured_output?: boolean; text?: boolean; thinking?: boolean; tools?: boolean; vision?: boolean; }`\n  - `defaults?: { max_output_tokens?: number; temperature?: number; top_k?: number; top_p?: number; }`\n  - `description?: string`\n  - `display_name?: string`\n  - `provider_declared_generation_methods?: string[]`\n  - `provider_info?: object`\n  - `version?: string`\n\n### Example\n\n```typescript\nimport Dedalus from \'dedalus-labs\';\n\nconst client = new Dedalus();\n\nconst model = await client.models.retrieve(\'model_id\');\n\nconsole.log(model);\n```',
    perLanguage: {
      typescript: {
        method: 'client.models.retrieve',
        example:
          "import Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst model = await client.models.retrieve('model_id');\n\nconsole.log(model.id);",
      },
      python: {
        method: 'models.retrieve',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\nmodel = client.models.retrieve(\n    "model_id",\n)\nprint(model.id)',
      },
      java: {
        method: 'models().retrieve',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.models.Model;\nimport com.dedalus_sdk.api.models.models.ModelRetrieveParams;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        Model model = client.models().retrieve("model_id");\n    }\n}',
      },
      kotlin: {
        method: 'models().retrieve',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.models.Model\nimport com.dedalus_sdk.api.models.models.ModelRetrieveParams\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val model: Model = client.models().retrieve("model_id")\n}',
      },
      go: {
        method: 'client.Models.Get',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tmodel, err := client.Models.Get(context.TODO(), "model_id")\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", model.ID)\n}\n',
      },
      ruby: {
        method: 'models.retrieve',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\nmodel = dedalus.models.retrieve("model_id")\n\nputs(model)',
      },
      cli: {
        method: 'models retrieve',
        example: "dedalus-sdk models retrieve \\\n  --api-key 'My API Key' \\\n  --model-id model_id",
      },
      php: {
        method: 'models->retrieve',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$model = $client->models->retrieve('model_id');\n\nvar_dump($model);",
      },
      csharp: {
        method: 'Models.Retrieve',
        example:
          'ModelRetrieveParams parameters = new() { ModelID = "model_id" };\n\nvar model = await client.Models.Retrieve(parameters);\n\nConsole.WriteLine(model);',
      },
      http: {
        example:
          "curl https://api.dedaluslabs.ai/v1/models/$MODEL_ID \\\n    -H 'User-Agent: Dedalus-SDK' \\\n    -H 'X-SDK-Version: 1.0.0' \\\n    -H \"Authorization: Bearer $DEDALUS_API_KEY\"",
      },
    },
  },
  {
    name: 'create',
    endpoint: '/v1/embeddings',
    httpMethod: 'post',
    summary: 'Create Embeddings',
    description: 'Create embeddings using the configured provider.',
    stainlessPath: '(resource) embeddings > (method) create',
    qualified: 'client.embeddings.create',
    params: [
      'input: string | string[] | number[] | number[][];',
      "model: string | 'text-embedding-ada-002' | 'text-embedding-3-small' | 'text-embedding-3-large';",
      'dimensions?: number;',
      "encoding_format?: 'float' | 'base64';",
      'user?: string;',
    ],
    response:
      "{ data: { embedding: number[]; index: number; object: 'embedding'; }[]; model: string; object: 'list'; usage: { prompt_tokens: number; total_tokens: number; }; }",
    markdown:
      "## create\n\n`client.embeddings.create(input: string | string[] | number[] | number[][], model: string | 'text-embedding-ada-002' | 'text-embedding-3-small' | 'text-embedding-3-large', dimensions?: number, encoding_format?: 'float' | 'base64', user?: string): { data: object[]; model: string; object: 'list'; usage: object; }`\n\n**post** `/v1/embeddings`\n\nCreate embeddings using the configured provider.\n\n### Parameters\n\n- `input: string | string[] | number[] | number[][]`\n  Input text to embed, encoded as a string or array of tokens. To embed multiple inputs in a single request, pass an array of strings or array of token arrays. The input must not exceed the max input tokens for the model (8192 tokens for all embedding models), cannot be an empty string, and any array must be 2048 dimensions or less. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens. In addition to the per-input token limit, all embedding  models enforce a maximum of 300,000 tokens summed across all inputs in a  single request.\n\n- `model: string | 'text-embedding-ada-002' | 'text-embedding-3-small' | 'text-embedding-3-large'`\n  ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them.\n\n- `dimensions?: number`\n  The number of dimensions the resulting output embeddings should have. Only supported in `text-embedding-3` and later models.\n\n- `encoding_format?: 'float' | 'base64'`\n  The format to return the embeddings in. Can be either `float` or [`base64`](https://pypi.org/project/pybase64/).\n\n- `user?: string`\n  A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices#end-user-ids).\n\n### Returns\n\n- `{ data: { embedding: number[]; index: number; object: 'embedding'; }[]; model: string; object: 'list'; usage: { prompt_tokens: number; total_tokens: number; }; }`\n  Schema for EmbeddingResponse.\n\nFields:\n- data (required): list[Embedding]\n- model (required): str\n- object (required): Literal[\"list\"]\n- usage (required): Usage\n\n  - `data: { embedding: number[]; index: number; object: 'embedding'; }[]`\n  - `model: string`\n  - `object: 'list'`\n  - `usage: { prompt_tokens: number; total_tokens: number; }`\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst createEmbeddingResponse = await client.embeddings.create({ input: 'string', model: 'text-embedding-ada-002' });\n\nconsole.log(createEmbeddingResponse);\n```",
    perLanguage: {
      typescript: {
        method: 'client.embeddings.create',
        example:
          "import Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst createEmbeddingResponse = await client.embeddings.create({\n  input: 'string',\n  model: 'text-embedding-ada-002',\n});\n\nconsole.log(createEmbeddingResponse.data);",
      },
      python: {
        method: 'embeddings.create',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\ncreate_embedding_response = client.embeddings.create(\n    input="string",\n    model="text-embedding-ada-002",\n)\nprint(create_embedding_response.data)',
      },
      java: {
        method: 'embeddings().create',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.embeddings.CreateEmbeddingRequest;\nimport com.dedalus_sdk.api.models.embeddings.CreateEmbeddingResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        CreateEmbeddingRequest params = CreateEmbeddingRequest.builder()\n            .input("string")\n            .model(CreateEmbeddingRequest.Model.TEXT_EMBEDDING_ADA_002)\n            .build();\n        CreateEmbeddingResponse createEmbeddingResponse = client.embeddings().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'embeddings().create',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.embeddings.CreateEmbeddingRequest\nimport com.dedalus_sdk.api.models.embeddings.CreateEmbeddingResponse\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val params: CreateEmbeddingRequest = CreateEmbeddingRequest.builder()\n        .input("string")\n        .model(CreateEmbeddingRequest.Model.TEXT_EMBEDDING_ADA_002)\n        .build()\n    val createEmbeddingResponse: CreateEmbeddingResponse = client.embeddings().create(params)\n}',
      },
      go: {
        method: 'client.Embeddings.New',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n\t"github.com/dedalus-labs/dedalus-sdk-go/shared"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tcreateEmbeddingResponse, err := client.Embeddings.New(context.TODO(), githubcomdedaluslabsdedalussdkgo.EmbeddingNewParams{\n\t\tCreateEmbeddingRequest: githubcomdedaluslabsdedalussdkgo.CreateEmbeddingRequestParam{\n\t\t\tInput: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.CreateEmbeddingRequestInputUnionParam](shared.UnionString("string")),\n\t\t\tModel: githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.CreateEmbeddingRequestModelTextEmbeddingAda002),\n\t\t},\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", createEmbeddingResponse.Data)\n}\n',
      },
      ruby: {
        method: 'embeddings.create',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\ncreate_embedding_response = dedalus.embeddings.create(input: "string", model: :"text-embedding-ada-002")\n\nputs(create_embedding_response)',
      },
      cli: {
        method: 'embeddings create',
        example:
          "dedalus-sdk embeddings create \\\n  --api-key 'My API Key' \\\n  --input string \\\n  --model text-embedding-ada-002",
      },
      php: {
        method: 'embeddings->create',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$createEmbeddingResponse = $client->embeddings->create(\n  input: 'string',\n  model: 'text-embedding-ada-002',\n  dimensions: 1,\n  encodingFormat: 'float',\n  user: 'user',\n);\n\nvar_dump($createEmbeddingResponse);",
      },
      csharp: {
        method: 'Embeddings.Create',
        example:
          'EmbeddingCreateParams parameters = new()\n{\n    Input = "string",\n    Model = Model.TextEmbeddingAda002,\n};\n\nvar createEmbeddingResponse = await client.Embeddings.Create(parameters);\n\nConsole.WriteLine(createEmbeddingResponse);',
      },
      http: {
        example:
          'curl https://api.dedaluslabs.ai/v1/embeddings \\\n    -H \'Content-Type: application/json\' \\\n    -H \'User-Agent: Dedalus-SDK\' \\\n    -H \'X-SDK-Version: 1.0.0\' \\\n    -H "Authorization: Bearer $DEDALUS_API_KEY" \\\n    -d \'{\n          "input": "string",\n          "model": "text-embedding-ada-002"\n        }\'',
      },
    },
  },
  {
    name: 'create',
    endpoint: '/v1/audio/speech',
    httpMethod: 'post',
    summary: 'Create Speech',
    description:
      'Generate speech audio from text.\n\nGenerates audio from the input text using text-to-speech models. Supports multiple\nvoices and output formats including mp3, opus, aac, flac, wav, and pcm.\n\nReturns streaming audio data that can be saved to a file or streamed directly to users.',
    stainlessPath: '(resource) audio.speech > (method) create',
    qualified: 'client.audio.speech.create',
    params: [
      'input: string;',
      "model: string | 'tts-1' | 'tts-1-hd' | 'gpt-4o-mini-tts' | 'gpt-4o-mini-tts-2025-12-15';",
      "voice: string | 'alloy' | 'ash' | 'ballad' | 'coral' | 'echo' | 'sage' | 'shimmer' | 'verse' | 'marin' | 'cedar' | { id: string; };",
      'instructions?: string;',
      "response_format?: 'mp3' | 'opus' | 'aac' | 'flac' | 'wav' | 'pcm';",
      'speed?: number;',
      "stream_format?: 'sse' | 'audio';",
    ],
    response: 'string',
    markdown:
      "## create\n\n`client.audio.speech.create(input: string, model: string | 'tts-1' | 'tts-1-hd' | 'gpt-4o-mini-tts' | 'gpt-4o-mini-tts-2025-12-15', voice: string | 'alloy' | 'ash' | 'ballad' | 'coral' | 'echo' | 'sage' | 'shimmer' | 'verse' | 'marin' | 'cedar' | { id: string; }, instructions?: string, response_format?: 'mp3' | 'opus' | 'aac' | 'flac' | 'wav' | 'pcm', speed?: number, stream_format?: 'sse' | 'audio'): string`\n\n**post** `/v1/audio/speech`\n\nGenerate speech audio from text.\n\nGenerates audio from the input text using text-to-speech models. Supports multiple\nvoices and output formats including mp3, opus, aac, flac, wav, and pcm.\n\nReturns streaming audio data that can be saved to a file or streamed directly to users.\n\n### Parameters\n\n- `input: string`\n  The text to generate audio for. The maximum length is 4096 characters.\n\n- `model: string | 'tts-1' | 'tts-1-hd' | 'gpt-4o-mini-tts' | 'gpt-4o-mini-tts-2025-12-15'`\n  One of the available [TTS models](/docs/models#tts): `tts-1`, `tts-1-hd`, `gpt-4o-mini-tts`, or `gpt-4o-mini-tts-2025-12-15`.\n\n- `voice: string | 'alloy' | 'ash' | 'ballad' | 'coral' | 'echo' | 'sage' | 'shimmer' | 'verse' | 'marin' | 'cedar' | { id: string; }`\n  The voice to use when generating the audio. Supported built-in voices are `alloy`, `ash`, `ballad`, `coral`, `echo`, `fable`, `onyx`, `nova`, `sage`, `shimmer`, `verse`, `marin`, and `cedar`. You may also provide a custom voice object with an `id`, for example `{ \"id\": \"voice_1234\" }`. Previews of the voices are available in the [Text to speech guide](/docs/guides/text-to-speech#voice-options).\n\n- `instructions?: string`\n  Control the voice of your generated audio with additional instructions. Does not work with `tts-1` or `tts-1-hd`.\n\n- `response_format?: 'mp3' | 'opus' | 'aac' | 'flac' | 'wav' | 'pcm'`\n  The format to audio in. Supported formats are `mp3`, `opus`, `aac`, `flac`, `wav`, and `pcm`.\n\n- `speed?: number`\n  The speed of the generated audio. Select a value from `0.25` to `4.0`. `1.0` is the default.\n\n- `stream_format?: 'sse' | 'audio'`\n  The format to stream the audio in. Supported formats are `sse` and `audio`. `sse` is not supported for `tts-1` or `tts-1-hd`.\n\n### Returns\n\n- `string`\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst speech = await client.audio.speech.create({\n  input: 'input',\n  model: 'tts-1',\n  voice: 'alloy',\n});\n\nconsole.log(speech);\n\nconst content = await speech.blob()\nconsole.log(content)\n```",
    perLanguage: {
      typescript: {
        method: 'client.audio.speech.create',
        example:
          "import Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst speech = await client.audio.speech.create({\n  input: 'input',\n  model: 'tts-1',\n  voice: 'alloy',\n});\n\nconsole.log(speech);\n\nconst content = await speech.blob();\nconsole.log(content);",
      },
      python: {
        method: 'audio.speech.create',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\nspeech = client.audio.speech.create(\n    input="input",\n    model="tts-1",\n    voice="alloy",\n)\nprint(speech)\ncontent = speech.read()\nprint(content)',
      },
      java: {
        method: 'audio().speech().create',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.core.http.HttpResponse;\nimport com.dedalus_sdk.api.models.audio.speech.SpeechCreateParams;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        SpeechCreateParams params = SpeechCreateParams.builder()\n            .input("input")\n            .model(SpeechCreateParams.Model.TTS_1)\n            .voice(SpeechCreateParams.Voice.UnionMember1.ALLOY)\n            .build();\n        HttpResponse speech = client.audio().speech().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'audio().speech().create',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.core.http.HttpResponse\nimport com.dedalus_sdk.api.models.audio.speech.SpeechCreateParams\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val params: SpeechCreateParams = SpeechCreateParams.builder()\n        .input("input")\n        .model(SpeechCreateParams.Model.TTS_1)\n        .voice(SpeechCreateParams.Voice.UnionMember1.ALLOY)\n        .build()\n    val speech: HttpResponse = client.audio().speech().create(params)\n}',
      },
      go: {
        method: 'client.Audio.Speech.New',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tspeech, err := client.Audio.Speech.New(context.TODO(), githubcomdedaluslabsdedalussdkgo.AudioSpeechNewParams{\n\t\tInput: githubcomdedaluslabsdedalussdkgo.F("input"),\n\t\tModel: githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.AudioSpeechNewParamsModelTts1),\n\t\tVoice: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.AudioSpeechNewParamsVoiceUnion](githubcomdedaluslabsdedalussdkgo.AudioSpeechNewParamsVoiceString(githubcomdedaluslabsdedalussdkgo.AudioSpeechNewParamsVoiceStringAlloy)),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", speech)\n}\n',
      },
      ruby: {
        method: 'audio.speech.create',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\nspeech = dedalus.audio.speech.create(input: "input", model: :"tts-1", voice: :alloy)\n\nputs(speech)',
      },
      cli: {
        method: 'speech create',
        example:
          "dedalus-sdk audio:speech create \\\n  --api-key 'My API Key' \\\n  --input input \\\n  --model tts-1 \\\n  --voice alloy",
      },
      php: {
        method: 'audio->speech->create',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$speech = $client->audio->speech->create(\n  input: 'input',\n  model: 'tts-1',\n  voice: 'alloy',\n  instructions: 'instructions',\n  responseFormat: 'mp3',\n  speed: 0.25,\n  streamFormat: 'sse',\n);\n\nvar_dump($speech);",
      },
      csharp: {
        method: 'Audio.Speech.Create',
        example:
          'SpeechCreateParams parameters = new()\n{\n    Input = "input",\n    Model = Model.Tts1,\n    Voice = UnionMember1.Alloy,\n};\n\nvar speech = await client.Audio.Speech.Create(parameters);\n\nConsole.WriteLine(speech);',
      },
      http: {
        example:
          'curl https://api.dedaluslabs.ai/v1/audio/speech \\\n    -H \'Content-Type: application/json\' \\\n    -H \'User-Agent: Dedalus-SDK\' \\\n    -H \'X-SDK-Version: 1.0.0\' \\\n    -H "Authorization: Bearer $DEDALUS_API_KEY" \\\n    -d \'{\n          "input": "input",\n          "model": "tts-1",\n          "voice": "alloy"\n        }\'',
      },
    },
  },
  {
    name: 'create',
    endpoint: '/v1/audio/transcriptions',
    httpMethod: 'post',
    summary: 'Create Transcription',
    description:
      'Transcribe audio into text.\n\nTranscribes audio files using OpenAI\'s Whisper model. Supports multiple audio formats\nincluding mp3, mp4, mpeg, mpga, m4a, wav, and webm. Maximum file size is 25 MB.\n\nArgs:\n    file: Audio file to transcribe (required)\n    model: Model ID to use (e.g., "openai/whisper-1")\n    language: ISO-639-1 language code (e.g., "en", "es") - improves accuracy\n    prompt: Optional text to guide the model\'s style\n    response_format: Format of the output (json, text, srt, verbose_json, vtt)\n    temperature: Sampling temperature between 0 and 1\n\nReturns:\n    Transcription object with the transcribed text',
    stainlessPath: '(resource) audio.transcriptions > (method) create',
    qualified: 'client.audio.transcriptions.create',
    params: [
      'file: string;',
      'model: string;',
      'language?: string;',
      'prompt?: string;',
      'response_format?: string;',
      'temperature?: number;',
    ],
    response:
      "{ duration: number; language: string; text: string; segments?: { id: number; avg_logprob: number; compression_ratio: number; end: number; no_speech_prob: number; seek: number; start: number; temperature: number; text: string; tokens: number[]; }[]; usage?: { seconds: number; type: 'duration'; }; words?: { end: number; start: number; word: string; }[]; } | { text: string; logprobs?: { token?: string; bytes?: number[]; logprob?: number; }[]; usage?: { input_tokens: number; output_tokens: number; total_tokens: number; type: 'tokens'; input_token_details?: object; } | { seconds: number; type: 'duration'; }; }",
    markdown:
      "## create\n\n`client.audio.transcriptions.create(file: string, model: string, language?: string, prompt?: string, response_format?: string, temperature?: number): { duration: number; language: string; text: string; segments?: object[]; usage?: object; words?: object[]; } | { text: string; logprobs?: object[]; usage?: object | object; }`\n\n**post** `/v1/audio/transcriptions`\n\nTranscribe audio into text.\n\nTranscribes audio files using OpenAI's Whisper model. Supports multiple audio formats\nincluding mp3, mp4, mpeg, mpga, m4a, wav, and webm. Maximum file size is 25 MB.\n\nArgs:\n    file: Audio file to transcribe (required)\n    model: Model ID to use (e.g., \"openai/whisper-1\")\n    language: ISO-639-1 language code (e.g., \"en\", \"es\") - improves accuracy\n    prompt: Optional text to guide the model's style\n    response_format: Format of the output (json, text, srt, verbose_json, vtt)\n    temperature: Sampling temperature between 0 and 1\n\nReturns:\n    Transcription object with the transcribed text\n\n### Parameters\n\n- `file: string`\n\n- `model: string`\n\n- `language?: string`\n\n- `prompt?: string`\n\n- `response_format?: string`\n\n- `temperature?: number`\n\n### Returns\n\n- `{ duration: number; language: string; text: string; segments?: { id: number; avg_logprob: number; compression_ratio: number; end: number; no_speech_prob: number; seek: number; start: number; temperature: number; text: string; tokens: number[]; }[]; usage?: { seconds: number; type: 'duration'; }; words?: { end: number; start: number; word: string; }[]; } | { text: string; logprobs?: { token?: string; bytes?: number[]; logprob?: number; }[]; usage?: { input_tokens: number; output_tokens: number; total_tokens: number; type: 'tokens'; input_token_details?: object; } | { seconds: number; type: 'duration'; }; }`\n  Represents a verbose json transcription response returned by model, based on the provided input.\n\nFields:\n  - language (required): str\n  - duration (required): float\n  - text (required): str\n  - words (optional): list[TranscriptionWord]\n  - segments (optional): list[TranscriptionSegment]\n  - usage (optional): TranscriptTextUsageDuration\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst transcription = await client.audio.transcriptions.create({ file: fs.createReadStream('path/to/file'), model: 'model' });\n\nconsole.log(transcription);\n```",
    perLanguage: {
      typescript: {
        method: 'client.audio.transcriptions.create',
        example:
          "import fs from 'fs';\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst transcription = await client.audio.transcriptions.create({\n  file: fs.createReadStream('path/to/file'),\n  model: 'model',\n});\n\nconsole.log(transcription);",
      },
      python: {
        method: 'audio.transcriptions.create',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\ntranscription = client.audio.transcriptions.create(\n    file=b"Example data",\n    model="model",\n)\nprint(transcription)',
      },
      java: {
        method: 'audio().transcriptions().create',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateParams;\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateResponse;\nimport java.io.ByteArrayInputStream;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        TranscriptionCreateParams params = TranscriptionCreateParams.builder()\n            .file(new ByteArrayInputStream("Example data".getBytes()))\n            .model("model")\n            .build();\n        TranscriptionCreateResponse transcription = client.audio().transcriptions().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'audio().transcriptions().create',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateParams\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateResponse\nimport java.io.ByteArrayInputStream\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val params: TranscriptionCreateParams = TranscriptionCreateParams.builder()\n        .file("Example data".byteInputStream())\n        .model("model")\n        .build()\n    val transcription: TranscriptionCreateResponse = client.audio().transcriptions().create(params)\n}',
      },
      go: {
        method: 'client.Audio.Transcriptions.New',
        example:
          'package main\n\nimport (\n\t"bytes"\n\t"context"\n\t"fmt"\n\t"io"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\ttranscription, err := client.Audio.Transcriptions.New(context.TODO(), githubcomdedaluslabsdedalussdkgo.AudioTranscriptionNewParams{\n\t\tFile:  githubcomdedaluslabsdedalussdkgo.F(io.Reader(bytes.NewBuffer([]byte("Example data")))),\n\t\tModel: githubcomdedaluslabsdedalussdkgo.F("model"),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", transcription)\n}\n',
      },
      ruby: {
        method: 'audio.transcriptions.create',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\ntranscription = dedalus.audio.transcriptions.create(file: StringIO.new("Example data"), model: "model")\n\nputs(transcription)',
      },
      cli: {
        method: 'transcriptions create',
        example:
          "dedalus-sdk audio:transcriptions create \\\n  --api-key 'My API Key' \\\n  --file 'Example data' \\\n  --model model",
      },
      php: {
        method: 'audio->transcriptions->create',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$transcription = $client->audio->transcriptions->create(\n  file: FileParam::fromString('Example data', filename: uniqid('file-upload-', true)),\n  model: 'model',\n  language: 'language',\n  prompt: 'prompt',\n  responseFormat: 'response_format',\n  temperature: 0,\n);\n\nvar_dump($transcription);",
      },
      csharp: {
        method: 'Audio.Transcriptions.Create',
        example:
          'TranscriptionCreateParams parameters = new()\n{\n    File = Encoding.UTF8.GetBytes("Example data"),\n    Model = "model",\n};\n\nvar transcription = await client.Audio.Transcriptions.Create(parameters);\n\nConsole.WriteLine(transcription);',
      },
      http: {
        example:
          "curl https://api.dedaluslabs.ai/v1/audio/transcriptions \\\n    -H 'Content-Type: multipart/form-data' \\\n    -H 'User-Agent: Dedalus-SDK' \\\n    -H 'X-SDK-Version: 1.0.0' \\\n    -H \"Authorization: Bearer $DEDALUS_API_KEY\" \\\n    -F 'file=@/path/to/file' \\\n    -F model=model",
      },
    },
  },
  {
    name: 'create',
    endpoint: '/v1/audio/translations',
    httpMethod: 'post',
    summary: 'Create Translation',
    description:
      'Translate audio into English.\n\nTranslates audio files in any supported language to English text using OpenAI\'s\nWhisper model. Supports the same audio formats as transcription. Maximum file size\nis 25 MB.\n\nArgs:\n    file: Audio file to translate (required)\n    model: Model ID to use (e.g., "openai/whisper-1")\n    prompt: Optional text to guide the model\'s style\n    response_format: Format of the output (json, text, srt, verbose_json, vtt)\n    temperature: Sampling temperature between 0 and 1\n\nReturns:\n    Translation object with the English translation',
    stainlessPath: '(resource) audio.translations > (method) create',
    qualified: 'client.audio.translations.create',
    params: [
      'file: string;',
      'model: string;',
      'prompt?: string;',
      'response_format?: string;',
      'temperature?: number;',
    ],
    response:
      '{ duration: number; language: string; text: string; segments?: { id: number; avg_logprob: number; compression_ratio: number; end: number; no_speech_prob: number; seek: number; start: number; temperature: number; text: string; tokens: number[]; }[]; } | { text: string; }',
    markdown:
      "## create\n\n`client.audio.translations.create(file: string, model: string, prompt?: string, response_format?: string, temperature?: number): { duration: number; language: string; text: string; segments?: object[]; } | { text: string; }`\n\n**post** `/v1/audio/translations`\n\nTranslate audio into English.\n\nTranslates audio files in any supported language to English text using OpenAI's\nWhisper model. Supports the same audio formats as transcription. Maximum file size\nis 25 MB.\n\nArgs:\n    file: Audio file to translate (required)\n    model: Model ID to use (e.g., \"openai/whisper-1\")\n    prompt: Optional text to guide the model's style\n    response_format: Format of the output (json, text, srt, verbose_json, vtt)\n    temperature: Sampling temperature between 0 and 1\n\nReturns:\n    Translation object with the English translation\n\n### Parameters\n\n- `file: string`\n\n- `model: string`\n\n- `prompt?: string`\n\n- `response_format?: string`\n\n- `temperature?: number`\n\n### Returns\n\n- `{ duration: number; language: string; text: string; segments?: { id: number; avg_logprob: number; compression_ratio: number; end: number; no_speech_prob: number; seek: number; start: number; temperature: number; text: string; tokens: number[]; }[]; } | { text: string; }`\n  Fields:  # noqa: D415.\n\n- language (required): str\n- duration (required): float\n- text (required): str\n- segments (optional): list[TranscriptionSegment]\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst translation = await client.audio.translations.create({ file: fs.createReadStream('path/to/file'), model: 'model' });\n\nconsole.log(translation);\n```",
    perLanguage: {
      typescript: {
        method: 'client.audio.translations.create',
        example:
          "import fs from 'fs';\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst translation = await client.audio.translations.create({\n  file: fs.createReadStream('path/to/file'),\n  model: 'model',\n});\n\nconsole.log(translation);",
      },
      python: {
        method: 'audio.translations.create',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\ntranslation = client.audio.translations.create(\n    file=b"Example data",\n    model="model",\n)\nprint(translation)',
      },
      java: {
        method: 'audio().translations().create',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.audio.translations.TranslationCreateParams;\nimport com.dedalus_sdk.api.models.audio.translations.TranslationCreateResponse;\nimport java.io.ByteArrayInputStream;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        TranslationCreateParams params = TranslationCreateParams.builder()\n            .file(new ByteArrayInputStream("Example data".getBytes()))\n            .model("model")\n            .build();\n        TranslationCreateResponse translation = client.audio().translations().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'audio().translations().create',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.audio.translations.TranslationCreateParams\nimport com.dedalus_sdk.api.models.audio.translations.TranslationCreateResponse\nimport java.io.ByteArrayInputStream\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val params: TranslationCreateParams = TranslationCreateParams.builder()\n        .file("Example data".byteInputStream())\n        .model("model")\n        .build()\n    val translation: TranslationCreateResponse = client.audio().translations().create(params)\n}',
      },
      go: {
        method: 'client.Audio.Translations.New',
        example:
          'package main\n\nimport (\n\t"bytes"\n\t"context"\n\t"fmt"\n\t"io"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\ttranslation, err := client.Audio.Translations.New(context.TODO(), githubcomdedaluslabsdedalussdkgo.AudioTranslationNewParams{\n\t\tFile:  githubcomdedaluslabsdedalussdkgo.F(io.Reader(bytes.NewBuffer([]byte("Example data")))),\n\t\tModel: githubcomdedaluslabsdedalussdkgo.F("model"),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", translation)\n}\n',
      },
      ruby: {
        method: 'audio.translations.create',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\ntranslation = dedalus.audio.translations.create(file: StringIO.new("Example data"), model: "model")\n\nputs(translation)',
      },
      cli: {
        method: 'translations create',
        example:
          "dedalus-sdk audio:translations create \\\n  --api-key 'My API Key' \\\n  --file 'Example data' \\\n  --model model",
      },
      php: {
        method: 'audio->translations->create',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$translation = $client->audio->translations->create(\n  file: FileParam::fromString('Example data', filename: uniqid('file-upload-', true)),\n  model: 'model',\n  prompt: 'prompt',\n  responseFormat: 'response_format',\n  temperature: 0,\n);\n\nvar_dump($translation);",
      },
      csharp: {
        method: 'Audio.Translations.Create',
        example:
          'TranslationCreateParams parameters = new()\n{\n    File = Encoding.UTF8.GetBytes("Example data"),\n    Model = "model",\n};\n\nvar translation = await client.Audio.Translations.Create(parameters);\n\nConsole.WriteLine(translation);',
      },
      http: {
        example:
          "curl https://api.dedaluslabs.ai/v1/audio/translations \\\n    -H 'Content-Type: multipart/form-data' \\\n    -H 'User-Agent: Dedalus-SDK' \\\n    -H 'X-SDK-Version: 1.0.0' \\\n    -H \"Authorization: Bearer $DEDALUS_API_KEY\" \\\n    -F 'file=@/path/to/file' \\\n    -F model=model",
      },
    },
  },
  {
    name: 'generate',
    endpoint: '/v1/images/generations',
    httpMethod: 'post',
    summary: 'Create Image',
    description:
      'Generate images from text prompts.\n\nPure image generation models only (DALL-E, GPT Image).\nFor multimodal models like gemini-2.5-flash-image, use /v1/chat/completions.',
    stainlessPath: '(resource) images > (method) generate',
    qualified: 'client.images.generate',
    params: [
      'prompt: string;',
      "background?: 'transparent' | 'opaque' | 'auto';",
      'model?: string;',
      "moderation?: 'low' | 'auto';",
      'n?: number;',
      'output_compression?: number;',
      "output_format?: 'png' | 'jpeg' | 'webp';",
      'partial_images?: number;',
      "quality?: 'auto' | 'high' | 'medium' | 'low' | 'hd' | 'standard';",
      "response_format?: 'url' | 'b64_json';",
      "size?: '256x256' | '512x512' | '1024x1024' | '1536x1024' | '1024x1536' | '1792x1024' | '1024x1792' | 'auto';",
      'stream?: boolean;',
      "style?: 'vivid' | 'natural';",
      'user?: string;',
    ],
    response: '{ created: number; data: { b64_json?: string; revised_prompt?: string; url?: string; }[]; }',
    markdown:
      "## generate\n\n`client.images.generate(prompt: string, background?: 'transparent' | 'opaque' | 'auto', model?: string, moderation?: 'low' | 'auto', n?: number, output_compression?: number, output_format?: 'png' | 'jpeg' | 'webp', partial_images?: number, quality?: 'auto' | 'high' | 'medium' | 'low' | 'hd' | 'standard', response_format?: 'url' | 'b64_json', size?: '256x256' | '512x512' | '1024x1024' | '1536x1024' | '1024x1536' | '1792x1024' | '1024x1792' | 'auto', stream?: boolean, style?: 'vivid' | 'natural', user?: string): { created: number; data: image[]; }`\n\n**post** `/v1/images/generations`\n\nGenerate images from text prompts.\n\nPure image generation models only (DALL-E, GPT Image).\nFor multimodal models like gemini-2.5-flash-image, use /v1/chat/completions.\n\n### Parameters\n\n- `prompt: string`\n  A text description of the desired image(s). The maximum length is 32000 characters for `gpt-image-1`, 1000 characters for `dall-e-2` and 4000 characters for `dall-e-3`.\n\n- `background?: 'transparent' | 'opaque' | 'auto'`\n  Allows to set transparency for the background of the generated image(s).\nThis parameter is only supported for `gpt-image-1`. Must be one of\n`transparent`, `opaque` or `auto` (default value). When `auto` is used, the\nmodel will automatically determine the best background for the image.\n\nIf `transparent`, the output format needs to support transparency, so it\nshould be set to either `png` (default value) or `webp`.\n\n- `model?: string`\n  The model to use for image generation. One of `openai/dall-e-2`, `openai/dall-e-3`, or `openai/gpt-image-1`. Defaults to `openai/dall-e-2` unless a parameter specific to `gpt-image-1` is used.\n\n- `moderation?: 'low' | 'auto'`\n  Control the content-moderation level for images generated by `gpt-image-1`. Must be either `low` for less restrictive filtering or `auto` (default value).\n\n- `n?: number`\n  The number of images to generate. Must be between 1 and 10. For `dall-e-3`, only `n=1` is supported.\n\n- `output_compression?: number`\n  The compression level (0-100%) for the generated images. This parameter is only supported for `gpt-image-1` with the `webp` or `jpeg` output formats, and defaults to 100.\n\n- `output_format?: 'png' | 'jpeg' | 'webp'`\n  The format in which the generated images are returned. This parameter is only supported for `gpt-image-1`. Must be one of `png`, `jpeg`, or `webp`.\n\n- `partial_images?: number`\n  The number of partial images to generate. This parameter is used for\nstreaming responses that return partial images. Value must be between 0 and 3.\nWhen set to 0, the response will be a single image sent in one streaming event.\n\nNote that the final image may be sent before the full number of partial images\nare generated if the full image is generated more quickly.\n\n- `quality?: 'auto' | 'high' | 'medium' | 'low' | 'hd' | 'standard'`\n  The quality of the image that will be generated.\n\n- `auto` (default value) will automatically select the best quality for the given model.\n- `high`, `medium` and `low` are supported for `gpt-image-1`.\n- `hd` and `standard` are supported for `dall-e-3`.\n- `standard` is the only option for `dall-e-2`.\n\n- `response_format?: 'url' | 'b64_json'`\n  The format in which generated images with `dall-e-2` and `dall-e-3` are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter isn't supported for `gpt-image-1` which will always return base64-encoded images.\n\n- `size?: '256x256' | '512x512' | '1024x1024' | '1536x1024' | '1024x1536' | '1792x1024' | '1024x1792' | 'auto'`\n  The size of the generated images. Must be one of `1024x1024`, `1536x1024` (landscape), `1024x1536` (portrait), or `auto` (default value) for `gpt-image-1`, one of `256x256`, `512x512`, or `1024x1024` for `dall-e-2`, and one of `1024x1024`, `1792x1024`, or `1024x1792` for `dall-e-3`.\n\n- `stream?: boolean`\n  Generate the image in streaming mode. Defaults to `false`. See the\n[Image generation guide](https://platform.openai.com/docs/guides/image-generation) for more information.\nThis parameter is only supported for `gpt-image-1`.\n\n- `style?: 'vivid' | 'natural'`\n  The style of the generated images. This parameter is only supported for `dall-e-3`. Must be one of `vivid` or `natural`. Vivid causes the model to lean towards generating hyper-real and dramatic images. Natural causes the model to produce more natural, less hyper-real looking images.\n\n- `user?: string`\n  A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).\n\n### Returns\n\n- `{ created: number; data: { b64_json?: string; revised_prompt?: string; url?: string; }[]; }`\n  Response from image generation.\n\n  - `created: number`\n  - `data: { b64_json?: string; revised_prompt?: string; url?: string; }[]`\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst imagesResponse = await client.images.generate({ prompt: 'A white siamese cat' });\n\nconsole.log(imagesResponse);\n```",
    perLanguage: {
      typescript: {
        method: 'client.images.generate',
        example:
          "import Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst imagesResponse = await client.images.generate({ prompt: 'A white siamese cat' });\n\nconsole.log(imagesResponse.created);",
      },
      python: {
        method: 'images.generate',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\nimages_response = client.images.generate(\n    prompt="A white siamese cat",\n)\nprint(images_response.created)',
      },
      java: {
        method: 'images().generate',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.images.CreateImageRequest;\nimport com.dedalus_sdk.api.models.images.ImagesResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        CreateImageRequest params = CreateImageRequest.builder()\n            .prompt("A white siamese cat")\n            .build();\n        ImagesResponse imagesResponse = client.images().generate(params);\n    }\n}',
      },
      kotlin: {
        method: 'images().generate',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.images.CreateImageRequest\nimport com.dedalus_sdk.api.models.images.ImagesResponse\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val params: CreateImageRequest = CreateImageRequest.builder()\n        .prompt("A white siamese cat")\n        .build()\n    val imagesResponse: ImagesResponse = client.images().generate(params)\n}',
      },
      go: {
        method: 'client.Images.Generate',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\timagesResponse, err := client.Images.Generate(context.TODO(), githubcomdedaluslabsdedalussdkgo.ImageGenerateParams{\n\t\tCreateImageRequest: githubcomdedaluslabsdedalussdkgo.CreateImageRequestParam{\n\t\t\tPrompt: githubcomdedaluslabsdedalussdkgo.F("A white siamese cat"),\n\t\t},\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", imagesResponse.Created)\n}\n',
      },
      ruby: {
        method: 'images.generate',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\nimages_response = dedalus.images.generate(prompt: "A white siamese cat")\n\nputs(images_response)',
      },
      cli: {
        method: 'images generate',
        example:
          "dedalus-sdk images generate \\\n  --api-key 'My API Key' \\\n  --prompt 'A white siamese cat'",
      },
      php: {
        method: 'images->generate',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$imagesResponse = $client->images->generate(\n  prompt: 'A white siamese cat',\n  background: 'transparent',\n  model: 'openai/dall-e-3',\n  moderation: 'auto',\n  n: 1,\n  outputCompression: 85,\n  outputFormat: 'png',\n  partialImages: 0,\n  quality: 'standard',\n  responseFormat: 'url',\n  size: '1024x1024',\n  stream: true,\n  style: 'vivid',\n  user: 'user',\n);\n\nvar_dump($imagesResponse);",
      },
      csharp: {
        method: 'Images.Generate',
        example:
          'ImageGenerateParams parameters = new() { Prompt = "A white siamese cat" };\n\nvar imagesResponse = await client.Images.Generate(parameters);\n\nConsole.WriteLine(imagesResponse);',
      },
      http: {
        example:
          'curl https://api.dedaluslabs.ai/v1/images/generations \\\n    -H \'Content-Type: application/json\' \\\n    -H \'User-Agent: Dedalus-SDK\' \\\n    -H \'X-SDK-Version: 1.0.0\' \\\n    -H "Authorization: Bearer $DEDALUS_API_KEY" \\\n    -d \'{\n          "prompt": "A white siamese cat",\n          "background": "transparent",\n          "model": "openai/dall-e-3",\n          "moderation": "auto",\n          "n": 1,\n          "output_compression": 85,\n          "output_format": "png",\n          "partial_images": 0,\n          "quality": "standard",\n          "response_format": "url",\n          "size": "1024x1024",\n          "stream": true,\n          "style": "vivid"\n        }\'',
      },
    },
  },
  {
    name: 'edit',
    endpoint: '/v1/images/edits',
    httpMethod: 'post',
    summary: 'Edit Image',
    description:
      'Edit images using inpainting.\n\nSupports dall-e-2 and gpt-image-1. Upload an image and optionally a mask\nto indicate which areas to regenerate based on the prompt.',
    stainlessPath: '(resource) images > (method) edit',
    qualified: 'client.images.edit',
    params: [
      'image: string;',
      'prompt: string;',
      'mask?: string;',
      'model?: string;',
      'n?: number;',
      'response_format?: string;',
      'size?: string;',
      'user?: string;',
    ],
    response: '{ created: number; data: { b64_json?: string; revised_prompt?: string; url?: string; }[]; }',
    markdown:
      "## edit\n\n`client.images.edit(image: string, prompt: string, mask?: string, model?: string, n?: number, response_format?: string, size?: string, user?: string): { created: number; data: image[]; }`\n\n**post** `/v1/images/edits`\n\nEdit images using inpainting.\n\nSupports dall-e-2 and gpt-image-1. Upload an image and optionally a mask\nto indicate which areas to regenerate based on the prompt.\n\n### Parameters\n\n- `image: string`\n\n- `prompt: string`\n\n- `mask?: string`\n\n- `model?: string`\n\n- `n?: number`\n\n- `response_format?: string`\n\n- `size?: string`\n\n- `user?: string`\n\n### Returns\n\n- `{ created: number; data: { b64_json?: string; revised_prompt?: string; url?: string; }[]; }`\n  Response from image generation.\n\n  - `created: number`\n  - `data: { b64_json?: string; revised_prompt?: string; url?: string; }[]`\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst imagesResponse = await client.images.edit({ image: fs.createReadStream('path/to/file'), prompt: 'prompt' });\n\nconsole.log(imagesResponse);\n```",
    perLanguage: {
      typescript: {
        method: 'client.images.edit',
        example:
          "import fs from 'fs';\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst imagesResponse = await client.images.edit({\n  image: fs.createReadStream('path/to/file'),\n  prompt: 'prompt',\n});\n\nconsole.log(imagesResponse.created);",
      },
      python: {
        method: 'images.edit',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\nimages_response = client.images.edit(\n    image=b"Example data",\n    prompt="prompt",\n)\nprint(images_response.created)',
      },
      java: {
        method: 'images().edit',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.images.ImageEditParams;\nimport com.dedalus_sdk.api.models.images.ImagesResponse;\nimport java.io.ByteArrayInputStream;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        ImageEditParams params = ImageEditParams.builder()\n            .image(new ByteArrayInputStream("Example data".getBytes()))\n            .prompt("prompt")\n            .build();\n        ImagesResponse imagesResponse = client.images().edit(params);\n    }\n}',
      },
      kotlin: {
        method: 'images().edit',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.images.ImageEditParams\nimport com.dedalus_sdk.api.models.images.ImagesResponse\nimport java.io.ByteArrayInputStream\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val params: ImageEditParams = ImageEditParams.builder()\n        .image("Example data".byteInputStream())\n        .prompt("prompt")\n        .build()\n    val imagesResponse: ImagesResponse = client.images().edit(params)\n}',
      },
      go: {
        method: 'client.Images.Edit',
        example:
          'package main\n\nimport (\n\t"bytes"\n\t"context"\n\t"fmt"\n\t"io"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\timagesResponse, err := client.Images.Edit(context.TODO(), githubcomdedaluslabsdedalussdkgo.ImageEditParams{\n\t\tImage:  githubcomdedaluslabsdedalussdkgo.F(io.Reader(bytes.NewBuffer([]byte("Example data")))),\n\t\tPrompt: githubcomdedaluslabsdedalussdkgo.F("prompt"),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", imagesResponse.Created)\n}\n',
      },
      ruby: {
        method: 'images.edit',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\nimages_response = dedalus.images.edit(image: StringIO.new("Example data"), prompt: "prompt")\n\nputs(images_response)',
      },
      cli: {
        method: 'images edit',
        example:
          "dedalus-sdk images edit \\\n  --api-key 'My API Key' \\\n  --image 'Example data' \\\n  --prompt prompt",
      },
      php: {
        method: 'images->edit',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$imagesResponse = $client->images->edit(\n  image: FileParam::fromString('Example data', filename: uniqid('file-upload-', true)),\n  prompt: 'prompt',\n  mask: FileParam::fromString('Example data', filename: uniqid('file-upload-', true)),\n  model: 'model',\n  n: 0,\n  responseFormat: 'response_format',\n  size: 'size',\n  user: 'user',\n);\n\nvar_dump($imagesResponse);",
      },
      csharp: {
        method: 'Images.Edit',
        example:
          'ImageEditParams parameters = new()\n{\n    Image = Encoding.UTF8.GetBytes("Example data"),\n    Prompt = "prompt",\n};\n\nvar imagesResponse = await client.Images.Edit(parameters);\n\nConsole.WriteLine(imagesResponse);',
      },
      http: {
        example:
          "curl https://api.dedaluslabs.ai/v1/images/edits \\\n    -H 'Content-Type: multipart/form-data' \\\n    -H 'User-Agent: Dedalus-SDK' \\\n    -H 'X-SDK-Version: 1.0.0' \\\n    -H \"Authorization: Bearer $DEDALUS_API_KEY\" \\\n    -F 'image=@/path/to/image' \\\n    -F prompt=prompt",
      },
    },
  },
  {
    name: 'create_variation',
    endpoint: '/v1/images/variations',
    httpMethod: 'post',
    summary: 'Create Variation',
    description: 'Create variations of an image.\n\nDALL·E 2 only. Upload an image to generate variations.',
    stainlessPath: '(resource) images > (method) create_variation',
    qualified: 'client.images.createVariation',
    params: [
      'image: string;',
      'model?: string;',
      'n?: number;',
      'response_format?: string;',
      'size?: string;',
      'user?: string;',
    ],
    response: '{ created: number; data: { b64_json?: string; revised_prompt?: string; url?: string; }[]; }',
    markdown:
      "## create_variation\n\n`client.images.createVariation(image: string, model?: string, n?: number, response_format?: string, size?: string, user?: string): { created: number; data: image[]; }`\n\n**post** `/v1/images/variations`\n\nCreate variations of an image.\n\nDALL·E 2 only. Upload an image to generate variations.\n\n### Parameters\n\n- `image: string`\n\n- `model?: string`\n\n- `n?: number`\n\n- `response_format?: string`\n\n- `size?: string`\n\n- `user?: string`\n\n### Returns\n\n- `{ created: number; data: { b64_json?: string; revised_prompt?: string; url?: string; }[]; }`\n  Response from image generation.\n\n  - `created: number`\n  - `data: { b64_json?: string; revised_prompt?: string; url?: string; }[]`\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst imagesResponse = await client.images.createVariation({ image: fs.createReadStream('path/to/file') });\n\nconsole.log(imagesResponse);\n```",
    perLanguage: {
      typescript: {
        method: 'client.images.createVariation',
        example:
          "import fs from 'fs';\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst imagesResponse = await client.images.createVariation({\n  image: fs.createReadStream('path/to/file'),\n});\n\nconsole.log(imagesResponse.created);",
      },
      python: {
        method: 'images.create_variation',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\nimages_response = client.images.create_variation(\n    image=b"Example data",\n)\nprint(images_response.created)',
      },
      java: {
        method: 'images().createVariation',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.images.ImageCreateVariationParams;\nimport com.dedalus_sdk.api.models.images.ImagesResponse;\nimport java.io.ByteArrayInputStream;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        ImageCreateVariationParams params = ImageCreateVariationParams.builder()\n            .image(new ByteArrayInputStream("Example data".getBytes()))\n            .build();\n        ImagesResponse imagesResponse = client.images().createVariation(params);\n    }\n}',
      },
      kotlin: {
        method: 'images().createVariation',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.images.ImageCreateVariationParams\nimport com.dedalus_sdk.api.models.images.ImagesResponse\nimport java.io.ByteArrayInputStream\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val params: ImageCreateVariationParams = ImageCreateVariationParams.builder()\n        .image("Example data".byteInputStream())\n        .build()\n    val imagesResponse: ImagesResponse = client.images().createVariation(params)\n}',
      },
      go: {
        method: 'client.Images.NewVariation',
        example:
          'package main\n\nimport (\n\t"bytes"\n\t"context"\n\t"fmt"\n\t"io"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\timagesResponse, err := client.Images.NewVariation(context.TODO(), githubcomdedaluslabsdedalussdkgo.ImageNewVariationParams{\n\t\tImage: githubcomdedaluslabsdedalussdkgo.F(io.Reader(bytes.NewBuffer([]byte("Example data")))),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", imagesResponse.Created)\n}\n',
      },
      ruby: {
        method: 'images.create_variation',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\nimages_response = dedalus.images.create_variation(image: StringIO.new("Example data"))\n\nputs(images_response)',
      },
      cli: {
        method: 'images create_variation',
        example:
          "dedalus-sdk images create-variation \\\n  --api-key 'My API Key' \\\n  --image 'Example data'",
      },
      php: {
        method: 'images->createVariation',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$imagesResponse = $client->images->createVariation(\n  image: FileParam::fromString('Example data', filename: uniqid('file-upload-', true)),\n  model: 'model',\n  n: 0,\n  responseFormat: 'response_format',\n  size: 'size',\n  user: 'user',\n);\n\nvar_dump($imagesResponse);",
      },
      csharp: {
        method: 'Images.CreateVariation',
        example:
          'ImageCreateVariationParams parameters = new()\n{\n    Image = Encoding.UTF8.GetBytes("Example data")\n};\n\nvar imagesResponse = await client.Images.CreateVariation(parameters);\n\nConsole.WriteLine(imagesResponse);',
      },
      http: {
        example:
          "curl https://api.dedaluslabs.ai/v1/images/variations \\\n    -H 'Content-Type: multipart/form-data' \\\n    -H 'User-Agent: Dedalus-SDK' \\\n    -H 'X-SDK-Version: 1.0.0' \\\n    -H \"Authorization: Bearer $DEDALUS_API_KEY\" \\\n    -F 'image=@/path/to/image'",
      },
    },
  },
  {
    name: 'process',
    endpoint: '/v1/ocr',
    httpMethod: 'post',
    summary: 'Process Ocr',
    description:
      'Process a document through Mistral OCR.\n\nExtracts text from PDFs and images, returning markdown-formatted content.',
    stainlessPath: '(resource) ocr > (method) process',
    qualified: 'client.ocr.process',
    params: ['document: { document_url: string; type?: string; };', 'model?: string;'],
    response: '{ model: string; pages: { index: number; markdown: string; }[]; usage?: object; }',
    markdown:
      "## process\n\n`client.ocr.process(document: { document_url: string; type?: string; }, model?: string): { model: string; pages: ocr_page[]; usage?: object; }`\n\n**post** `/v1/ocr`\n\nProcess a document through Mistral OCR.\n\nExtracts text from PDFs and images, returning markdown-formatted content.\n\n### Parameters\n\n- `document: { document_url: string; type?: string; }`\n  Document input for OCR.\n  - `document_url: string`\n    Data URI with base64-encoded document\n  - `type?: string`\n\n- `model?: string`\n\n### Returns\n\n- `{ model: string; pages: { index: number; markdown: string; }[]; usage?: object; }`\n  OCR response schema.\n\n  - `model: string`\n  - `pages: { index: number; markdown: string; }[]`\n  - `usage?: object`\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst ocrResponse = await client.ocr.process({ document: { document_url: 'document_url' } });\n\nconsole.log(ocrResponse);\n```",
    perLanguage: {
      typescript: {
        method: 'client.ocr.process',
        example:
          "import Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst ocrResponse = await client.ocr.process({ document: { document_url: 'document_url' } });\n\nconsole.log(ocrResponse.model);",
      },
      python: {
        method: 'ocr.process',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\nocr_response = client.ocr.process(\n    document={\n        "document_url": "document_url"\n    },\n)\nprint(ocr_response.model)',
      },
      java: {
        method: 'ocr().process',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.ocr.OcrDocument;\nimport com.dedalus_sdk.api.models.ocr.OcrRequest;\nimport com.dedalus_sdk.api.models.ocr.OcrResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        OcrRequest params = OcrRequest.builder()\n            .document(OcrDocument.builder()\n                .documentUrl("document_url")\n                .build())\n            .build();\n        OcrResponse ocrResponse = client.ocr().process(params);\n    }\n}',
      },
      kotlin: {
        method: 'ocr().process',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.ocr.OcrDocument\nimport com.dedalus_sdk.api.models.ocr.OcrRequest\nimport com.dedalus_sdk.api.models.ocr.OcrResponse\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val params: OcrRequest = OcrRequest.builder()\n        .document(OcrDocument.builder()\n            .documentUrl("document_url")\n            .build())\n        .build()\n    val ocrResponse: OcrResponse = client.ocr().process(params)\n}',
      },
      go: {
        method: 'client.OCR.Process',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tocrResponse, err := client.OCR.Process(context.TODO(), githubcomdedaluslabsdedalussdkgo.OCRProcessParams{\n\t\tOCRRequest: githubcomdedaluslabsdedalussdkgo.OCRRequestParam{\n\t\t\tDocument: githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.OCRDocumentParam{\n\t\t\t\tDocumentURL: githubcomdedaluslabsdedalussdkgo.F("document_url"),\n\t\t\t}),\n\t\t},\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", ocrResponse.Model)\n}\n',
      },
      ruby: {
        method: 'ocr.process',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\nocr_response = dedalus.ocr.process(document: {document_url: "document_url"})\n\nputs(ocr_response)',
      },
      cli: {
        method: 'ocr process',
        example:
          "dedalus-sdk ocr process \\\n  --api-key 'My API Key' \\\n  --document '{document_url: document_url}'",
      },
      php: {
        method: 'ocr->process',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$ocrResponse = $client->ocr->process(\n  document: ['documentURL' => 'document_url', 'type' => 'type'], model: 'model'\n);\n\nvar_dump($ocrResponse);",
      },
      csharp: {
        method: 'Ocr.Process',
        example:
          'OcrProcessParams parameters = new()\n{\n    Document = new()\n    {\n        DocumentUrl = "document_url",\n        Type = "type",\n    },\n};\n\nvar ocrResponse = await client.Ocr.Process(parameters);\n\nConsole.WriteLine(ocrResponse);',
      },
      http: {
        example:
          'curl https://api.dedaluslabs.ai/v1/ocr \\\n    -H \'Content-Type: application/json\' \\\n    -H \'User-Agent: Dedalus-SDK\' \\\n    -H \'X-SDK-Version: 1.0.0\' \\\n    -H "Authorization: Bearer $DEDALUS_API_KEY" \\\n    -d \'{\n          "document": {\n            "document_url": "document_url"\n          }\n        }\'',
      },
    },
  },
  {
    name: 'create',
    endpoint: '/v1/responses',
    httpMethod: 'post',
    summary: 'Create Response',
    description:
      "Create a response using the OpenAI Responses API.\n\nThis endpoint routes directly to OpenAI's Responses API.\nOnly OpenAI models are supported.",
    stainlessPath: '(resource) responses > (method) create',
    qualified: 'client.responses.create',
    params: [
      'background?: boolean;',
      'conversation?: string | { id: string; };',
      'credentials?: { connection_name: string; values: object; } | { connection_name: string; values: object; }[];',
      'frequency_penalty?: number;',
      'include?: string[];',
      'input?: string | object[];',
      'instructions?: string | object[];',
      'max_output_tokens?: number;',
      'max_tool_calls?: number;',
      'mcp_servers?: string | { name: string; credentials?: object; slug?: string; url?: string; version?: string; } | string | { name: string; credentials?: object; slug?: string; url?: string; version?: string; }[];',
      'metadata?: object;',
      "model?: string | { model: string; settings?: { attributes?: object; audio?: json_object_input; deferred?: boolean; extra_args?: object; extra_headers?: object; extra_query?: object; frequency_penalty?: number; generation_config?: json_object_input; include_usage?: boolean; input_audio_format?: string; input_audio_transcription?: json_object_input; logit_bias?: object; logprobs?: boolean; max_completion_tokens?: number; max_tokens?: number; metadata?: object; modalities?: string[]; n?: number; output_audio_format?: string; parallel_tool_calls?: boolean; prediction?: json_object_input; presence_penalty?: number; prompt_cache_key?: string; reasoning?: reasoning; reasoning_effort?: string; response_format?: json_object_input; safety_identifier?: string; safety_settings?: json_object_input[]; search_parameters?: json_object_input; seed?: number; service_tier?: string; stop?: string | string[]; store?: boolean; stream?: boolean; stream_options?: json_object_input; structured_output?: object; system_instruction?: json_object_input; temperature?: number; thinking?: json_object_input; timeout?: number; tool_choice?: tool_choice; tool_config?: json_object_input; top_k?: number; top_logprobs?: number; top_p?: number; truncation?: 'auto' | 'disabled'; turn_detection?: json_object_input; user?: string; verbosity?: string; voice?: string; web_search_options?: json_object_input; }; } | string | { model: string; settings?: object; }[];",
      'parallel_tool_calls?: boolean;',
      'presence_penalty?: number;',
      'previous_response_id?: string;',
      'prompt?: { id: string; variables?: object; version?: string; };',
      'prompt_cache_key?: string;',
      'reasoning?: object;',
      'safety_identifier?: string;',
      "service_tier?: 'auto' | 'default';",
      'store?: boolean;',
      'stream?: boolean;',
      'stream_options?: object;',
      'temperature?: number;',
      'text?: object;',
      'tool_choice?: string | object;',
      'tools?: object[];',
      'top_logprobs?: number;',
      'top_p?: number;',
      "truncation?: 'auto' | 'disabled';",
      'user?: string;',
    ],
    response:
      "{ id: string; created_at: number; model: string; output: object[]; status: 'completed' | 'failed' | 'in_progress' | 'cancelled' | 'queued' | 'incomplete'; background?: boolean; completed_at?: number; conversation?: object; error?: object; frequency_penalty?: number; incomplete_details?: object; instructions?: string | object[]; max_output_tokens?: number; max_tool_calls?: number; mcp_server_errors?: object; mcp_tool_results?: { arguments: json_object_input; is_error: boolean; server_name: string; tool_name: string; duration_ms?: number; result?: json_value_input; }[]; metadata?: object; object?: 'response'; output_text?: string; parallel_tool_calls?: boolean; presence_penalty?: number; previous_response_id?: string; prompt_cache_key?: string; reasoning?: object; safety_identifier?: string; service_tier?: string; store?: boolean; temperature?: number; text?: object; tool_choice?: string | object; tools?: object[]; tools_executed?: string[]; top_logprobs?: number; top_p?: number; truncation?: string; usage?: object; }",
    markdown:
      "## create\n\n`client.responses.create(background?: boolean, conversation?: string | { id: string; }, credentials?: { connection_name: string; values: object; } | object[], frequency_penalty?: number, include?: string[], input?: string | object[], instructions?: string | object[], max_output_tokens?: number, max_tool_calls?: number, mcp_servers?: string | { name: string; credentials?: object; slug?: string; url?: string; version?: string; } | string | object[], metadata?: object, model?: string | { model: string; settings?: model_settings; } | string | object[], parallel_tool_calls?: boolean, presence_penalty?: number, previous_response_id?: string, prompt?: { id: string; variables?: object; version?: string; }, prompt_cache_key?: string, reasoning?: object, safety_identifier?: string, service_tier?: 'auto' | 'default', store?: boolean, stream?: boolean, stream_options?: object, temperature?: number, text?: object, tool_choice?: string | object, tools?: object[], top_logprobs?: number, top_p?: number, truncation?: 'auto' | 'disabled', user?: string): { id: string; created_at: number; model: string; output: json_object_input[]; status: 'completed' | 'failed' | 'in_progress' | 'cancelled' | 'queued' | 'incomplete'; background?: boolean; completed_at?: number; conversation?: json_object_input; error?: json_object_input; frequency_penalty?: number; incomplete_details?: object; instructions?: string | json_object_input[]; max_output_tokens?: number; max_tool_calls?: number; mcp_server_errors?: object; mcp_tool_results?: mcp_tool_result[]; metadata?: object; object?: 'response'; output_text?: string; parallel_tool_calls?: boolean; presence_penalty?: number; previous_response_id?: string; prompt_cache_key?: string; reasoning?: json_object_input; safety_identifier?: string; service_tier?: string; store?: boolean; temperature?: number; text?: json_object_input; tool_choice?: string | json_object_input; tools?: json_object_input[]; tools_executed?: string[]; top_logprobs?: number; top_p?: number; truncation?: string; usage?: json_object_input; }`\n\n**post** `/v1/responses`\n\nCreate a response using the OpenAI Responses API.\n\nThis endpoint routes directly to OpenAI's Responses API.\nOnly OpenAI models are supported.\n\n### Parameters\n\n- `background?: boolean`\n  Whether to run the model response in the background.\n[Learn more](https://platform.openai.com/docs/guides/background).\n\n- `conversation?: string | { id: string; }`\n  Conversation that this response belongs to. Items from this conversation are prepended to the input items, and output items from this response are automatically added after completion.\n\n- `credentials?: { connection_name: string; values: object; } | { connection_name: string; values: object; }[]`\n  Credentials for MCP server authentication. Each credential is matched to servers by connection name.\n\n- `frequency_penalty?: number`\n  Penalizes new tokens based on their frequency in the text so far.\n\n- `include?: string[]`\n  Specify additional output data to include in the model response. Currently\nsupported values are:\n- `web_search_call.action.sources`: Include the sources of the web search tool call.\n- `code_interpreter_call.outputs`: Includes the outputs of python code execution\n  in code interpreter tool call items.\n- `computer_call_output.output.image_url`: Include image urls from the computer call output.\n- `file_search_call.results`: Include the search results of\n  the file search tool call.\n- `message.input_image.image_url`: Include image urls from the input message.\n- `message.output_text.logprobs`: Include logprobs with assistant messages.\n- `reasoning.encrypted_content`: Includes an encrypted version of reasoning\n  tokens in reasoning item outputs. This enables reasoning items to be used in\n  multi-turn conversations when using the Responses API statelessly (like\n  when the `store` parameter is set to `false`, or when an organization is\n  enrolled in the zero data retention program).\n\n- `input?: string | object[]`\n  Text, image, or file inputs to the model, used to generate a response.\n\nLearn more:\n- [Text inputs and outputs](https://platform.openai.com/docs/guides/text)\n- [Image inputs](https://platform.openai.com/docs/guides/images)\n- [File inputs](https://platform.openai.com/docs/guides/pdf-files)\n- [Conversation state](https://platform.openai.com/docs/guides/conversation-state)\n- [Function calling](https://platform.openai.com/docs/guides/function-calling)\n\n- `instructions?: string | object[]`\n  A system (or developer) message inserted into the model's context.\n\nWhen using along with `previous_response_id`, the instructions from a previous\nresponse will not be carried over to the next response. This makes it simple\nto swap out system (or developer) messages in new responses.\n\n- `max_output_tokens?: number`\n  An upper bound for the number of tokens that can be generated for a response, including visible output tokens and [reasoning tokens](https://platform.openai.com/docs/guides/reasoning).\n\n- `max_tool_calls?: number`\n  The maximum number of total calls to built-in tools that can be processed in a response. This maximum number applies across all built-in tool calls, not per individual tool. Any further attempts to call a tool by the model will be ignored.\n\n- `mcp_servers?: string | { name: string; credentials?: object; slug?: string; url?: string; version?: string; } | string | { name: string; credentials?: object; slug?: string; url?: string; version?: string; }[]`\n  MCP server identifiers. Accepts marketplace slugs, URLs, or MCPServerSpec objects. MCP tools are executed server-side and billed separately.\n\n- `metadata?: object`\n  Set of up to 16 key-value string pairs that can be attached to the response for structured metadata and later querying via the API or dashboard.\n\n- `model?: string | { model: string; settings?: { attributes?: object; audio?: json_object_input; deferred?: boolean; extra_args?: object; extra_headers?: object; extra_query?: object; frequency_penalty?: number; generation_config?: json_object_input; include_usage?: boolean; input_audio_format?: string; input_audio_transcription?: json_object_input; logit_bias?: object; logprobs?: boolean; max_completion_tokens?: number; max_tokens?: number; metadata?: object; modalities?: string[]; n?: number; output_audio_format?: string; parallel_tool_calls?: boolean; prediction?: json_object_input; presence_penalty?: number; prompt_cache_key?: string; reasoning?: reasoning; reasoning_effort?: string; response_format?: json_object_input; safety_identifier?: string; safety_settings?: json_object_input[]; search_parameters?: json_object_input; seed?: number; service_tier?: string; stop?: string | string[]; store?: boolean; stream?: boolean; stream_options?: json_object_input; structured_output?: object; system_instruction?: json_object_input; temperature?: number; thinking?: json_object_input; timeout?: number; tool_choice?: tool_choice; tool_config?: json_object_input; top_k?: number; top_logprobs?: number; top_p?: number; truncation?: 'auto' | 'disabled'; turn_detection?: json_object_input; user?: string; verbosity?: string; voice?: string; web_search_options?: json_object_input; }; } | string | { model: string; settings?: object; }[]`\n  Model ID used to generate the response, like `gpt-4o` or `o3`. OpenAI\noffers a wide range of models with different capabilities, performance\ncharacteristics, and price points. Refer to the [model guide](https://platform.openai.com/docs/models)\nto browse and compare available models.\n\n- `parallel_tool_calls?: boolean`\n  Whether to allow the model to run tool calls in parallel.\n\n- `presence_penalty?: number`\n  Penalizes new tokens based on whether they appear in the text so far.\n\n- `previous_response_id?: string`\n  Unique ID of the previous response to continue from when creating multi-turn conversations. Cannot be used together with `conversation`.\n\n- `prompt?: { id: string; variables?: object; version?: string; }`\n  Stored prompt template reference (BYOK).\n  - `id: string`\n    Identifier of the stored prompt.\n  - `variables?: object`\n    Variables to substitute into the stored prompt template.\n  - `version?: string`\n    Optional version identifier of the stored prompt.\n\n- `prompt_cache_key?: string`\n  Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](https://platform.openai.com/docs/guides/prompt-caching).\n\n- `reasoning?: object`\n  **gpt-5 and o-series models only**\n\nConfiguration options for\n[reasoning models](https://platform.openai.com/docs/guides/reasoning).\n\n- `safety_identifier?: string`\n  A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies.\nThe IDs should be a string that uniquely identifies each user. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#safety-identifiers).\n\n- `service_tier?: 'auto' | 'default'`\n  Specifies the processing type used for serving the request.\n  - If set to 'auto', then the request will be processed with the service tier configured in the Project settings. Unless otherwise configured, the Project will use 'default'.\n  - If set to 'default', then the request will be processed with the standard pricing and performance for the selected model.\n  - If set to '[flex](https://platform.openai.com/docs/guides/flex-processing)' or '[priority](https://openai.com/api-priority-processing/)', then the request will be processed with the corresponding service tier.\n  - When not set, the default behavior is 'auto'.\n\n  When the `service_tier` parameter is set, the response body will include the `service_tier` value based on the processing mode actually used to serve the request. This response value may be different from the value set in the parameter.\n\n- `store?: boolean`\n  Whether to store the generated response for later retrieval via the Responses API.\n\n- `stream?: boolean`\n  If set to true, the model response data will be streamed to the client\nas it is generated using [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format).\nSee the [Streaming section below](https://platform.openai.com/docs/api-reference/responses-streaming)\nfor more information.\n\n- `stream_options?: object`\n  Options for streaming response. Only set this when you set `stream: true`.\n\n- `temperature?: number`\n  What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.\nWe generally recommend altering this or `top_p` but not both.\n\n- `text?: object`\n  Configuration options for a text response from the model. Can be plain\ntext or structured JSON data. Learn more:\n- [Text inputs and outputs](https://platform.openai.com/docs/guides/text)\n- [Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs)\n\n- `tool_choice?: string | object`\n  How the model should select which tool (or tools) to use when generating\na response. See the `tools` parameter to see how to specify which tools\nthe model can call.\n\n- `tools?: object[]`\n  An array of tools the model may call while generating a response. You\ncan specify which tool to use by setting the `tool_choice` parameter.\n\nWe support the following categories of tools:\n- **Built-in tools**: Tools that are provided by OpenAI that extend the\n  model's capabilities, like [web search](https://platform.openai.com/docs/guides/tools-web-search)\n  or [file search](https://platform.openai.com/docs/guides/tools-file-search). Learn more about\n  [built-in tools](https://platform.openai.com/docs/guides/tools).\n- **MCP Tools**: Integrations with third-party systems via custom MCP servers\n  or predefined connectors such as Google Drive and SharePoint. Learn more about\n  [MCP Tools](https://platform.openai.com/docs/guides/tools-connectors-mcp).\n- **Function calls (custom tools)**: Functions that are defined by you,\n  enabling the model to call your own code with strongly typed arguments\n  and outputs. Learn more about\n  [function calling](https://platform.openai.com/docs/guides/function-calling). You can also use\n  custom tools to call your own code.\n\n- `top_logprobs?: number`\n  An integer between 0 and 20 specifying the number of most likely tokens to\nreturn at each token position, each with an associated log probability.\n\n- `top_p?: number`\n  An alternative to sampling with temperature, called nucleus sampling,\nwhere the model considers the results of the tokens with top_p probability\nmass. So 0.1 means only the tokens comprising the top 10% probability mass\nare considered.\n\nWe generally recommend altering this or `temperature` but not both.\n\n- `truncation?: 'auto' | 'disabled'`\n  The truncation strategy to use for the model response.\n- `auto`: If the input to this Response exceeds\n  the model's context window size, the model will truncate the\n  response to fit the context window by dropping items from the beginning of the conversation.\n- `disabled` (default): If the input size will exceed the context window\n  size for a model, the request will fail with a 400 error.\n\n- `user?: string`\n  This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to maintain caching optimizations.\nA stable identifier for your end-users.\nUsed to boost cache hit rates by better bucketing similar requests and  to help OpenAI detect and prevent abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#safety-identifiers).\n\n### Returns\n\n- `{ id: string; created_at: number; model: string; output: object[]; status: 'completed' | 'failed' | 'in_progress' | 'cancelled' | 'queued' | 'incomplete'; background?: boolean; completed_at?: number; conversation?: object; error?: object; frequency_penalty?: number; incomplete_details?: object; instructions?: string | object[]; max_output_tokens?: number; max_tool_calls?: number; mcp_server_errors?: object; mcp_tool_results?: { arguments: json_object_input; is_error: boolean; server_name: string; tool_name: string; duration_ms?: number; result?: json_value_input; }[]; metadata?: object; object?: 'response'; output_text?: string; parallel_tool_calls?: boolean; presence_penalty?: number; previous_response_id?: string; prompt_cache_key?: string; reasoning?: object; safety_identifier?: string; service_tier?: string; store?: boolean; temperature?: number; text?: object; tool_choice?: string | object; tools?: object[]; tools_executed?: string[]; top_logprobs?: number; top_p?: number; truncation?: string; usage?: object; }`\n  Responses API response with Dedalus extensions.\n\n  - `id: string`\n  - `created_at: number`\n  - `model: string`\n  - `output: object[]`\n  - `status: 'completed' | 'failed' | 'in_progress' | 'cancelled' | 'queued' | 'incomplete'`\n  - `background?: boolean`\n  - `completed_at?: number`\n  - `conversation?: object`\n  - `error?: object`\n  - `frequency_penalty?: number`\n  - `incomplete_details?: object`\n  - `instructions?: string | object[]`\n  - `max_output_tokens?: number`\n  - `max_tool_calls?: number`\n  - `mcp_server_errors?: object`\n  - `mcp_tool_results?: { arguments: object; is_error: boolean; server_name: string; tool_name: string; duration_ms?: number; result?: string | number | boolean | object | string | number | boolean | object | json_value_input[][]; }[]`\n  - `metadata?: object`\n  - `object?: 'response'`\n  - `output_text?: string`\n  - `parallel_tool_calls?: boolean`\n  - `presence_penalty?: number`\n  - `previous_response_id?: string`\n  - `prompt_cache_key?: string`\n  - `reasoning?: object`\n  - `safety_identifier?: string`\n  - `service_tier?: string`\n  - `store?: boolean`\n  - `temperature?: number`\n  - `text?: object`\n  - `tool_choice?: string | object`\n  - `tools?: object[]`\n  - `tools_executed?: string[]`\n  - `top_logprobs?: number`\n  - `top_p?: number`\n  - `truncation?: string`\n  - `usage?: object`\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst response = await client.responses.create();\n\nconsole.log(response);\n```",
    perLanguage: {
      typescript: {
        method: 'client.responses.create',
        example:
          "import Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst response = await client.responses.create();\n\nconsole.log(response.id);",
      },
      python: {
        method: 'responses.create',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\nresponse = client.responses.create()\nprint(response.id)',
      },
      java: {
        method: 'responses().create',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.responses.Response;\nimport com.dedalus_sdk.api.models.responses.ResponseCreateParams;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        ResponseCreateParams params = ResponseCreateParams.builder().build();\n        Response response = client.responses().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'responses().create',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.responses.Response\nimport com.dedalus_sdk.api.models.responses.ResponseCreateParams\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val params: ResponseCreateParams = ResponseCreateParams.builder().build()\n    val response: Response = client.responses().create(params)\n}',
      },
      go: {
        method: 'client.Responses.New',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tresponse, err := client.Responses.New(context.TODO(), githubcomdedaluslabsdedalussdkgo.ResponseNewParams{\n\t\tResponseCreateParams: githubcomdedaluslabsdedalussdkgo.ResponseCreateParams{},\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", response.ID)\n}\n',
      },
      ruby: {
        method: 'responses.create',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\nresponse = dedalus.responses.create\n\nputs(response)',
      },
      cli: {
        method: 'responses create',
        example: "dedalus-sdk responses create \\\n  --api-key 'My API Key'",
      },
      php: {
        method: 'responses->create',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$response = $client->responses->create(\n  background: true,\n  conversation: 'string',\n  credentials: [\n    'connectionName' => 'external-service', 'values' => ['api_key' => 'sk-...']\n  ],\n  frequencyPenalty: 0,\n  include: ['message.output_text.logprobs'],\n  input: 'What is the capital of France?',\n  instructions: 'You are a helpful assistant.',\n  maxOutputTokens: 1000,\n  maxToolCalls: 10,\n  mcpServers: 'dedalus-labs/example-server',\n  metadata: ['foo' => 'string'],\n  model: 'openai/gpt-4o',\n  parallelToolCalls: true,\n  presencePenalty: 0,\n  previousResponseID: 'previous_response_id',\n  prompt: [\n    'id' => 'id', 'variables' => ['foo' => 'string'], 'version' => 'version'\n  ],\n  promptCacheKey: 'prompt_cache_key',\n  reasoning: ['foo' => 'string'],\n  safetyIdentifier: 'safety_identifier',\n  serviceTier: 'auto',\n  store: true,\n  stream: true,\n  streamOptions: ['include_usage' => true],\n  temperature: 0,\n  text: ['type' => 'text'],\n  toolChoice: 'auto',\n  tools: [\n    [\n      'function' => [\n        'description' => null, 'name' => null, 'parameters' => null\n      ],\n      'type' => 'function',\n    ],\n  ],\n  topLogprobs: 5,\n  topP: 0.1,\n  truncation: 'auto',\n  user: 'user',\n);\n\nvar_dump($response);",
      },
      csharp: {
        method: 'Responses.Create',
        example:
          'ResponseCreateParams parameters = new();\n\nvar response = await client.Responses.Create(parameters);\n\nConsole.WriteLine(response);',
      },
      http: {
        example:
          'curl https://api.dedaluslabs.ai/v1/responses \\\n    -H \'Content-Type: application/json\' \\\n    -H \'User-Agent: Dedalus-SDK\' \\\n    -H \'X-SDK-Version: 1.0.0\' \\\n    -H "Authorization: Bearer $DEDALUS_API_KEY" \\\n    -d \'{\n          "background": true,\n          "credentials": {\n            "connection_name": "external-service",\n            "values": {\n              "api_key": "sk-..."\n            }\n          },\n          "include": [\n            "message.output_text.logprobs"\n          ],\n          "input": "What is the capital of France?",\n          "instructions": "You are a helpful assistant.",\n          "max_output_tokens": 1000,\n          "max_tool_calls": 10,\n          "mcp_servers": "dedalus-labs/example-server",\n          "model": "openai/gpt-4o",\n          "parallel_tool_calls": true,\n          "service_tier": "auto",\n          "stream": true,\n          "stream_options": {\n            "include_usage": true\n          },\n          "temperature": 0,\n          "text": {\n            "type": "text"\n          },\n          "tool_choice": "auto",\n          "tools": [\n            {\n              "function": {\n                "description": null,\n                "name": null,\n                "parameters": null\n              },\n              "type": "function"\n            }\n          ],\n          "top_logprobs": 5,\n          "top_p": 0.1,\n          "truncation": "auto"\n        }\'',
      },
    },
  },
  {
    name: 'create',
    endpoint: '/v1/chat/completions',
    httpMethod: 'post',
    summary: 'Create Chat Completion',
    description:
      'Create a chat completion.\n\nGenerates a model response for the given conversation and configuration.\nSupports OpenAI-compatible parameters and provider-specific extensions.\n\nHeaders:\n  - Authorization: bearer key for the calling account.\n  - X-Provider / X-Provider-Key: optional headers for using your own provider API key.\n\nBehavior:\n  - If multiple models are supplied, the first one is used, and the agent may hand off to another model.\n  - Tools may be invoked on the server or signaled for the client to run.\n  - Streaming responses emit incremental deltas; non-streaming returns a single object.\n  - Usage metrics are computed when available and returned in the response.\n\nResponses:\n  - 200 OK: JSON completion object with choices, message content, and usage.\n  - 400 Bad Request: validation error.\n  - 401 Unauthorized: authentication failed.\n  - 402 Payment Required or 429 Too Many Requests: quota, balance, or rate limit issue.\n  - 500 Internal Server Error: unexpected failure.\n\nBilling:\n  - Token usage metered by the selected model(s).\n  - Tool calls and MCP sessions may be billed separately.\n  - Streaming is settled after the stream ends via an async task.\n\nExample (non-streaming HTTP):\n  POST /v1/chat/completions\n  Content-Type: application/json\n  Authorization: Bearer <key>\n\n  {\n    "model": "provider/model-name",\n    "messages": [{"role": "user", "content": "Hello"}]\n  }\n\n  200 OK\n  {\n    "id": "cmpl_123",\n    "object": "chat.completion",\n    "choices": [\n      {"index": 0, "message": {"role": "assistant", "content": "Hi there!"}, "finish_reason": "stop"}\n    ],\n    "usage": {"prompt_tokens": 3, "completion_tokens": 4, "total_tokens": 7}\n  }\n\nExample (streaming over SSE):\n  POST /v1/chat/completions\n  Accept: text/event-stream\n\n  data: {"id":"cmpl_123","choices":[{"index":0,"delta":{"content":"Hi"}}]}\n  data: {"id":"cmpl_123","choices":[{"index":0,"delta":{"content":" there!"}}]}\n  data: [DONE]',
    stainlessPath: '(resource) chat.completions > (method) create',
    qualified: 'client.chat.completions.create',
    params: [
      "model: string | { model: string; settings?: { attributes?: object; audio?: json_object_input; deferred?: boolean; extra_args?: object; extra_headers?: object; extra_query?: object; frequency_penalty?: number; generation_config?: json_object_input; include_usage?: boolean; input_audio_format?: string; input_audio_transcription?: json_object_input; logit_bias?: object; logprobs?: boolean; max_completion_tokens?: number; max_tokens?: number; metadata?: object; modalities?: string[]; n?: number; output_audio_format?: string; parallel_tool_calls?: boolean; prediction?: json_object_input; presence_penalty?: number; prompt_cache_key?: string; reasoning?: reasoning; reasoning_effort?: string; response_format?: json_object_input; safety_identifier?: string; safety_settings?: json_object_input[]; search_parameters?: json_object_input; seed?: number; service_tier?: string; stop?: string | string[]; store?: boolean; stream?: boolean; stream_options?: json_object_input; structured_output?: object; system_instruction?: json_object_input; temperature?: number; thinking?: json_object_input; timeout?: number; tool_choice?: tool_choice; tool_config?: json_object_input; top_k?: number; top_logprobs?: number; top_p?: number; truncation?: 'auto' | 'disabled'; turn_detection?: json_object_input; user?: string; verbosity?: string; voice?: string; web_search_options?: json_object_input; }; } | string | { model: string; settings?: object; }[];",
      'agent_attributes?: object;',
      "audio?: { format: 'wav' | 'aac' | 'mp3' | 'flac' | 'opus' | 'pcm16'; voice: string | 'alloy' | 'ash' | 'ballad' | 'coral' | 'echo' | 'sage' | 'shimmer' | 'verse' | 'marin' | 'cedar' | { id: string; }; };",
      'automatic_tool_execution?: boolean;',
      'cached_content?: string;',
      'correlation_id?: string;',
      'credentials?: { connection_name: string; values: object; } | { connection_name: string; values: object; }[];',
      'deferred?: boolean;',
      'deferred_calls?: { id: string; name: string; arguments?: object; blocked_by?: string[]; dependencies?: string[]; venue?: string; }[];',
      'frequency_penalty?: number;',
      'function_call?: string;',
      'functions?: { name: string; description?: string; parameters?: object; }[];',
      'generation_config?: object;',
      'guardrails?: object[];',
      'handoff_config?: object;',
      'handoff_mode?: boolean;',
      'inference_geo?: string;',
      'logit_bias?: object;',
      'logprobs?: boolean;',
      'max_completion_tokens?: number;',
      'max_tokens?: number;',
      'max_turns?: number;',
      'mcp_servers?: string | { name: string; credentials?: object; slug?: string; url?: string; version?: string; } | string | { name: string; credentials?: object; slug?: string; url?: string; version?: string; }[];',
      "messages?: { content: string | { text: string; type: 'text'; }[]; role: 'developer'; name?: string; } | { content: string | { text: string; type: 'text'; }[]; role: 'system'; name?: string; } | { content: string | { text: string; type: 'text'; } | { image_url: object; type: 'image_url'; } | { input_audio: object; type: 'input_audio'; } | { file: object; type: 'file'; }[]; role: 'user'; name?: string; } | { role: 'assistant'; audio?: { id: string; }; content?: string | { text: string; type: 'text'; } | { refusal: string; type: 'refusal'; }[]; function_call?: { arguments: string; name: string; }; name?: string; refusal?: string; tool_calls?: { id: string; function: object; type: 'function'; thought_signature?: string; } | { id: string; custom: object; type: 'custom'; }[]; } | { content: string | { text: string; type: 'text'; }[]; role: 'tool'; tool_call_id: string; } | { content: string; name: string; role: 'function'; }[];",
      'metadata?: object;',
      'modalities?: string[];',
      'model_attributes?: object;',
      'n?: number;',
      'output_config?: object;',
      'parallel_tool_calls?: boolean;',
      "prediction?: { content: string | { text: string; type: 'text'; }[]; type: 'content'; };",
      'presence_penalty?: number;',
      'prompt_cache_key?: string;',
      'prompt_cache_retention?: string;',
      "prompt_mode?: 'reasoning';",
      'reasoning_effort?: string;',
      "response_format?: { type: 'text'; } | { json_schema: { name: string; description?: string; schema?: object; strict?: boolean; }; type: 'json_schema'; } | { type: 'json_object'; };",
      'safe_prompt?: boolean;',
      'safety_identifier?: string;',
      'safety_settings?: { category: string; threshold: string; }[];',
      'search_parameters?: object;',
      'seed?: number;',
      'service_tier?: string;',
      "speed?: 'standard' | 'fast';",
      'stop?: string[] | string;',
      'store?: boolean;',
      'stream?: boolean;',
      'stream_options?: object;',
      'system_instruction?: object | string;',
      'temperature?: number;',
      "thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; } | { type: 'adaptive'; };",
      "tool_choice?: string | { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; };",
      'tool_config?: object;',
      "tools?: { function: { name: string; }; type?: 'function'; }[];",
      'top_k?: number;',
      'top_logprobs?: number;',
      'top_p?: number;',
      'user?: string;',
      'verbosity?: string;',
      'web_search_options?: object;',
    ],
    response:
      "{ id: string; choices: { index: number; message: chat_completion_message; finish_reason?: 'stop' | 'length' | 'tool_calls' | 'content_filter' | 'function_call'; logprobs?: choice_logprobs; }[]; created: number; model: string; object: 'chat.completion'; correlation_id?: string; deferred?: { id: string; name: string; arguments?: json_object_input; blocked_by?: string[]; dependencies?: string[]; venue?: string; }[]; mcp_server_errors?: object; mcp_tool_results?: { arguments: json_object_input; is_error: boolean; server_name: string; tool_name: string; duration_ms?: number; result?: json_value_input; }[]; pending_tools?: { id: string; arguments: object; name: string; dependencies?: string[]; }[]; server_results?: object; service_tier?: 'auto' | 'default' | 'flex' | 'scale' | 'priority'; system_fingerprint?: string; tools_executed?: string[]; turns_consumed?: number; usage?: { completion_tokens: number; prompt_tokens: number; total_tokens: number; completion_tokens_details?: completion_tokens_details; prompt_tokens_details?: prompt_tokens_details; }; }",
    markdown:
      "## create\n\n`client.chat.completions.create(model: string | { model: string; settings?: model_settings; } | string | object[], agent_attributes?: object, audio?: { format: 'wav' | 'aac' | 'mp3' | 'flac' | 'opus' | 'pcm16'; voice: string | 'alloy' | 'ash' | 'ballad' | 'coral' | 'echo' | 'sage' | 'shimmer' | 'verse' | 'marin' | 'cedar' | voice_ids_or_custom_voice; }, automatic_tool_execution?: boolean, cached_content?: string, correlation_id?: string, credentials?: { connection_name: string; values: object; } | object[], deferred?: boolean, deferred_calls?: { id: string; name: string; arguments?: json_object_input; blocked_by?: string[]; dependencies?: string[]; venue?: string; }[], frequency_penalty?: number, function_call?: string, functions?: { name: string; description?: string; parameters?: object; }[], generation_config?: object, guardrails?: object[], handoff_config?: object, handoff_mode?: boolean, inference_geo?: string, logit_bias?: object, logprobs?: boolean, max_completion_tokens?: number, max_tokens?: number, max_turns?: number, mcp_servers?: string | { name: string; credentials?: object; slug?: string; url?: string; version?: string; } | string | object[], messages?: { content: string | chat_completion_content_part_text_param[]; role: 'developer'; name?: string; } | { content: string | chat_completion_content_part_text_param[]; role: 'system'; name?: string; } | { content: string | chat_completion_content_part_text_param | chat_completion_content_part_image_param | chat_completion_content_part_input_audio_param | chat_completion_content_part_file_param[]; role: 'user'; name?: string; } | { role: 'assistant'; audio?: audio; content?: string | chat_completion_content_part_text_param | chat_completion_content_part_refusal_param[]; function_call?: object; name?: string; refusal?: string; tool_calls?: chat_completion_message_tool_call | chat_completion_message_custom_tool_call[]; } | { content: string | chat_completion_content_part_text_param[]; role: 'tool'; tool_call_id: string; } | { content: string; name: string; role: 'function'; }[], metadata?: object, modalities?: string[], model_attributes?: object, n?: number, output_config?: object, parallel_tool_calls?: boolean, prediction?: { content: string | chat_completion_content_part_text_param[]; type: 'content'; }, presence_penalty?: number, prompt_cache_key?: string, prompt_cache_retention?: string, prompt_mode?: 'reasoning', reasoning_effort?: string, response_format?: { type: 'text'; } | { json_schema: object; type: 'json_schema'; } | { type: 'json_object'; }, safe_prompt?: boolean, safety_identifier?: string, safety_settings?: { category: string; threshold: string; }[], search_parameters?: object, seed?: number, service_tier?: string, speed?: 'standard' | 'fast', stop?: string[] | string, store?: boolean, stream?: boolean, stream_options?: object, system_instruction?: object | string, temperature?: number, thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; } | { type: 'adaptive'; }, tool_choice?: string | { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }, tool_config?: object, tools?: { function: function_definition; type?: 'function'; }[], top_k?: number, top_logprobs?: number, top_p?: number, user?: string, verbosity?: string, web_search_options?: object): { id: string; choices: choice[]; created: number; model: string; object: 'chat.completion'; correlation_id?: string; deferred?: deferred_call_response[]; mcp_server_errors?: object; mcp_tool_results?: mcp_tool_result[]; pending_tools?: object[]; server_results?: object; service_tier?: 'auto' | 'default' | 'flex' | 'scale' | 'priority'; system_fingerprint?: string; tools_executed?: string[]; turns_consumed?: number; usage?: completion_usage; }`\n\n**post** `/v1/chat/completions`\n\nCreate a chat completion.\n\nGenerates a model response for the given conversation and configuration.\nSupports OpenAI-compatible parameters and provider-specific extensions.\n\nHeaders:\n  - Authorization: bearer key for the calling account.\n  - X-Provider / X-Provider-Key: optional headers for using your own provider API key.\n\nBehavior:\n  - If multiple models are supplied, the first one is used, and the agent may hand off to another model.\n  - Tools may be invoked on the server or signaled for the client to run.\n  - Streaming responses emit incremental deltas; non-streaming returns a single object.\n  - Usage metrics are computed when available and returned in the response.\n\nResponses:\n  - 200 OK: JSON completion object with choices, message content, and usage.\n  - 400 Bad Request: validation error.\n  - 401 Unauthorized: authentication failed.\n  - 402 Payment Required or 429 Too Many Requests: quota, balance, or rate limit issue.\n  - 500 Internal Server Error: unexpected failure.\n\nBilling:\n  - Token usage metered by the selected model(s).\n  - Tool calls and MCP sessions may be billed separately.\n  - Streaming is settled after the stream ends via an async task.\n\nExample (non-streaming HTTP):\n  POST /v1/chat/completions\n  Content-Type: application/json\n  Authorization: Bearer <key>\n\n  {\n    \"model\": \"provider/model-name\",\n    \"messages\": [{\"role\": \"user\", \"content\": \"Hello\"}]\n  }\n\n  200 OK\n  {\n    \"id\": \"cmpl_123\",\n    \"object\": \"chat.completion\",\n    \"choices\": [\n      {\"index\": 0, \"message\": {\"role\": \"assistant\", \"content\": \"Hi there!\"}, \"finish_reason\": \"stop\"}\n    ],\n    \"usage\": {\"prompt_tokens\": 3, \"completion_tokens\": 4, \"total_tokens\": 7}\n  }\n\nExample (streaming over SSE):\n  POST /v1/chat/completions\n  Accept: text/event-stream\n\n  data: {\"id\":\"cmpl_123\",\"choices\":[{\"index\":0,\"delta\":{\"content\":\"Hi\"}}]}\n  data: {\"id\":\"cmpl_123\",\"choices\":[{\"index\":0,\"delta\":{\"content\":\" there!\"}}]}\n  data: [DONE]\n\n### Parameters\n\n- `model: string | { model: string; settings?: { attributes?: object; audio?: json_object_input; deferred?: boolean; extra_args?: object; extra_headers?: object; extra_query?: object; frequency_penalty?: number; generation_config?: json_object_input; include_usage?: boolean; input_audio_format?: string; input_audio_transcription?: json_object_input; logit_bias?: object; logprobs?: boolean; max_completion_tokens?: number; max_tokens?: number; metadata?: object; modalities?: string[]; n?: number; output_audio_format?: string; parallel_tool_calls?: boolean; prediction?: json_object_input; presence_penalty?: number; prompt_cache_key?: string; reasoning?: reasoning; reasoning_effort?: string; response_format?: json_object_input; safety_identifier?: string; safety_settings?: json_object_input[]; search_parameters?: json_object_input; seed?: number; service_tier?: string; stop?: string | string[]; store?: boolean; stream?: boolean; stream_options?: json_object_input; structured_output?: object; system_instruction?: json_object_input; temperature?: number; thinking?: json_object_input; timeout?: number; tool_choice?: tool_choice; tool_config?: json_object_input; top_k?: number; top_logprobs?: number; top_p?: number; truncation?: 'auto' | 'disabled'; turn_detection?: json_object_input; user?: string; verbosity?: string; voice?: string; web_search_options?: json_object_input; }; } | string | { model: string; settings?: object; }[]`\n  Model identifier. Accepts model ID strings, lists for routing, or DedalusModel objects with per-model settings.\n\n- `agent_attributes?: object`\n  Agent attributes. Values in [0.0, 1.0].\n\n- `audio?: { format: 'wav' | 'aac' | 'mp3' | 'flac' | 'opus' | 'pcm16'; voice: string | 'alloy' | 'ash' | 'ballad' | 'coral' | 'echo' | 'sage' | 'shimmer' | 'verse' | 'marin' | 'cedar' | { id: string; }; }`\n  Parameters for audio output. Required when audio output is requested with\n`modalities: [\"audio\"]`. [Learn more](/docs/guides/audio).\n\nFields:\n- voice (required): VoiceIdsOrCustomVoice\n- format (required): Literal[\"wav\", \"aac\", \"mp3\", \"flac\", \"opus\", \"pcm16\"]\n  - `format: 'wav' | 'aac' | 'mp3' | 'flac' | 'opus' | 'pcm16'`\n    Specifies the output audio format. Must be one of `wav`, `mp3`, `flac`,\n`opus`, or `pcm16`.\n  - `voice: string | 'alloy' | 'ash' | 'ballad' | 'coral' | 'echo' | 'sage' | 'shimmer' | 'verse' | 'marin' | 'cedar' | { id: string; }`\n    The voice the model uses to respond. Supported built-in voices are\n`alloy`, `ash`, `ballad`, `coral`, `echo`, `fable`, `nova`, `onyx`,\n`sage`, `shimmer`, `marin`, and `cedar`. You may also provide a\ncustom voice object with an `id`, for example `{ \"id\": \"voice_1234\" }`.\n\n- `automatic_tool_execution?: boolean`\n  Execute tools server-side. If false, returns raw tool calls for manual handling.\n\n- `cached_content?: string`\n  Optional. The name of the content [cached](https://ai.google.dev/gemini-api/docs/caching) to use as context to serve the prediction. Format: `cachedContents/{cachedContent}`\n\n- `correlation_id?: string`\n  Stable session ID for resuming a previous handoff. Returned by the server on handoff; echo it on the next request to resume.\n\n- `credentials?: { connection_name: string; values: object; } | { connection_name: string; values: object; }[]`\n  Credentials for MCP server authentication. Each credential is matched to servers by connection name.\n\n- `deferred?: boolean`\n  If set to `true`, the request returns a `request_id`. You can then get the deferred response by GET `/v1/chat/deferred-completion/{request_id}`.\n\n- `deferred_calls?: { id: string; name: string; arguments?: object; blocked_by?: string[]; dependencies?: string[]; venue?: string; }[]`\n  Tier 2 stateless resumption. Deferred tool specs from a previous handoff response, sent back verbatim so the server can resume without Redis.\n\n- `frequency_penalty?: number`\n  Number between -2.0 and 2.0. Positive values penalize new tokens based on their existing frequency in the text so far, decreasing the model's likelihood to repeat the same line verbatim. \n\n- `function_call?: string`\n  Deprecated in favor of `tool_choice`.  Controls which (if any) function is called by the model.  `none` means the model will not call a function and instead generates a message.  `auto` means the model can pick between generating a message or calling a function.  Specifying a particular function via `{\"name\": \"my_function\"}` forces the model to call that function.  `none` is the default when no functions are present. `auto` is the default if functions are present. \n\n- `functions?: { name: string; description?: string; parameters?: object; }[]`\n  Deprecated in favor of `tools`.  A list of functions the model may generate JSON inputs for. \n\n- `generation_config?: object`\n  Generation parameters wrapper (Google-specific)\n\n- `guardrails?: object[]`\n  Content filtering and safety policy configuration.\n\n- `handoff_config?: object`\n  Configuration for multi-model handoffs.\n\n- `handoff_mode?: boolean`\n  Handoff control. None or omitted: auto-detect. true: structured handoff (SDK). false: drop-in (LLM re-run for mixed turns).\n\n- `inference_geo?: string`\n  Specifies the geographic region for inference processing. If not specified, the workspace's `default_inference_geo` is used.\n\n- `logit_bias?: object`\n  Modify the likelihood of specified tokens appearing in the completion.  Accepts a JSON object that maps tokens (specified by their token ID in the tokenizer) to an associated bias value from -100 to 100. Mathematically, the bias is added to the logits generated by the model prior to sampling. The exact effect will vary per model, but values between -1 and 1 should decrease or increase likelihood of selection; values like -100 or 100 should result in a ban or exclusive selection of the relevant token. \n\n- `logprobs?: boolean`\n  Whether to return log probabilities of the output tokens or not. If true, returns the log probabilities of each output token returned in the `content` of `message`. \n\n- `max_completion_tokens?: number`\n  Maximum tokens in completion (newer parameter name)\n\n- `max_tokens?: number`\n  Maximum tokens in completion\n\n- `max_turns?: number`\n  Maximum conversation turns.\n\n- `mcp_servers?: string | { name: string; credentials?: object; slug?: string; url?: string; version?: string; } | string | { name: string; credentials?: object; slug?: string; url?: string; version?: string; }[]`\n  MCP server identifiers. Accepts marketplace slugs, URLs, or MCPServerSpec objects. MCP tools are executed server-side and billed separately.\n\n- `messages?: { content: string | { text: string; type: 'text'; }[]; role: 'developer'; name?: string; } | { content: string | { text: string; type: 'text'; }[]; role: 'system'; name?: string; } | { content: string | { text: string; type: 'text'; } | { image_url: object; type: 'image_url'; } | { input_audio: object; type: 'input_audio'; } | { file: object; type: 'file'; }[]; role: 'user'; name?: string; } | { role: 'assistant'; audio?: { id: string; }; content?: string | { text: string; type: 'text'; } | { refusal: string; type: 'refusal'; }[]; function_call?: { arguments: string; name: string; }; name?: string; refusal?: string; tool_calls?: { id: string; function: object; type: 'function'; thought_signature?: string; } | { id: string; custom: object; type: 'custom'; }[]; } | { content: string | { text: string; type: 'text'; }[]; role: 'tool'; tool_call_id: string; } | { content: string; name: string; role: 'function'; }[]`\n  Conversation history (OpenAI: messages, Google: contents, Responses: input)\n\n- `metadata?: object`\n  Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format, and querying for objects via API or the dashboard.  Keys are strings with a maximum length of 64 characters. Values are strings with a maximum length of 512 characters. \n\n- `modalities?: string[]`\n  Output types that you would like the model to generate. Most models are capable of generating text, which is the default:  `[\"text\"]`  The `gpt-4o-audio-preview` model can also be used to [generate audio](/docs/guides/audio). To request that this model generate both text and audio responses, you can use:  `[\"text\", \"audio\"]` \n\n- `model_attributes?: object`\n  Model attributes for routing. Maps model IDs to attribute dictionaries with values in [0.0, 1.0].\n\n- `n?: number`\n  How many chat completion choices to generate for each input message. Note that you will be charged based on the number of generated tokens across all of the choices. Keep `n` as `1` to minimize costs.\n\n- `output_config?: object`\n\n- `parallel_tool_calls?: boolean`\n  Whether to enable parallel tool calls (Anthropic uses inverted polarity).\n\n- `prediction?: { content: string | { text: string; type: 'text'; }[]; type: 'content'; }`\n  Static predicted output content, such as the content of a text file that is\nbeing regenerated.\n\nFields:\n- type (required): Literal[\"content\"]\n- content (required): str | Annotated[list[ChatCompletionRequestMessageContentPartText], MinLen(1), ArrayTitle(\"PredictionContentArray\")]\n  - `content: string | { text: string; type: 'text'; }[]`\n    The content that should be matched when generating a model response.\nIf generated tokens would match this content, the entire model response\ncan be returned much more quickly.\n  - `type: 'content'`\n    The type of the predicted content you want to provide. This type is\ncurrently always `content`.\n\n- `presence_penalty?: number`\n  Number between -2.0 and 2.0. Positive values penalize new tokens based on whether they appear in the text so far, increasing the model's likelihood to talk about new topics. \n\n- `prompt_cache_key?: string`\n  Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](/docs/guides/prompt-caching). \n\n- `prompt_cache_retention?: string`\n  The retention policy for the prompt cache. Set to `24h` to enable extended prompt caching, which keeps cached prefixes active for longer, up to a maximum of 24 hours. [Learn more](/docs/guides/prompt-caching#prompt-cache-retention). \n\n- `prompt_mode?: 'reasoning'`\n  Allows toggling between the reasoning mode and no system prompt. When set to `reasoning` the system prompt for reasoning models will be used.\n\n- `reasoning_effort?: string`\n  Constrains effort on reasoning for [reasoning models](https://platform.openai.com/docs/guides/reasoning). Currently supported values are `none`, `minimal`, `low`, `medium`, `high`, and `xhigh`. Reducing reasoning effort can result in faster responses and fewer tokens used on reasoning in a response.  - `gpt-5.1` defaults to `none`, which does not perform reasoning. The supported reasoning values for `gpt-5.1` are `none`, `low`, `medium`, and `high`. Tool calls are supported for all reasoning values in gpt-5.1. - All models before `gpt-5.1` default to `medium` reasoning effort, and do not support `none`. - The `gpt-5-pro` model defaults to (and only supports) `high` reasoning effort. - `xhigh` is supported for all models after `gpt-5.1-codex-max`. \n\n- `response_format?: { type: 'text'; } | { json_schema: { name: string; description?: string; schema?: object; strict?: boolean; }; type: 'json_schema'; } | { type: 'json_object'; }`\n  An object specifying the format that the model must output.  Setting to `{ \"type\": \"json_schema\", \"json_schema\": {...} }` enables Structured Outputs which ensures the model will match your supplied JSON schema. Learn more in the [Structured Outputs guide](/docs/guides/structured-outputs).  Setting to `{ \"type\": \"json_object\" }` enables the older JSON mode, which ensures the message the model generates is valid JSON. Using `json_schema` is preferred for models that support it. \n\n- `safe_prompt?: boolean`\n  Whether to inject a safety prompt before all conversations.\n\n- `safety_identifier?: string`\n  A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies. The IDs should be a string that uniquely identifies each user. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](/docs/guides/safety-best-practices#safety-identifiers). \n\n- `safety_settings?: { category: string; threshold: string; }[]`\n  Safety/content filtering settings (Google-specific)\n\n- `search_parameters?: object`\n  Set the parameters to be used for searched data. If not set, no data will be acquired by the model.\n\n- `seed?: number`\n  Random seed for deterministic output\n\n- `service_tier?: string`\n  Service tier for request processing\n\n- `speed?: 'standard' | 'fast'`\n  The inference speed mode for this request. `\"fast\"` enables high output-tokens-per-second inference.\n\n- `stop?: string[] | string`\n  Sequences that stop generation\n\n- `store?: boolean`\n  Whether or not to store the output of this chat completion request for use in our [model distillation](/docs/guides/distillation) or [evals](/docs/guides/evals) products.  Supports text and image inputs. Note: image inputs over 8MB will be dropped. \n\n- `stream?: boolean`\n  Enable streaming response\n\n- `stream_options?: object`\n  Options for streaming response. Only set this when you set `stream: true`. \n\n- `system_instruction?: object | string`\n  System instruction/prompt\n\n- `temperature?: number`\n  Sampling temperature (0-2 for most providers)\n\n- `thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; } | { type: 'adaptive'; }`\n  Extended thinking configuration (Anthropic-specific)\n\n- `tool_choice?: string | { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }`\n  Controls which (if any) tool is called by the model. `none` means the model will not call any tool and instead generates a message. `auto` means the model can pick between generating a message or calling one or more tools. `required` means the model must call one or more tools. Specifying a particular tool via `{\"type\": \"function\", \"function\": {\"name\": \"my_function\"}}` forces the model to call that tool.  `none` is the default when no tools are present. `auto` is the default if tools are present. \n\n- `tool_config?: object`\n  Tool calling configuration (Google-specific)\n\n- `tools?: { function: { name: string; }; type?: 'function'; }[]`\n  Available tools/functions for the model\n\n- `top_k?: number`\n  Top-k sampling parameter\n\n- `top_logprobs?: number`\n  An integer between 0 and 20 specifying the number of most likely tokens to return at each token position, each with an associated log probability. `logprobs` must be set to `true` if this parameter is used. \n\n- `top_p?: number`\n  Nucleus sampling threshold\n\n- `user?: string`\n  This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to maintain caching optimizations. A stable identifier for your end-users. Used to boost cache hit rates by better bucketing similar requests and  to help OpenAI detect and prevent abuse. [Learn more](/docs/guides/safety-best-practices#safety-identifiers). \n\n- `verbosity?: string`\n  Constrains the verbosity of the model's response. Lower values will result in more concise responses, while higher values will result in more verbose responses. Currently supported values are `low`, `medium`, and `high`. \n\n- `web_search_options?: object`\n  This tool searches the web for relevant results to use in a response. Learn more about the [web search tool](/docs/guides/tools-web-search?api-mode=chat). \n\n### Returns\n\n- `{ id: string; choices: { index: number; message: chat_completion_message; finish_reason?: 'stop' | 'length' | 'tool_calls' | 'content_filter' | 'function_call'; logprobs?: choice_logprobs; }[]; created: number; model: string; object: 'chat.completion'; correlation_id?: string; deferred?: { id: string; name: string; arguments?: json_object_input; blocked_by?: string[]; dependencies?: string[]; venue?: string; }[]; mcp_server_errors?: object; mcp_tool_results?: { arguments: json_object_input; is_error: boolean; server_name: string; tool_name: string; duration_ms?: number; result?: json_value_input; }[]; pending_tools?: { id: string; arguments: object; name: string; dependencies?: string[]; }[]; server_results?: object; service_tier?: 'auto' | 'default' | 'flex' | 'scale' | 'priority'; system_fingerprint?: string; tools_executed?: string[]; turns_consumed?: number; usage?: { completion_tokens: number; prompt_tokens: number; total_tokens: number; completion_tokens_details?: completion_tokens_details; prompt_tokens_details?: prompt_tokens_details; }; }`\n  Chat completion response for Dedalus API.\n\nOpenAI-compatible chat completion response with Dedalus extensions.\nMaintains full compatibility with OpenAI API while providing additional\nfeatures like server-side tool execution tracking and MCP error reporting.\n\n  - `id: string`\n  - `choices: { index: number; message: { content: string; refusal: string; role: 'assistant'; annotations?: object[]; audio?: object; function_call?: object; tool_calls?: chat_completion_message_tool_call | chat_completion_message_custom_tool_call[]; }; finish_reason?: 'stop' | 'length' | 'tool_calls' | 'content_filter' | 'function_call'; logprobs?: { content?: chat_completion_token_logprob[]; refusal?: chat_completion_token_logprob[]; }; }[]`\n  - `created: number`\n  - `model: string`\n  - `object: 'chat.completion'`\n  - `correlation_id?: string`\n  - `deferred?: { id: string; name: string; arguments?: object; blocked_by?: string[]; dependencies?: string[]; venue?: string; }[]`\n  - `mcp_server_errors?: object`\n  - `mcp_tool_results?: { arguments: object; is_error: boolean; server_name: string; tool_name: string; duration_ms?: number; result?: string | number | boolean | object | string | number | boolean | object | json_value_input[][]; }[]`\n  - `pending_tools?: { id: string; arguments: object; name: string; dependencies?: string[]; }[]`\n  - `server_results?: object`\n  - `service_tier?: 'auto' | 'default' | 'flex' | 'scale' | 'priority'`\n  - `system_fingerprint?: string`\n  - `tools_executed?: string[]`\n  - `turns_consumed?: number`\n  - `usage?: { completion_tokens: number; prompt_tokens: number; total_tokens: number; completion_tokens_details?: { accepted_prediction_tokens?: number; audio_tokens?: number; reasoning_tokens?: number; rejected_prediction_tokens?: number; }; prompt_tokens_details?: { audio_tokens?: number; cached_tokens?: number; }; }`\n\n### Example\n\n```typescript\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst stream = await client.chat.completions.create({ model: 'openai/gpt-5' });\nfor await (const chatCompletionChunk of stream) {\n  console.log(chatCompletionChunk);\n}\n```",
    perLanguage: {
      typescript: {
        method: 'client.chat.completions.create',
        example:
          "import Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n});\n\nconst chatCompletion = await client.chat.completions.create({ model: 'openai/gpt-5' });\n\nconsole.log(chatCompletion.id);",
      },
      python: {
        method: 'chat.completions.create',
        example:
          'import os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n)\nfor completion in client.chat.completions.create(\n    model="openai/gpt-5",\n):\n  print(completion)',
      },
      java: {
        method: 'chat().completions().create',
        example:
          'package com.dedalus_sdk.api.example;\n\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        DedalusClient client = DedalusOkHttpClient.fromEnv();\n\n        ChatCompletionCreateParams params = ChatCompletionCreateParams.builder()\n            .model("openai/gpt-5")\n            .build();\n        ChatCompletion chatCompletion = client.chat().completions().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'chat().completions().create',
        example:
          'package com.dedalus_sdk.api.example\n\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams\n\nfun main() {\n    val client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\n    val params: ChatCompletionCreateParams = ChatCompletionCreateParams.builder()\n        .model("openai/gpt-5")\n        .build()\n    val chatCompletion: ChatCompletion = client.chat().completions().create(params)\n}',
      },
      go: {
        method: 'client.Chat.Completions.New',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n\t"github.com/dedalus-labs/dedalus-sdk-go/shared"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tchatCompletion, err := client.Chat.Completions.New(context.TODO(), githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParams{\n\t\tModel: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsModelUnion](shared.UnionString("openai/gpt-5")),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", chatCompletion.ID)\n}\n',
      },
      ruby: {
        method: 'chat.completions.create',
        example:
          'require "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: "My API Key",\n  environment: "development" # defaults to "production"\n)\n\nchat_completion = dedalus.chat.completions.create(model: "openai/gpt-5")\n\nputs(chat_completion)',
      },
      cli: {
        method: 'completions create',
        example:
          "dedalus-sdk chat:completions create \\\n  --api-key 'My API Key' \\\n  --model openai/gpt-5",
      },
      php: {
        method: 'chat->completions->create',
        example:
          "<?php\n\nrequire_once dirname(__DIR__) . '/vendor/autoload.php';\n\n$client = new Client(apiKey: 'My API Key', environment: 'development');\n\n$chatCompletion = $client->chat->completions->create(\n  model: 'openai/gpt-5',\n  agentAttributes: ['accuracy' => 0.9, 'complexity' => 0.8],\n  audio: ['format' => 'mp3', 'voice' => 'alloy'],\n  automaticToolExecution: true,\n  cachedContent: 'cached_content',\n  correlationID: 'correlation_id',\n  credentials: [\n    'connectionName' => 'external-service', 'values' => ['api_key' => 'sk-...']\n  ],\n  deferred: true,\n  deferredCalls: [\n    [\n      'id' => 'id',\n      'name' => 'name',\n      'arguments' => ['foo' => 'string'],\n      'blockedBy' => ['string'],\n      'dependencies' => ['string'],\n      'venue' => 'venue',\n    ],\n  ],\n  frequencyPenalty: -2,\n  functionCall: 'function_call',\n  functions: [\n    [\n      'name' => 'name',\n      'description' => 'description',\n      'parameters' => ['foo' => 'bar'],\n    ],\n  ],\n  generationConfig: ['foo' => 'string'],\n  guardrails: [['foo' => 'bar']],\n  handoffConfig: ['foo' => 'bar'],\n  handoffMode: true,\n  inferenceGeo: 'inference_geo',\n  logitBias: ['foo' => 0],\n  logprobs: true,\n  maxCompletionTokens: 0,\n  maxTokens: 1,\n  maxTurns: 5,\n  mcpServers: 'dedalus-labs/example-server',\n  messages: [['content' => 'string', 'role' => 'developer', 'name' => 'name']],\n  metadata: ['foo' => 'string'],\n  modalities: ['string'],\n  modelAttributes: ['gpt-5' => ['accuracy' => 0.95, 'speed' => 0.6]],\n  n: 1,\n  outputConfig: ['foo' => 'string'],\n  parallelToolCalls: true,\n  prediction: ['content' => 'string', 'type' => 'content'],\n  presencePenalty: -2,\n  promptCacheKey: 'prompt_cache_key',\n  promptCacheRetention: 'prompt_cache_retention',\n  promptMode: 'reasoning',\n  reasoningEffort: 'reasoning_effort',\n  responseFormat: ['type' => 'text'],\n  safePrompt: true,\n  safetyIdentifier: 'safety_identifier',\n  safetySettings: [\n    [\n      'category' => 'HARM_CATEGORY_UNSPECIFIED',\n      'threshold' => 'HARM_BLOCK_THRESHOLD_UNSPECIFIED',\n    ],\n  ],\n  searchParameters: ['foo' => 'string'],\n  seed: 0,\n  serviceTier: 'service_tier',\n  speed: 'standard',\n  stop: ['string'],\n  store: true,\n  streamOptions: ['foo' => 'string'],\n  systemInstruction: ['foo' => 'string'],\n  temperature: 0,\n  thinking: ['budgetTokens' => 1024, 'type' => 'enabled'],\n  toolChoice: 'string',\n  toolConfig: ['foo' => 'string'],\n  tools: [['function' => ['name' => 'name'], 'type' => 'function']],\n  topK: 0,\n  topLogprobs: 0,\n  topP: 0,\n  user: 'user',\n  verbosity: 'verbosity',\n  webSearchOptions: ['foo' => 'string'],\n);\n\nvar_dump($chatCompletion);",
      },
      csharp: {
        method: 'Chat.Completions.Create',
        example:
          'CompletionCreateParams parameters = new() { Model = "openai/gpt-5" };\n\nvar chatCompletion = await client.Chat.Completions.Create(parameters);\n\nConsole.WriteLine(chatCompletion);',
      },
      http: {
        example:
          'curl https://api.dedaluslabs.ai/v1/chat/completions \\\n    -H \'Content-Type: application/json\' \\\n    -H \'User-Agent: Dedalus-SDK\' \\\n    -H \'X-SDK-Version: 1.0.0\' \\\n    -H "Authorization: Bearer $DEDALUS_API_KEY" \\\n    -d \'{\n          "model": "openai/gpt-5",\n          "agent_attributes": {\n            "accuracy": 0.9,\n            "complexity": 0.8\n          },\n          "audio": {\n            "format": "mp3",\n            "voice": "alloy"\n          },\n          "credentials": {\n            "connection_name": "external-service",\n            "values": {\n              "api_key": "sk-..."\n            }\n          },\n          "max_turns": 5,\n          "mcp_servers": "dedalus-labs/example-server",\n          "model_attributes": {\n            "gpt-5": {\n              "accuracy": 0.95,\n              "speed": 0.6\n            }\n          }\n        }\'',
      },
    },
  },
];

const EMBEDDED_READMES: { language: string; content: string }[] = [
  {
    language: 'cli',
    content:
      "# Dedalus CLI\n\nThe official CLI for the [Dedalus REST API](https://docs.dedaluslabs.ai).\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## Installation\n\n### Installing with Go\n\nTo test or install the CLI locally, you need [Go](https://go.dev/doc/install) version 1.22 or later installed.\n\n~~~sh\ngo install 'github.com/stainless-sdks/dedalus-sdk-cli/cmd/dedalus-sdk@latest'\n~~~\n\nOnce you have run `go install`, the binary is placed in your Go bin directory:\n\n- **Default location**: `$HOME/go/bin` (or `$GOPATH/bin` if GOPATH is set)\n- **Check your path**: Run `go env GOPATH` to see the base directory\n\nIf commands aren't found after installation, add the Go bin directory to your PATH:\n\n~~~sh\n# Add to your shell profile (.zshrc, .bashrc, etc.)\nexport PATH=\"$PATH:$(go env GOPATH)/bin\"\n~~~\n\n### Running Locally\n\nAfter cloning the git repository for this project, you can use the\n`scripts/run` script to run the tool locally:\n\n~~~sh\n./scripts/run args...\n~~~\n\n## Usage\n\nThe CLI follows a resource-based command structure:\n\n~~~sh\ndedalus-sdk [resource] <command> [flags...]\n~~~\n\n~~~sh\ndedalus-sdk chat:completions create \\\n  --api-key 'My API Key' \\\n  --model openai/gpt-5-nano \\\n  --message '{content: You are Stephen Dedalus. Respond in morose Joycean malaise., role: system}' \\\n  --message \"{content: 'Hello, how are you today?', role: user}\"\n~~~\n\nFor details about specific commands, use the `--help` flag.\n\n### Environment variables\n\n| Environment variable     | Description                                                          | Required | Default value                 |\n| ------------------------ | -------------------------------------------------------------------- | -------- | ----------------------------- |\n| `DEDALUS_API_KEY`        | API key for Bearer token authentication.                             | no       | `null`                        |\n| `DEDALUS_X_API_KEY`      | API key for X-API-Key header authentication.                         | no       | `null`                        |\n| `DEDALUS_AS_URL`         | MCP Authorization Server URL                                         | no       | `\"https://as.dedaluslabs.ai\"` |\n| `DEDALUS_ORG_ID`         | Organization ID for request scoping.                                 | no       | `null`                        |\n| `DEDALUS_PROVIDER`       | Provider name for BYOK mode (e.g., 'google', 'openai', 'anthropic'). | no       | `null`                        |\n| `DEDALUS_PROVIDER_KEY`   | Provider API key for BYOK mode.                                      | no       | `null`                        |\n| `DEDALUS_PROVIDER_MODEL` | Model identifier for BYOK provider.                                  | no       | `null`                        |\n\n### Global flags\n\n- `--api-key` - API key for Bearer token authentication. (can also be set with `DEDALUS_API_KEY` env var)\n- `--x-api-key` - API key for X-API-Key header authentication. (can also be set with `DEDALUS_X_API_KEY` env var)\n- `--as-base-url` - MCP Authorization Server URL (can also be set with `DEDALUS_AS_URL` env var)\n- `--dedalus-org-id` - Organization ID for request scoping. (can also be set with `DEDALUS_ORG_ID` env var)\n- `--provider` - Provider name for BYOK mode (e.g., 'google', 'openai', 'anthropic'). (can also be set with `DEDALUS_PROVIDER` env var)\n- `--provider-key` - Provider API key for BYOK mode. (can also be set with `DEDALUS_PROVIDER_KEY` env var)\n- `--provider-model` - Model identifier for BYOK provider. (can also be set with `DEDALUS_PROVIDER_MODEL` env var)\n- `--help` - Show command line usage\n- `--debug` - Enable debug logging (includes HTTP request/response details)\n- `--version`, `-v` - Show the CLI version\n- `--base-url` - Use a custom API backend URL\n- `--format` - Change the output format (`auto`, `explore`, `json`, `jsonl`, `pretty`, `raw`, `yaml`)\n- `--format-error` - Change the output format for errors (`auto`, `explore`, `json`, `jsonl`, `pretty`, `raw`, `yaml`)\n- `--transform` - Transform the data output using [GJSON syntax](https://github.com/tidwall/gjson/blob/master/SYNTAX.md)\n- `--transform-error` - Transform the error output using [GJSON syntax](https://github.com/tidwall/gjson/blob/master/SYNTAX.md)\n\n### Passing files as arguments\n\nTo pass files to your API, you can use the `@myfile.ext` syntax:\n\n~~~bash\ndedalus-sdk <command> --arg @abe.jpg\n~~~\n\nFiles can also be passed inside JSON or YAML blobs:\n\n~~~bash\ndedalus-sdk <command> --arg '{image: \"@abe.jpg\"}'\n# Equivalent:\ndedalus-sdk <command> <<YAML\narg:\n  image: \"@abe.jpg\"\nYAML\n~~~\n\nIf you need to pass a string literal that begins with an `@` sign, you can\nescape the `@` sign to avoid accidentally passing a file.\n\n~~~bash\ndedalus-sdk <command> --username '\\@abe'\n~~~\n\n#### Explicit encoding\n\nFor JSON endpoints, the CLI tool does filetype sniffing to determine whether the\nfile contents should be sent as a string literal (for plain text files) or as a\nbase64-encoded string literal (for binary files). If you need to explicitly send\nthe file as either plain text or base64-encoded data, you can use\n`@file://myfile.txt` (for string encoding) or `@data://myfile.dat` (for\nbase64-encoding). Note that absolute paths will begin with `@file://` or\n`@data://`, followed by a third `/` (for example, `@file:///tmp/file.txt`).\n\n~~~bash\ndedalus-sdk <command> --arg @data://file.txt\n~~~\n\n## Linking different Go SDK versions\n\nYou can link the CLI against a different version of the Dedalus Go SDK\nfor development purposes using the `./scripts/link` script.\n\nTo link to a specific version from a repository (version can be a branch,\ngit tag, or commit hash):\n\n~~~bash\n./scripts/link github.com/org/repo@version\n~~~\n\nTo link to a local copy of the SDK:\n\n~~~bash\n./scripts/link ../path/to/githubcomdedaluslabsdedalussdkgo-go\n~~~\n\nIf you run the link script without any arguments, it will default to `../githubcomdedaluslabsdedalussdkgo-go`.\n",
  },
  {
    language: 'csharp',
    content:
      '# Dedalus C# API Library\n\nThe Dedalus C# SDK provides convenient access to the [Dedalus REST API](https://docs.dedaluslabs.ai) from applications written in   C#.\n\n## Installation\n\n```bash\ngit clone git@github.com:stainless-sdks/dedalus-sdk-csharp.git\ndotnet add reference dedalus-sdk-csharp/src/DedalusSdk\n```\n\n## Requirements\n\nThis library requires .NET Standard 2.0 or later.\n\n## Usage\n\nSee the [`examples`](examples) directory for complete and runnable examples.\n\n```csharp\nDedalusClient client = new();\n\nCompletionCreateParams parameters = new()\n{\n    Model = "openai/gpt-5-nano",\n    Messages =\n    [\n        new ChatCompletionSystemMessageParam(\n            new ChatCompletionSystemMessageParamContent(\n                "You are Stephen Dedalus. Respond in morose Joycean malaise."\n            )\n        ),\n        new ChatCompletionUserMessageParam(\n            new ChatCompletionUserMessageParamContent(\n                "Hello, how are you today?"\n            )\n        ),\n    ],\n};\n\nvar chatCompletion = await client.Chat.Completions.Create(parameters);\n\nConsole.WriteLine(chatCompletion);\n```',
  },
  {
    language: 'go',
    content:
      '# Dedalus Go API Library\n\n<a href="https://pkg.go.dev/github.com/dedalus-labs/dedalus-sdk-go"><img src="https://pkg.go.dev/badge/github.com/dedalus-labs/dedalus-sdk-go.svg" alt="Go Reference"></a>\n\nThe Dedalus Go library provides convenient access to the [Dedalus REST API](https://docs.dedaluslabs.ai)\nfrom applications written in Go.\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Dedalus MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=dedalus-labs-mcp&config=eyJuYW1lIjoiZGVkYWx1cy1sYWJzLW1jcCIsInRyYW5zcG9ydCI6Imh0dHAiLCJ1cmwiOiJodHRwczovL2RlZGFsdXMtc2RrLnN0bG1jcC5jb20iLCJoZWFkZXJzIjp7IngtZGVkYWx1cy1hcGkta2V5IjoiTXkgQVBJIEtleSIsIngtYXBpLWtleSI6Ik15IFggQVBJIEtleSJ9fQ)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dedalus-labs-mcp%22%2C%22type%22%3A%22http%22%2C%22url%22%3A%22https%3A%2F%2Fdedalus-sdk.stlmcp.com%22%2C%22headers%22%3A%7B%22x-dedalus-api-key%22%3A%22My%20API%20Key%22%2C%22x-api-key%22%3A%22My%20X%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n## Installation\n\n<!-- x-release-please-start-version -->\n\n```go\nimport (\n\t"github.com/dedalus-labs/dedalus-sdk-go" // imported as SDK_PackageName\n)\n```\n\n<!-- x-release-please-end -->\n\nOr to pin the version:\n\n<!-- x-release-please-start-version -->\n\n```sh\ngo get -u \'github.com/dedalus-labs/dedalus-sdk-go@v0.0.1\'\n```\n\n<!-- x-release-please-end -->\n\n## Requirements\n\nThis library requires Go 1.22+.\n\n## Usage\n\nThe full API of this library can be found in [api.md](api.md).\n\n```go\npackage main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/dedalus-labs/dedalus-sdk-go"\n\t"github.com/dedalus-labs/dedalus-sdk-go/option"\n\t"github.com/dedalus-labs/dedalus-sdk-go/shared"\n)\n\nfunc main() {\n\tclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\t\toption.WithAPIKey("My API Key"),     // defaults to os.LookupEnv("DEDALUS_API_KEY")\n\t\toption.WithEnvironmentDevelopment(), // defaults to option.WithEnvironmentProduction()\n\t)\n\tchatCompletion, err := client.Chat.Completions.New(context.TODO(), githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParams{\n\t\tModel: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsModelUnion](shared.UnionString("openai/gpt-5-nano")),\n\t\tMessages: githubcomdedaluslabsdedalussdkgo.F([]githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsMessageUnion{githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParam{\n\t\t\tRole:    githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParamRoleSystem),\n\t\t\tContent: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParamContentUnion](shared.UnionString("You are Stephen Dedalus. Respond in morose Joycean malaise.")),\n\t\t}, githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParam{\n\t\t\tRole:    githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParamRoleUser),\n\t\t\tContent: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParamContentUnion](shared.UnionString("Hello, how are you today?")),\n\t\t}}),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", chatCompletion.ID)\n}\n\n```\n\n### Request fields\n\nAll request parameters are wrapped in a generic `Field` type,\nwhich we use to distinguish zero values from null or omitted fields.\n\nThis prevents accidentally sending a zero value if you forget a required parameter,\nand enables explicitly sending `null`, `false`, `\'\'`, or `0` on optional parameters.\nAny field not specified is not sent.\n\nTo construct fields with values, use the helpers `String()`, `Int()`, `Float()`, or most commonly, the generic `F[T]()`.\nTo send a null, use `Null[T]()`, and to send a nonconforming value, use `Raw[T](any)`. For example:\n\n```go\nparams := FooParams{\n\tName: SDK_PackageName.F("hello"),\n\n\t// Explicitly send `"description": null`\n\tDescription: SDK_PackageName.Null[string](),\n\n\tPoint: SDK_PackageName.F(SDK_PackageName.Point{\n\t\tX: SDK_PackageName.Int(0),\n\t\tY: SDK_PackageName.Int(1),\n\n\t\t// In cases where the API specifies a given type,\n\t\t// but you want to send something else, use `Raw`:\n\t\tZ: SDK_PackageName.Raw[int64](0.01), // sends a float\n\t}),\n}\n```\n\n### Response objects\n\nAll fields in response structs are value types (not pointers or wrappers).\n\nIf a given field is `null`, not present, or invalid, the corresponding field\nwill simply be its zero value.\n\nAll response structs also include a special `JSON` field, containing more detailed\ninformation about each property, which you can use like so:\n\n```go\nif res.Name == "" {\n\t// true if `"name"` is either not present or explicitly null\n\tres.JSON.Name.IsNull()\n\n\t// true if the `"name"` key was not present in the response JSON at all\n\tres.JSON.Name.IsMissing()\n\n\t// When the API returns data that cannot be coerced to the expected type:\n\tif res.JSON.Name.IsInvalid() {\n\t\traw := res.JSON.Name.Raw()\n\n\t\tlegacyName := struct{\n\t\t\tFirst string `json:"first"`\n\t\t\tLast  string `json:"last"`\n\t\t}{}\n\t\tjson.Unmarshal([]byte(raw), &legacyName)\n\t\tname = legacyName.First + " " + legacyName.Last\n\t}\n}\n```\n\nThese `.JSON` structs also include an `Extras` map containing\nany properties in the json response that were not specified\nin the struct. This can be useful for API features not yet\npresent in the SDK.\n\n```go\nbody := res.JSON.ExtraFields["my_unexpected_field"].Raw()\n```\n\n### RequestOptions\n\nThis library uses the functional options pattern. Functions defined in the\n`SDK_PackageOptionName` package return a `RequestOption`, which is a closure that mutates a\n`RequestConfig`. These options can be supplied to the client or at individual\nrequests. For example:\n\n```go\nclient := SDK_PackageName.SDK_ClientInitializerName(\n\t// Adds a header to every request made by the client\n\tSDK_PackageOptionName.WithHeader("X-Some-Header", "custom_header_info"),\n)\n\nclient.Chat.Completions.New(context.TODO(), ...,\n\t// Override the header\n\tSDK_PackageOptionName.WithHeader("X-Some-Header", "some_other_custom_header_info"),\n\t// Add an undocumented field to the request body, using sjson syntax\n\tSDK_PackageOptionName.WithJSONSet("some.json.path", map[string]string{"my": "object"}),\n)\n```\n\nSee the [full list of request options](https://pkg.go.dev/github.com/dedalus-labs/dedalus-sdk-go/SDK_PackageOptionName).\n\n### Pagination\n\nThis library provides some conveniences for working with paginated list endpoints.\n\nYou can use `.ListAutoPaging()` methods to iterate through items across all pages:\n\n\n\nOr you can use simple `.List()` methods to fetch a single page and receive a standard response object\nwith additional helper methods like `.GetNextPage()`, e.g.:\n\n\n\n### Errors\n\nWhen the API returns a non-success status code, we return an error with type\n`*SDK_PackageName.Error`. This contains the `StatusCode`, `*http.Request`, and\n`*http.Response` values of the request, as well as the JSON of the error body\n(much like other response objects in the SDK).\n\nTo handle errors, we recommend that you use the `errors.As` pattern:\n\n```go\n_, err := client.Chat.Completions.New(context.TODO(), githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParams{\n\tModel: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsModelUnion](shared.UnionString("openai/gpt-5-nano")),\n\tMessages: githubcomdedaluslabsdedalussdkgo.F([]githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsMessageUnion{githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParam{\n\t\tRole:    githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParamRoleSystem),\n\t\tContent: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParamContentUnion](shared.UnionString("You are Stephen Dedalus. Respond in morose Joycean malaise.")),\n\t}, githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParam{\n\t\tRole:    githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParamRoleUser),\n\t\tContent: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParamContentUnion](shared.UnionString("Hello, how are you today?")),\n\t}}),\n})\nif err != nil {\n\tvar apierr *githubcomdedaluslabsdedalussdkgo.Error\n\tif errors.As(err, &apierr) {\n\t\tprintln(string(apierr.DumpRequest(true)))  // Prints the serialized HTTP request\n\t\tprintln(string(apierr.DumpResponse(true))) // Prints the serialized HTTP response\n\t}\n\tpanic(err.Error()) // GET "/v1/chat/completions": 400 Bad Request { ... }\n}\n```\n\nWhen other errors occur, they are returned unwrapped; for example,\nif HTTP transport fails, you might receive `*url.Error` wrapping `*net.OpError`.\n\n### Timeouts\n\nRequests do not time out by default; use context to configure a timeout for a request lifecycle.\n\nNote that if a request is [retried](#retries), the context timeout does not start over.\nTo set a per-retry timeout, use `SDK_PackageOptionName.WithRequestTimeout()`.\n\n```go\n// This sets the timeout for the request, including all the retries.\nctx, cancel := context.WithTimeout(context.Background(), 5*time.Minute)\ndefer cancel()\nclient.Chat.Completions.New(\n\tctx,\n\tgithubcomdedaluslabsdedalussdkgo.ChatCompletionNewParams{\n\t\tModel: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsModelUnion](shared.UnionString("openai/gpt-5-nano")),\n\t\tMessages: githubcomdedaluslabsdedalussdkgo.F([]githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsMessageUnion{githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParam{\n\t\t\tRole:    githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParamRoleSystem),\n\t\t\tContent: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParamContentUnion](shared.UnionString("You are Stephen Dedalus. Respond in morose Joycean malaise.")),\n\t\t}, githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParam{\n\t\t\tRole:    githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParamRoleUser),\n\t\t\tContent: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParamContentUnion](shared.UnionString("Hello, how are you today?")),\n\t\t}}),\n\t},\n\t// This sets the per-retry timeout\n\toption.WithRequestTimeout(20*time.Second),\n)\n```\n\n### File uploads\n\nRequest parameters that correspond to file uploads in multipart requests are typed as\n`param.Field[io.Reader]`. The contents of the `io.Reader` will by default be sent as a multipart form\npart with the file name of "anonymous_file" and content-type of "application/octet-stream".\n\nThe file name and content-type can be customized by implementing `Name() string` or `ContentType()\nstring` on the run-time type of `io.Reader`. Note that `os.File` implements `Name() string`, so a\nfile returned by `os.Open` will be sent with the file name on disk.\n\nWe also provide a helper `SDK_PackageName.FileParam(reader io.Reader, filename string, contentType string)`\nwhich can be used to wrap any `io.Reader` with the appropriate file name and content type.\n\n```go\n// A file from the file system\nfile, err := os.Open("/path/to/file")\ngithubcomdedaluslabsdedalussdkgo.AudioTranscriptionNewParams{\n\tFile:  githubcomdedaluslabsdedalussdkgo.F[io.Reader](file),\n\tModel: githubcomdedaluslabsdedalussdkgo.F("model"),\n}\n\n// A file from a string\ngithubcomdedaluslabsdedalussdkgo.AudioTranscriptionNewParams{\n\tFile:  githubcomdedaluslabsdedalussdkgo.F[io.Reader](strings.NewReader("my file contents")),\n\tModel: githubcomdedaluslabsdedalussdkgo.F("model"),\n}\n\n// With a custom filename and contentType\ngithubcomdedaluslabsdedalussdkgo.AudioTranscriptionNewParams{\n\tFile:  githubcomdedaluslabsdedalussdkgo.FileParam(strings.NewReader(`{"hello": "foo"}`), "file.go", "application/json"),\n\tModel: githubcomdedaluslabsdedalussdkgo.F("model"),\n}\n```\n\n### Retries\n\nCertain errors will be automatically retried 2 times by default, with a short exponential backoff.\nWe retry by default all connection errors, 408 Request Timeout, 409 Conflict, 429 Rate Limit,\nand >=500 Internal errors.\n\nYou can use the `WithMaxRetries` option to configure or disable this:\n\n```go\n// Configure the default for all requests:\nclient := githubcomdedaluslabsdedalussdkgo.NewClient(\n\toption.WithMaxRetries(0), // default is 2\n)\n\n// Override per-request:\nclient.Chat.Completions.New(\n\tcontext.TODO(),\n\tgithubcomdedaluslabsdedalussdkgo.ChatCompletionNewParams{\n\t\tModel: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsModelUnion](shared.UnionString("openai/gpt-5-nano")),\n\t\tMessages: githubcomdedaluslabsdedalussdkgo.F([]githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsMessageUnion{githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParam{\n\t\t\tRole:    githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParamRoleSystem),\n\t\t\tContent: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParamContentUnion](shared.UnionString("You are Stephen Dedalus. Respond in morose Joycean malaise.")),\n\t\t}, githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParam{\n\t\t\tRole:    githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParamRoleUser),\n\t\t\tContent: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParamContentUnion](shared.UnionString("Hello, how are you today?")),\n\t\t}}),\n\t},\n\toption.WithMaxRetries(5),\n)\n```\n\n\n### Accessing raw response data (e.g. response headers)\n\nYou can access the raw HTTP response data by using the `option.WithResponseInto()` request option. This is useful when\nyou need to examine response headers, status codes, or other details.\n\n```go\n// Create a variable to store the HTTP response\nvar response *http.Response\nchatCompletion, err := client.Chat.Completions.New(\n\tcontext.TODO(),\n\tgithubcomdedaluslabsdedalussdkgo.ChatCompletionNewParams{\n\t\tModel: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsModelUnion](shared.UnionString("openai/gpt-5-nano")),\n\t\tMessages: githubcomdedaluslabsdedalussdkgo.F([]githubcomdedaluslabsdedalussdkgo.ChatCompletionNewParamsMessageUnion{githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParam{\n\t\t\tRole:    githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParamRoleSystem),\n\t\t\tContent: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionSystemMessageParamContentUnion](shared.UnionString("You are Stephen Dedalus. Respond in morose Joycean malaise.")),\n\t\t}, githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParam{\n\t\t\tRole:    githubcomdedaluslabsdedalussdkgo.F(githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParamRoleUser),\n\t\t\tContent: githubcomdedaluslabsdedalussdkgo.F[githubcomdedaluslabsdedalussdkgo.ChatCompletionUserMessageParamContentUnion](shared.UnionString("Hello, how are you today?")),\n\t\t}}),\n\t},\n\toption.WithResponseInto(&response),\n)\nif err != nil {\n\t// handle error\n}\nfmt.Printf("%+v\\n", chatCompletion)\n\nfmt.Printf("Status Code: %d\\n", response.StatusCode)\nfmt.Printf("Headers: %+#v\\n", response.Header)\n```\n\n### Making custom/undocumented requests\n\nThis library is typed for convenient access to the documented API. If you need to access undocumented\nendpoints, params, or response properties, the library can still be used.\n\n#### Undocumented endpoints\n\nTo make requests to undocumented endpoints, you can use `client.Get`, `client.Post`, and other HTTP verbs.\n`RequestOptions` on the client, such as retries, will be respected when making these requests.\n\n```go\nvar (\n    // params can be an io.Reader, a []byte, an encoding/json serializable object,\n    // or a "…Params" struct defined in this library.\n    params map[string]interface{}\n\n    // result can be an []byte, *http.Response, a encoding/json deserializable object,\n    // or a model defined in this library.\n    result *http.Response\n)\nerr := client.Post(context.Background(), "/unspecified", params, &result)\nif err != nil {\n    …\n}\n```\n\n#### Undocumented request params\n\nTo make requests using undocumented parameters, you may use either the `SDK_PackageOptionName.WithQuerySet()`\nor the `SDK_PackageOptionName.WithJSONSet()` methods.\n\n```go\nparams := FooNewParams{\n    ID:   SDK_PackageName.F("id_xxxx"),\n    Data: SDK_PackageName.F(FooNewParamsData{\n        FirstName: SDK_PackageName.F("John"),\n    }),\n}\nclient.Foo.New(context.Background(), params, SDK_PackageOptionName.WithJSONSet("data.last_name", "Doe"))\n```\n\n#### Undocumented response properties\n\nTo access undocumented response properties, you may either access the raw JSON of the response as a string\nwith `result.JSON.RawJSON()`, or get the raw JSON of a particular field on the result with\n`result.JSON.Foo.Raw()`.\n\nAny fields that are not present on the response struct will be saved and can be accessed by `result.JSON.ExtraFields()` which returns the extra fields as a `map[string]Field`.\n\n### Middleware\n\nWe provide `SDK_PackageOptionName.WithMiddleware` which applies the given\nmiddleware to requests.\n\n```go\nfunc Logger(req *http.Request, next SDK_PackageOptionName.MiddlewareNext) (res *http.Response, err error) {\n\t// Before the request\n\tstart := time.Now()\n\tLogReq(req)\n\n\t// Forward the request to the next handler\n\tres, err = next(req)\n\n\t// Handle stuff after the request\n\tend := time.Now()\n\tLogRes(res, err, start - end)\n\n    return res, err\n}\n\nclient := SDK_PackageName.SDK_ClientInitializerName(\n\tSDK_PackageOptionName.WithMiddleware(Logger),\n)\n```\n\nWhen multiple middlewares are provided as variadic arguments, the middlewares\nare applied left to right. If `SDK_PackageOptionName.WithMiddleware` is given\nmultiple times, for example first in the client then the method, the\nmiddleware in the client will run first and the middleware given in the method\nwill run next.\n\nYou may also replace the default `http.Client` with\n`SDK_PackageOptionName.WithHTTPClient(client)`. Only one http client is\naccepted (this overwrites any previous client) and receives requests after any\nmiddleware has been applied.\n\n## Semantic versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n2. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/dedalus-labs/dedalus-sdk-go/issues) with questions, bugs, or suggestions.\n\n## Contributing\n\nSee [the contributing documentation](./CONTRIBUTING.md).\n',
  },
  {
    language: 'java',
    content:
      '# Dedalus Java API Library\n\n<!-- x-release-please-start-version -->\n[![Maven Central](https://img.shields.io/maven-central/v/com.dedalus_sdk.api/dedalus-java)](https://central.sonatype.com/artifact/com.dedalus_sdk.api/dedalus-java/0.0.1)\n[![javadoc](https://javadoc.io/badge2/com.dedalus_sdk.api/dedalus-java/0.0.1/javadoc.svg)](https://javadoc.io/doc/com.dedalus_sdk.api/dedalus-java/0.0.1)\n<!-- x-release-please-end -->\n\nThe Dedalus Java SDK provides convenient access to the [Dedalus REST API](https://docs.dedaluslabs.ai)   from applications written in Java.\n\n\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Dedalus MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=dedalus-labs-mcp&config=eyJuYW1lIjoiZGVkYWx1cy1sYWJzLW1jcCIsInRyYW5zcG9ydCI6Imh0dHAiLCJ1cmwiOiJodHRwczovL2RlZGFsdXMtc2RrLnN0bG1jcC5jb20iLCJoZWFkZXJzIjp7IngtZGVkYWx1cy1hcGkta2V5IjoiTXkgQVBJIEtleSIsIngtYXBpLWtleSI6Ik15IFggQVBJIEtleSJ9fQ)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dedalus-labs-mcp%22%2C%22type%22%3A%22http%22%2C%22url%22%3A%22https%3A%2F%2Fdedalus-sdk.stlmcp.com%22%2C%22headers%22%3A%7B%22x-dedalus-api-key%22%3A%22My%20API%20Key%22%2C%22x-api-key%22%3A%22My%20X%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n<!-- x-release-please-start-version -->\n\nThe REST API documentation can be found on [docs.dedaluslabs.ai](https://docs.dedaluslabs.ai). Javadocs are available on [javadoc.io](https://javadoc.io/doc/com.dedalus_sdk.api/dedalus-java/0.0.1).\n\n<!-- x-release-please-end -->\n\n## Installation\n\n<!-- x-release-please-start-version -->\n\n### Gradle\n\n~~~kotlin\nimplementation("com.dedalus_sdk.api:dedalus-java:0.0.1")\n~~~\n\n### Maven\n\n~~~xml\n<dependency>\n  <groupId>com.dedalus_sdk.api</groupId>\n  <artifactId>dedalus-java</artifactId>\n  <version>0.0.1</version>\n</dependency>\n~~~\n\n<!-- x-release-please-end -->\n\n## Requirements\n\nThis library requires Java 8 or later.\n\n## Usage\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams;\n\n// Configures using the `dedalus.apiKey`, `dedalus.xApiKey`, `dedalus.asUrl`, `dedalus.orgId`, `dedalus.provider`, `dedalus.providerKey`, `dedalus.providerModel` and `dedalus.baseUrl` system properties\n// Or configures using the `DEDALUS_API_KEY`, `DEDALUS_X_API_KEY`, `DEDALUS_AS_URL`, `DEDALUS_ORG_ID`, `DEDALUS_PROVIDER`, `DEDALUS_PROVIDER_KEY`, `DEDALUS_PROVIDER_MODEL` and `DEDALUS_BASE_URL` environment variables\nDedalusClient client = DedalusOkHttpClient.fromEnv();\n\nChatCompletionCreateParams params = ChatCompletionCreateParams.builder()\n    .model("openai/gpt-5")\n    .build();\nChatCompletion chatCompletion = client.chat().completions().create(params);\n```\n\n## Client configuration\n\nConfigure the client using system properties or environment variables:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\n\n// Configures using the `dedalus.apiKey`, `dedalus.xApiKey`, `dedalus.asUrl`, `dedalus.orgId`, `dedalus.provider`, `dedalus.providerKey`, `dedalus.providerModel` and `dedalus.baseUrl` system properties\n// Or configures using the `DEDALUS_API_KEY`, `DEDALUS_X_API_KEY`, `DEDALUS_AS_URL`, `DEDALUS_ORG_ID`, `DEDALUS_PROVIDER`, `DEDALUS_PROVIDER_KEY`, `DEDALUS_PROVIDER_MODEL` and `DEDALUS_BASE_URL` environment variables\nDedalusClient client = DedalusOkHttpClient.fromEnv();\n```\n\nOr manually:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .apiKey("My API Key")\n    .build();\n```\n\nOr using a combination of the two approaches:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    // Configures using the `dedalus.apiKey`, `dedalus.xApiKey`, `dedalus.asUrl`, `dedalus.orgId`, `dedalus.provider`, `dedalus.providerKey`, `dedalus.providerModel` and `dedalus.baseUrl` system properties\n    // Or configures using the `DEDALUS_API_KEY`, `DEDALUS_X_API_KEY`, `DEDALUS_AS_URL`, `DEDALUS_ORG_ID`, `DEDALUS_PROVIDER`, `DEDALUS_PROVIDER_KEY`, `DEDALUS_PROVIDER_MODEL` and `DEDALUS_BASE_URL` environment variables\n    .fromEnv()\n    .apiKey("My API Key")\n    .build();\n```\n\nSee this table for the available options:\n\n| Setter          | System property         | Environment variable     | Required | Default value                  |\n| --------------- | ----------------------- | ------------------------ | -------- | ------------------------------ |\n| `apiKey`        | `dedalus.apiKey`        | `DEDALUS_API_KEY`        | false    | -                              |\n| `xApiKey`       | `dedalus.xApiKey`       | `DEDALUS_X_API_KEY`      | false    | -                              |\n| `asBaseUrl`     | `dedalus.asUrl`         | `DEDALUS_AS_URL`         | false    | `"https://as.dedaluslabs.ai"`  |\n| `dedalusOrgId`  | `dedalus.orgId`         | `DEDALUS_ORG_ID`         | false    | -                              |\n| `provider`      | `dedalus.provider`      | `DEDALUS_PROVIDER`       | false    | -                              |\n| `providerKey`   | `dedalus.providerKey`   | `DEDALUS_PROVIDER_KEY`   | false    | -                              |\n| `providerModel` | `dedalus.providerModel` | `DEDALUS_PROVIDER_MODEL` | false    | -                              |\n| `baseUrl`       | `dedalus.baseUrl`       | `DEDALUS_BASE_URL`       | true     | `"https://api.dedaluslabs.ai"` |\n\nSystem properties take precedence over environment variables.\n\n> [!TIP]\n> Don\'t create more than one client in the same application. Each client has a connection pool and\n> thread pools, which are more efficient to share between requests.\n\n### Modifying configuration\n\nTo temporarily use a modified client configuration, while reusing the same connection and thread       pools, call `withOptions()` on any client or service:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\n\nDedalusClient clientWithOptions = client.withOptions(optionsBuilder -> {\n    optionsBuilder.baseUrl("https://example.com");\n    optionsBuilder.maxRetries(42);\n});\n```\n\nThe `withOptions()` method does not affect the original client or service.\n\n## Requests and responses\n\nTo send a request to the Dedalus API, build an instance of some `Params` class and pass it to the     corresponding client method. When the response is received, it will be deserialized into an instance of     a Java class.\n\nFor example, `client.chat().completions().create(...)` should be called with an instance of `CompletionCreateParams`, and it     will return an instance of `ChatCompletion`.\n\n## Immutability\n\nEach class in the SDK has an associated   [builder](https://blogs.oracle.com/javamagazine/post/exploring-joshua-blochs-builder-design-pattern-in-java)   or factory method for constructing it.\n\nEach class is [immutable](https://docs.oracle.com/javase/tutorial/essential/concurrency/immutable.html)   once constructed. If the class has an associated builder, then it has a `toBuilder()` method, which can   be used to convert it back to a builder for making a modified copy.\n\nBecause each class is immutable, builder modification will _never_ affect already built class instances.\n\n## Asynchronous execution\n\nThe default client is synchronous. To switch to asynchronous execution, call the `async()` method:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams;\nimport java.util.concurrent.CompletableFuture;\n\n// Configures using the `dedalus.apiKey`, `dedalus.xApiKey`, `dedalus.asUrl`, `dedalus.orgId`, `dedalus.provider`, `dedalus.providerKey`, `dedalus.providerModel` and `dedalus.baseUrl` system properties\n// Or configures using the `DEDALUS_API_KEY`, `DEDALUS_X_API_KEY`, `DEDALUS_AS_URL`, `DEDALUS_ORG_ID`, `DEDALUS_PROVIDER`, `DEDALUS_PROVIDER_KEY`, `DEDALUS_PROVIDER_MODEL` and `DEDALUS_BASE_URL` environment variables\nDedalusClient client = DedalusOkHttpClient.fromEnv();\n\nChatCompletionCreateParams params = ChatCompletionCreateParams.builder()\n    .model("openai/gpt-5")\n    .build();\nCompletableFuture<ChatCompletion> chatCompletion = client.async().chat().completions().create(params);\n```\n\nOr create an asynchronous client from the beginning:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClientAsync;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClientAsync;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams;\nimport java.util.concurrent.CompletableFuture;\n\n// Configures using the `dedalus.apiKey`, `dedalus.xApiKey`, `dedalus.asUrl`, `dedalus.orgId`, `dedalus.provider`, `dedalus.providerKey`, `dedalus.providerModel` and `dedalus.baseUrl` system properties\n// Or configures using the `DEDALUS_API_KEY`, `DEDALUS_X_API_KEY`, `DEDALUS_AS_URL`, `DEDALUS_ORG_ID`, `DEDALUS_PROVIDER`, `DEDALUS_PROVIDER_KEY`, `DEDALUS_PROVIDER_MODEL` and `DEDALUS_BASE_URL` environment variables\nDedalusClientAsync client = DedalusOkHttpClientAsync.fromEnv();\n\nChatCompletionCreateParams params = ChatCompletionCreateParams.builder()\n    .model("openai/gpt-5")\n    .build();\nCompletableFuture<ChatCompletion> chatCompletion = client.chat().completions().create(params);\n```\n\nThe asynchronous client supports the same options as the synchronous one, except most methods return `CompletableFuture`s.\n\n## Streaming\n\nThe SDK defines methods that return response "chunk" streams, where each chunk can be individually     processed as soon as it arrives instead of waiting on the full response. Streaming methods generally     correspond to [SSE](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events) or     [JSONL](https://jsonlines.org) responses.\n\nSome of these methods may have streaming and non-streaming variants, but a streaming method will     always have a `Streaming` suffix in its name, even if it doesn\'t have a non-streaming variant.\n\nThese streaming methods return [`StreamResponse`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/core/http/StreamResponse.kt) for synchronous clients:\n\n```java\nimport com.dedalus_sdk.api.core.http.StreamResponse;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionChunk;\n\ntry (StreamResponse<ChatCompletionChunk> streamResponse = client.chat().completions().createStreaming(params)) {\n    streamResponse.stream().forEach(chunk -> {\n        System.out.println(chunk);\n    });\n    System.out.println("No more chunks!");\n}\n```\n\nOr [`AsyncStreamResponse`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/core/http/AsyncStreamResponse.kt) for asynchronous clients:\n\n```java\nimport com.dedalus_sdk.api.core.http.AsyncStreamResponse;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionChunk;\nimport java.util.Optional;\n\nclient.async().chat().completions().createStreaming(params).subscribe(chunk -> {\n    System.out.println(chunk);\n});\n\n// If you need to handle errors or completion of the stream\nclient.async().chat().completions().createStreaming(params).subscribe(new AsyncStreamResponse.Handler<>() {\n    @Override\n    public void onNext(ChatCompletionChunk chunk) {\n        System.out.println(chunk);\n    }\n\n    @Override\n    public void onComplete(Optional<Throwable> error) {\n        if (error.isPresent()) {\n            System.out.println("Something went wrong!");\n            throw new RuntimeException(error.get());\n        } else {\n            System.out.println("No more chunks!");\n        }\n    }\n});\n\n// Or use futures\nclient.async().chat().completions().createStreaming(params)\n    .subscribe(chunk -> {\n        System.out.println(chunk);\n    })\n    .onCompleteFuture()\n    .whenComplete((unused, error) -> {\n        if (error != null) {\n            System.out.println("Something went wrong!");\n            throw new RuntimeException(error);\n        } else {\n            System.out.println("No more chunks!");\n        }\n    });\n```\n\nAsync streaming uses a dedicated per-client cached thread pool         [`Executor`](https://docs.oracle.com/javase/8/docs/api/java/util/concurrent/Executor.html)         to stream without blocking the current thread. This default is suitable for most purposes.\n\nTo use a different `Executor`, configure the subscription using the `executor` parameter:\n\n```java\nimport java.util.concurrent.Executor;\nimport java.util.concurrent.Executors;\n\nExecutor executor = Executors.newFixedThreadPool(4);\nclient.async().chat().completions().createStreaming(params).subscribe(\n    chunk -> System.out.println(chunk), executor\n);\n```\n\nOr configure the client globally using the `streamHandlerExecutor` method:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport java.util.concurrent.Executors;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .streamHandlerExecutor(Executors.newFixedThreadPool(4))\n    .build();\n```\n\n## File uploads\n\nThe SDK defines methods that accept files.\n\nTo upload a file, pass a [`Path`](https://docs.oracle.com/javase/8/docs/api/java/nio/file/Path.html):\n\n```java\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateParams;\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateResponse;\nimport java.nio.file.Paths;\n\nTranscriptionCreateParams params = TranscriptionCreateParams.builder()\n    .model("model")\n    .file(Paths.get("/path/to/file"))\n    .build();\nTranscriptionCreateResponse transcription = client.audio().transcriptions().create(params);\n```\n\nOr an arbitrary [`InputStream`](https://docs.oracle.com/javase/8/docs/api/java/io/InputStream.html):\n\n```java\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateParams;\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateResponse;\nimport java.net.URL;\n\nTranscriptionCreateParams params = TranscriptionCreateParams.builder()\n    .model("model")\n    .file(new URL("https://example.com//path/to/file").openStream())\n    .build();\nTranscriptionCreateResponse transcription = client.audio().transcriptions().create(params);\n```\n\nOr a `byte[]` array:\n\n```java\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateParams;\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateResponse;\n\nTranscriptionCreateParams params = TranscriptionCreateParams.builder()\n    .model("model")\n    .file("content".getBytes())\n    .build();\nTranscriptionCreateResponse transcription = client.audio().transcriptions().create(params);\n```\n\nNote that when passing a non-`Path` its filename is unknown so it will not be included in the request.     To manually set a filename, pass a [`MultipartField`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/core/Values.kt):\n\n```java\nimport com.dedalus_sdk.api.core.MultipartField;\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateParams;\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateResponse;\nimport java.io.InputStream;\nimport java.net.URL;\n\nTranscriptionCreateParams params = TranscriptionCreateParams.builder()\n    .model("model")\n    .file(MultipartField.<InputStream>builder()\n        .value(new URL("https://example.com//path/to/file").openStream())\n        .filename("/path/to/file")\n        .build())\n    .build();\nTranscriptionCreateResponse transcription = client.audio().transcriptions().create(params);\n```\n\n## Binary responses\n\nThe SDK defines methods that return binary responses, which are used for API responses that shouldn\'t     necessarily be parsed, like non-JSON data.\n\nThese methods return [`HttpResponse`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/core/http/HttpResponse.kt):\n\n```java\nimport com.dedalus_sdk.api.core.http.HttpResponse;\nimport com.dedalus_sdk.api.models.audio.speech.SpeechCreateParams;\n\nSpeechCreateParams params = SpeechCreateParams.builder()\n    .input("input")\n    .model(SpeechCreateParams.Model.TTS_1)\n    .voice(SpeechCreateParams.Voice.UnionMember1.ALLOY)\n    .build();\nHttpResponse speech = client.audio().speech().create(params);\n```\n\nTo save the response content to a file, use the     [`Files.copy(...)`](https://docs.oracle.com/javase/8/docs/api/java/nio/file/Files.html#copy-java.io.InputStream-java.nio.file.Path-java.nio.file.CopyOption...-)     method:\n\n```java\nimport com.dedalus_sdk.api.core.http.HttpResponse;\nimport java.nio.file.Files;\nimport java.nio.file.Paths;\nimport java.nio.file.StandardCopyOption;\n\ntry (HttpResponse response = client.audio().speech().create(params)) {\n    Files.copy(\n        response.body(),\n        Paths.get(path),\n        StandardCopyOption.REPLACE_EXISTING\n    );\n} catch (Exception e) {\n    System.out.println("Something went wrong!");\n    throw new RuntimeException(e);\n}\n```\n\nOr transfer the response content to any     [`OutputStream`](https://docs.oracle.com/javase/8/docs/api/java/io/OutputStream.html):\n\n```java\nimport com.dedalus_sdk.api.core.http.HttpResponse;\nimport java.nio.file.Files;\nimport java.nio.file.Paths;\n\ntry (HttpResponse response = client.audio().speech().create(params)) {\n    response.body().transferTo(Files.newOutputStream(Paths.get(path)));\n} catch (Exception e) {\n    System.out.println("Something went wrong!");\n    throw new RuntimeException(e);\n}\n```\n\n## Raw responses\n\nThe SDK defines methods that deserialize responses into instances of Java classes.       However, these methods don\'t provide access to the response headers, status code, or the raw response       body.\n\nTo access this data, prefix any HTTP method call on a client or service with `withRawResponse()`:\n\n```java\nimport com.dedalus_sdk.api.core.http.Headers;\nimport com.dedalus_sdk.api.core.http.HttpResponseFor;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams;\n\nChatCompletionCreateParams params = ChatCompletionCreateParams.builder()\n    .model("openai/gpt-5")\n    .build();\nHttpResponseFor<ChatCompletion> chatCompletion = client.chat().completions().withRawResponse().create(params);\n\nint statusCode = chatCompletion.statusCode();\nHeaders headers = chatCompletion.headers();\n```\n\nYou can still deserialize the response into an instance of a Java class if needed:\n\n```java\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion;\n\nChatCompletion parsedChatCompletion = chatCompletion.parse();\n```\n\n## Error handling\n\nThe SDK throws custom unchecked exception types:\n\n- [`DedalusServiceException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusServiceException.kt): Base class for HTTP errors. See this table for which exception       subclass is thrown for each HTTP status code:\n\n  | Status | Exception                                          |\n  | ------ | -------------------------------------------------- |\n  | 400    | [`BadRequestException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/BadRequestException.kt)           |\n  | 401    | [`UnauthorizedException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/UnauthorizedException.kt)         |\n  | 403    | [`PermissionDeniedException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/PermissionDeniedException.kt)     |\n  | 404    | [`NotFoundException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/NotFoundException.kt)             |\n  | 422    | [`UnprocessableEntityException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/UnprocessableEntityException.kt)  |\n  | 429    | [`RateLimitException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/RateLimitException.kt)            |\n  | 5xx    | [`InternalServerException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/InternalServerException.kt)       |\n  | others | [`UnexpectedStatusCodeException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/UnexpectedStatusCodeException.kt) |\n\n  [`SseException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/SseException.kt) is thrown for errors encountered during             [SSE streaming](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events) after a             successful initial HTTP response.\n\n- [`DedalusIoException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusIoException.kt): I/O networking errors.\n\n- [`DedalusRetryableException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusRetryableException.kt): Generic error indicating a failure that could be retried by the client.\n\n- [`DedalusInvalidDataException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusInvalidDataException.kt): Failure to interpret successfully parsed data. For example,       when accessing a property that\'s supposed to be required, but the API unexpectedly omitted it from the       response.\n\n- [`DedalusException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusException.kt): Base class for all exceptions. Most errors will result in one of the       previously mentioned ones, but completely generic errors may be thrown using the base class.\n\n\n\n## Logging\n\nEnable logging by setting the `DEDALUS_LOG` environment variable to   `info`:\n\n```sh\nexport DEDALUS_LOG=info\n```\n\nOr to `debug` for more verbose logging:\n\n```sh\nexport DEDALUS_LOG=debug\n```\n\nOr configure the client manually using the `logLevel` method:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.core.LogLevel;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .logLevel(LogLevel.INFO)\n    .build();\n```\n\n## ProGuard and R8\n\nAlthough the SDK uses reflection, it is still usable with     [ProGuard](https://github.com/Guardsquare/proguard) and     [R8](https://developer.android.com/topic/performance/app-optimization/enable-app-optimization) because     `dedalus-java-core` is published with a     [configuration file](dedalus-java-core/src/main/resources/META-INF/proguard/dedalus-java-core.pro) containing     [keep rules](https://www.guardsquare.com/manual/configuration/usage).\n\nProGuard and R8 should automatically detect and use the published rules, but you can also manually copy     the keep rules if necessary.\n\n\n\n\n\n## Jackson\n\nThe SDK depends on [Jackson](https://github.com/FasterXML/jackson) for JSON     serialization/deserialization. It is compatible with version 2.13.4 or higher,     but depends on version 2.18.2 by default.\n\nThe SDK throws an exception if it detects an incompatible Jackson version at runtime (e.g. if the     default version was overridden in your Maven or Gradle config).\n\nIf the SDK threw an exception, but you\'re _certain_ the version is compatible, then disable the version     check using the `checkJacksonVersionCompatibility` on [`DedalusOkHttpClient`](dedalus-java-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClient.kt) or     [`DedalusOkHttpClientAsync`](dedalus-java-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClientAsync.kt).\n\n> [!CAUTION]\n> We make no guarantee that the SDK works correctly when the Jackson version check is disabled.\n\nAlso note that there are bugs in older Jackson versions that can affect the SDK. We don\'t work around all     Jackson bugs ([example](https://github.com/FasterXML/jackson-databind/issues/3240)) and expect users to     upgrade Jackson for those instead.\n\n## Network options\n\n### Retries\n\nThe SDK automatically retries 2 times by default, with a short exponential backoff between requests.\n\nOnly the following error types are retried:\n- Connection errors (for example, due to a network connectivity problem)\n- 408 Request Timeout\n- 409 Conflict\n- 429 Rate Limit\n- 5xx Internal\n\nThe API may also explicitly instruct the SDK to retry or not retry a request.\n\nTo set a custom number of retries, configure the client using the `maxRetries` method:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .maxRetries(4)\n    .build();\n```\n\n### Timeouts\n\nRequests time out after 1 minute by default.\n\nTo set a custom timeout, configure the method call using the `timeout` method:\n\n```java\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion;\n\nChatCompletion chatCompletion = client.chat().completions().create(\n  params, RequestOptions.builder().timeout(Duration.ofSeconds(30)).build()\n);\n```\n\nOr configure the default for all method calls at the client level:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport java.time.Duration;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .timeout(Duration.ofSeconds(30))\n    .build();\n```\n\n### Proxies\n\nTo route requests through a proxy, configure the client using the `proxy` method:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport java.net.InetSocketAddress;\nimport java.net.Proxy;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .proxy(new Proxy(\n      Proxy.Type.HTTP, new InetSocketAddress(\n        "https://example.com", 8080\n      )\n    ))\n    .build();\n```\n\nIf the proxy responds with `407 Proxy Authentication Required`, supply credentials by also   configuring `proxyAuthenticator`:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport com.dedalus_sdk.api.core.http.ProxyAuthenticator;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .proxy(...)\n    // Or a custom implementation of `ProxyAuthenticator`.\n    .proxyAuthenticator(ProxyAuthenticator.basic("username", "password"))\n    .build();\n```\n\n### Connection pooling\n\nTo customize the underlying OkHttp connection pool, configure the client using the   `maxIdleConnections` and `keepAliveDuration` methods:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\nimport java.time.Duration;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .fromEnv()\n    // If `maxIdleConnections` is set, then `keepAliveDuration` must be set, and vice versa.\n    .maxIdleConnections(10)\n    .keepAliveDuration(Duration.ofMinutes(2))\n    .build();\n```\n\nIf both options are unset, OkHttp\'s default connection pool settings are used.\n\n### HTTPS\n\n> [!NOTE]\n> Most applications should not call these methods, and instead use the system defaults. The defaults include\n> special optimizations that can be lost if the implementations are modified.\n\nTo configure how HTTPS connections are secured, configure the client using the `sslSocketFactory`,   `trustManager`, and `hostnameVerifier` methods:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .fromEnv()\n    // If `sslSocketFactory` is set, then `trustManager` must be set, and vice versa.\n    .sslSocketFactory(yourSSLSocketFactory)\n    .trustManager(yourTrustManager)\n    .hostnameVerifier(yourHostnameVerifier)\n    .build();\n```\n\n### Environments\n\nThe SDK sends requests to the production by default. To send requests to a different     environment, configure the client like so:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .development()\n    .build();\n```\n\n### Custom HTTP client\n\nThe SDK consists of three artifacts:\n- `dedalus-java-core`\n  - Contains core SDK logic\n  - Does not depend on [OkHttp](https://square.github.io/okhttp)\n  - Exposes [`DedalusClient`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClient.kt), [`DedalusClientAsync`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientAsync.kt),             [`DedalusClientImpl`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientImpl.kt), and [`DedalusClientAsyncImpl`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientAsyncImpl.kt), all of which can             work with any HTTP client\n- `dedalus-java-client-okhttp`\n  - Depends on [OkHttp](https://square.github.io/okhttp)\n  - Exposes [`DedalusOkHttpClient`](dedalus-java-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClient.kt) and [`DedalusOkHttpClientAsync`](dedalus-java-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClientAsync.kt), which             provide a way to construct [`DedalusClientImpl`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientImpl.kt) and             [`DedalusClientAsyncImpl`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientAsyncImpl.kt), respectively, using OkHttp\n- `dedalus-java`\n  - Depends on and exposes the APIs of both `dedalus-java-core` and `dedalus-java-client-okhttp`\n  - Does not have its own logic\n\nThis structure allows replacing the SDK\'s default HTTP client without pulling in unnecessary dependencies.\n\n#### Customized [`OkHttpClient`](https://square.github.io/okhttp/3.x/okhttp/okhttp3/OkHttpClient.html)\n\n> [!TIP]\n> Try the available [network options](#network-options) before replacing the default client.\n\nTo use a customized `OkHttpClient`:\n\n1. Replace your [`dedalus-java` dependency](#installation) with `dedalus-java-core`\n2. Copy `dedalus-java-client-okhttp`\'s [`OkHttpClient`](dedalus-java-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/OkHttpClient.kt) class into your code and        customize it\n3. Construct [`DedalusClientImpl`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientImpl.kt) or [`DedalusClientAsyncImpl`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientAsyncImpl.kt), similarly to        [`DedalusOkHttpClient`](dedalus-java-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClient.kt) or [`DedalusOkHttpClientAsync`](dedalus-java-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClientAsync.kt), using your        customized client\n\n### Completely custom HTTP client\n\nTo use a completely custom HTTP client:\n\n1. Replace your [`dedalus-java` dependency](#installation) with `dedalus-java-core`\n2. Write a class that implements the [`HttpClient`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/core/http/HttpClient.kt) interface\n3. Construct [`DedalusClientImpl`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientImpl.kt) or [`DedalusClientAsyncImpl`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientAsyncImpl.kt), similarly to        [`DedalusOkHttpClient`](dedalus-java-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClient.kt) or [`DedalusOkHttpClientAsync`](dedalus-java-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClientAsync.kt), using your new        client class\n\n## Undocumented API functionality\n\nThe SDK is typed for convenient usage of the documented API. However, it also supports working with undocumented or not yet supported parts of the API.\n\n### Parameters\n\nTo set undocumented parameters, call the `putAdditionalHeader`, `putAdditionalQueryParam`, or       `putAdditionalBodyProperty` methods on any `Params` class:\n\n```java\nimport com.dedalus_sdk.api.core.JsonValue;\nimport com.dedalus_sdk.api.models.chat.completions.CompletionCreateParams;\n\nCompletionCreateParams params = CompletionCreateParams.builder()\n    .putAdditionalHeader("Secret-Header", "42")\n    .putAdditionalQueryParam("secret_query_param", "42")\n    .putAdditionalBodyProperty("secretProperty", JsonValue.from("42"))\n    .build();\n```\n\nThese can be accessed on the built object later using the `_additionalHeaders()`,       `_additionalQueryParams()`, and `_additionalBodyProperties()` methods.\n\nTo set a documented parameter or property to an undocumented or not yet supported _value_, pass a       [`JsonValue`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/core/Values.kt) object to its setter:\n\n```java\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams;\nimport com.dedalus_sdk.api.models.chat.completions.CompletionCreateParams;\n\nCompletionCreateParams params = CompletionCreateParams.builder()\n    .chatCompletionCreateParams(ChatCompletionCreateParams.builder()\n        .model("openai/gpt-5")\n        .build())\n    .build();\n```\n\nThe most straightforward way to create a [`JsonValue`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/core/Values.kt) is using its       `from(...)` method:\n\n```java\nimport com.dedalus_sdk.api.core.JsonValue;\nimport java.util.List;\nimport java.util.Map;\n\n// Create primitive JSON values\nJsonValue nullValue = JsonValue.from(null);\nJsonValue booleanValue = JsonValue.from(true);\nJsonValue numberValue = JsonValue.from(42);\nJsonValue stringValue = JsonValue.from("Hello World!");\n\n// Create a JSON array value equivalent to `["Hello", "World"]`\nJsonValue arrayValue = JsonValue.from(List.of(\n  "Hello", "World"\n));\n\n// Create a JSON object value equivalent to `{ "a": 1, "b": 2 }`\nJsonValue objectValue = JsonValue.from(Map.of(\n  "a", 1,\n  "b", 2\n));\n\n// Create an arbitrarily nested JSON equivalent to:\n// {\n//   "a": [1, 2],\n//   "b": [3, 4]\n// }\nJsonValue complexValue = JsonValue.from(Map.of(\n  "a", List.of(\n    1, 2\n  ),\n  "b", List.of(\n    3, 4\n  )\n));\n```\n\nNormally a `Builder` class\'s `build` method will throw         [`IllegalStateException`](https://docs.oracle.com/javase/8/docs/api/java/lang/IllegalStateException.html)         if any required parameter or property is unset.\n\nTo forcibly omit a required parameter or property, pass [`JsonMissing`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/core/Values.kt):\n\n```java\nimport com.dedalus_sdk.api.core.JsonMissing;\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams;\nimport com.dedalus_sdk.api.models.chat.completions.CompletionCreateParams;\n\nCompletionCreateParams params = CompletionCreateParams.builder()\n    .chatCompletionCreateParams(ChatCompletionCreateParams.builder()\n        .model("openai/gpt-5")\n        .build())\n    .model(JsonMissing.of())\n    .build();\n```\n\n### Response properties\n\nTo access undocumented response properties, call the `_additionalProperties()` method:\n\n```java\nimport com.dedalus_sdk.api.core.JsonValue;\nimport java.util.Map;\n\nMap<String, JsonValue> additionalProperties = client.chat().completions().create(params)._additionalProperties();\nJsonValue secretPropertyValue = additionalProperties.get("secretProperty");\n\nString result = secretPropertyValue.accept(new JsonValue.Visitor<>() {\n    @Override\n    public String visitNull() {\n        return "It\'s null!";\n    }\n\n    @Override\n    public String visitBoolean(boolean value) {\n        return "It\'s a boolean!";\n    }\n\n    @Override\n    public String visitNumber(Number value) {\n        return "It\'s a number!";\n    }\n\n    // Other methods include `visitMissing`, `visitString`, `visitArray`, and `visitObject`\n    // The default implementation of each unimplemented method delegates to `visitDefault`, which throws by default, but can also be overridden\n});\n```\n\nTo access a property\'s raw JSON value, which may be undocumented, call its `_` prefixed method:\n\n```java\nimport com.dedalus_sdk.api.core.JsonField;\nimport java.util.Optional;\n\nJsonField<Object> field = client.chat().completions().create(params)._field();\n\nif (field.isMissing()) {\n  // The property is absent from the JSON response\n} else if (field.isNull()) {\n  // The property was set to literal null\n} else {\n  // Check if value was provided as a string\n  // Other methods include `asNumber()`, `asBoolean()`, etc.\n  Optional<String> jsonString = field.asString();\n\n  // Try to deserialize into a custom type\n  MyClass myObject = field.asUnknown().orElseThrow().convert(MyClass.class);\n}\n```\n\n### Response validation\n\nIn rare cases, the API may return a response that doesn\'t match the expected type. For example, the SDK     may expect a property to contain a `String`, but the API could return something else.\n\nBy default, the SDK will not throw an exception in this case. It will throw     [`DedalusInvalidDataException`](dedalus-java-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusInvalidDataException.kt) only if you directly access the property.\n\nValidating the response is _not_ forwards compatible with new types from the API for existing fields.\n\nIf you would still prefer to check that the response is completely well-typed upfront, then either call     `validate()`:\n\n```java\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion;\n\nChatCompletion chatCompletion = client.chat().completions().create(params).validate();\n```\n\nOr configure the method call to validate the response using the `responseValidation` method:\n\n```java\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion;\n\nChatCompletion chatCompletion = client.chat().completions().create(\n  params, RequestOptions.builder().responseValidation(true).build()\n);\n```\n\nOr configure the default for all method calls at the client level:\n\n```java\nimport com.dedalus_sdk.api.client.DedalusClient;\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient;\n\nDedalusClient client = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .responseValidation(true)\n    .build();\n```\n\n## FAQ\n\n### Why don\'t you use plain `enum` classes?\n\nJava `enum` classes are not trivially   [forwards compatible](https://www.stainless.com/blog/making-java-enums-forwards-compatible). Using them in   the SDK could cause runtime exceptions if the API is updated to respond with a new enum value.\n\n### Why do you represent fields using `JsonField<T>` instead of just plain `T`?\n\nUsing `JsonField<T>` enables a few features:\n\n- Allowing usage of [undocumented API functionality](#undocumented-api-functionality)\n- Lazily [validating the API response against the expected shape](#response-validation)\n- Representing absent vs explicitly null values\n\n### Why don\'t you use [`data` classes](https://kotlinlang.org/docs/data-classes.html)?\n\nIt is not [backwards compatible to add new fields to a data class](https://kotlinlang.org/docs/api-guidelines-backward-compatibility.html#avoid-using-data-classes-in-your-api)   and we don\'t want to introduce a breaking change every time we add a field to a class.\n\n### Why don\'t you use checked exceptions?\n\nChecked exceptions are widely considered a mistake in the Java programming language. In fact, they were   omitted from Kotlin for this reason.\n\nChecked exceptions:\n\n- Are verbose to handle\n- Encourage error handling at the wrong level of abstraction, where nothing can be done about the error\n- Are tedious to propagate due to the [function coloring problem](https://journal.stuffwithstuff.com/2015/02/01/what-color-is-your-function)\n- Don\'t play well with lambdas (also due to the function coloring problem)\n\n## Semantic versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n2. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/dedalus-labs/dedalus-sdk-java/issues) with questions, bugs, or suggestions.\n',
  },
  {
    language: 'kotlin',
    content:
      '# Dedalus Kotlin API Library\n\n<!-- x-release-please-start-version -->\n[![Maven Central](https://img.shields.io/maven-central/v/com.dedalus_sdk.api/dedalus-kotlin)](https://central.sonatype.com/artifact/com.dedalus_sdk.api/dedalus-kotlin/0.0.1)\n[![javadoc](https://javadoc.io/badge2/com.dedalus_sdk.api/dedalus-kotlin/0.0.1/javadoc.svg)](https://javadoc.io/doc/com.dedalus_sdk.api/dedalus-kotlin/0.0.1)\n<!-- x-release-please-end -->\n\nThe Dedalus Kotlin SDK provides convenient access to the [Dedalus REST API](https://docs.dedaluslabs.ai)   from applications written in Kotlin.\n\n\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Dedalus MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=dedalus-labs-mcp&config=eyJuYW1lIjoiZGVkYWx1cy1sYWJzLW1jcCIsInRyYW5zcG9ydCI6Imh0dHAiLCJ1cmwiOiJodHRwczovL2RlZGFsdXMtc2RrLnN0bG1jcC5jb20iLCJoZWFkZXJzIjp7IngtZGVkYWx1cy1hcGkta2V5IjoiTXkgQVBJIEtleSIsIngtYXBpLWtleSI6Ik15IFggQVBJIEtleSJ9fQ)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dedalus-labs-mcp%22%2C%22type%22%3A%22http%22%2C%22url%22%3A%22https%3A%2F%2Fdedalus-sdk.stlmcp.com%22%2C%22headers%22%3A%7B%22x-dedalus-api-key%22%3A%22My%20API%20Key%22%2C%22x-api-key%22%3A%22My%20X%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n<!-- x-release-please-start-version -->\n\nThe REST API documentation can be found on [docs.dedaluslabs.ai](https://docs.dedaluslabs.ai). KDocs are available on [javadoc.io](https://javadoc.io/doc/com.dedalus_sdk.api/dedalus-kotlin/0.0.1).\n\n<!-- x-release-please-end -->\n\n## Installation\n\n<!-- x-release-please-start-version -->\n\n### Gradle\n\n~~~kotlin\nimplementation("com.dedalus_sdk.api:dedalus-kotlin:0.0.1")\n~~~\n\n### Maven\n\n~~~xml\n<dependency>\n  <groupId>com.dedalus_sdk.api</groupId>\n  <artifactId>dedalus-kotlin</artifactId>\n  <version>0.0.1</version>\n</dependency>\n~~~\n\n<!-- x-release-please-end -->\n\n## Requirements\n\nThis library requires Java 8 or later.\n\n## Usage\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams\n\n// Configures using the `dedalus.apiKey`, `dedalus.xApiKey`, `dedalus.asUrl`, `dedalus.orgId`, `dedalus.provider`, `dedalus.providerKey`, `dedalus.providerModel` and `dedalus.baseUrl` system properties\n// Or configures using the `DEDALUS_API_KEY`, `DEDALUS_X_API_KEY`, `DEDALUS_AS_URL`, `DEDALUS_ORG_ID`, `DEDALUS_PROVIDER`, `DEDALUS_PROVIDER_KEY`, `DEDALUS_PROVIDER_MODEL` and `DEDALUS_BASE_URL` environment variables\nval client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\nval params: ChatCompletionCreateParams = ChatCompletionCreateParams.builder()\n    .model("openai/gpt-5")\n    .build()\nval chatCompletion: ChatCompletion = client.chat().completions().create(params)\n```\n\n## Client configuration\n\nConfigure the client using system properties or environment variables:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\n\n// Configures using the `dedalus.apiKey`, `dedalus.xApiKey`, `dedalus.asUrl`, `dedalus.orgId`, `dedalus.provider`, `dedalus.providerKey`, `dedalus.providerModel` and `dedalus.baseUrl` system properties\n// Or configures using the `DEDALUS_API_KEY`, `DEDALUS_X_API_KEY`, `DEDALUS_AS_URL`, `DEDALUS_ORG_ID`, `DEDALUS_PROVIDER`, `DEDALUS_PROVIDER_KEY`, `DEDALUS_PROVIDER_MODEL` and `DEDALUS_BASE_URL` environment variables\nval client: DedalusClient = DedalusOkHttpClient.fromEnv()\n```\n\nOr manually:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    .apiKey("My API Key")\n    .build()\n```\n\nOr using a combination of the two approaches:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    // Configures using the `dedalus.apiKey`, `dedalus.xApiKey`, `dedalus.asUrl`, `dedalus.orgId`, `dedalus.provider`, `dedalus.providerKey`, `dedalus.providerModel` and `dedalus.baseUrl` system properties\n    // Or configures using the `DEDALUS_API_KEY`, `DEDALUS_X_API_KEY`, `DEDALUS_AS_URL`, `DEDALUS_ORG_ID`, `DEDALUS_PROVIDER`, `DEDALUS_PROVIDER_KEY`, `DEDALUS_PROVIDER_MODEL` and `DEDALUS_BASE_URL` environment variables\n    .fromEnv()\n    .apiKey("My API Key")\n    .build()\n```\n\nSee this table for the available options:\n\n| Setter          | System property         | Environment variable     | Required | Default value                  |\n| --------------- | ----------------------- | ------------------------ | -------- | ------------------------------ |\n| `apiKey`        | `dedalus.apiKey`        | `DEDALUS_API_KEY`        | false    | -                              |\n| `xApiKey`       | `dedalus.xApiKey`       | `DEDALUS_X_API_KEY`      | false    | -                              |\n| `asBaseUrl`     | `dedalus.asUrl`         | `DEDALUS_AS_URL`         | false    | `"https://as.dedaluslabs.ai"`  |\n| `dedalusOrgId`  | `dedalus.orgId`         | `DEDALUS_ORG_ID`         | false    | -                              |\n| `provider`      | `dedalus.provider`      | `DEDALUS_PROVIDER`       | false    | -                              |\n| `providerKey`   | `dedalus.providerKey`   | `DEDALUS_PROVIDER_KEY`   | false    | -                              |\n| `providerModel` | `dedalus.providerModel` | `DEDALUS_PROVIDER_MODEL` | false    | -                              |\n| `baseUrl`       | `dedalus.baseUrl`       | `DEDALUS_BASE_URL`       | true     | `"https://api.dedaluslabs.ai"` |\n\nSystem properties take precedence over environment variables.\n\n> [!TIP]\n> Don\'t create more than one client in the same application. Each client has a connection pool and\n> thread pools, which are more efficient to share between requests.\n\n### Modifying configuration\n\nTo temporarily use a modified client configuration, while reusing the same connection and thread       pools, call `withOptions()` on any client or service:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\n\nval clientWithOptions: DedalusClient = client.withOptions {\n    it.baseUrl("https://example.com")\n    it.maxRetries(42)\n}\n```\n\nThe `withOptions()` method does not affect the original client or service.\n\n## Requests and responses\n\nTo send a request to the Dedalus API, build an instance of some `Params` class and pass it to the     corresponding client method. When the response is received, it will be deserialized into an instance of     a Kotlin class.\n\nFor example, `client.chat().completions().create(...)` should be called with an instance of `CompletionCreateParams`, and it     will return an instance of `ChatCompletion`.\n\n## Immutability\n\nEach class in the SDK has an associated   [builder](https://blogs.oracle.com/javamagazine/post/exploring-joshua-blochs-builder-design-pattern-in-java)   or factory method for constructing it.\n\nEach class is [immutable](https://docs.oracle.com/javase/tutorial/essential/concurrency/immutable.html)   once constructed. If the class has an associated builder, then it has a `toBuilder()` method, which can   be used to convert it back to a builder for making a modified copy.\n\nBecause each class is immutable, builder modification will _never_ affect already built class instances.\n\n## Asynchronous execution\n\nThe default client is synchronous. To switch to asynchronous execution, call the `async()` method:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams\n\n// Configures using the `dedalus.apiKey`, `dedalus.xApiKey`, `dedalus.asUrl`, `dedalus.orgId`, `dedalus.provider`, `dedalus.providerKey`, `dedalus.providerModel` and `dedalus.baseUrl` system properties\n// Or configures using the `DEDALUS_API_KEY`, `DEDALUS_X_API_KEY`, `DEDALUS_AS_URL`, `DEDALUS_ORG_ID`, `DEDALUS_PROVIDER`, `DEDALUS_PROVIDER_KEY`, `DEDALUS_PROVIDER_MODEL` and `DEDALUS_BASE_URL` environment variables\nval client: DedalusClient = DedalusOkHttpClient.fromEnv()\n\nval params: ChatCompletionCreateParams = ChatCompletionCreateParams.builder()\n    .model("openai/gpt-5")\n    .build()\nval chatCompletion: ChatCompletion = client.async().chat().completions().create(params)\n```\n\nOr create an asynchronous client from the beginning:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClientAsync\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClientAsync\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams\n\n// Configures using the `dedalus.apiKey`, `dedalus.xApiKey`, `dedalus.asUrl`, `dedalus.orgId`, `dedalus.provider`, `dedalus.providerKey`, `dedalus.providerModel` and `dedalus.baseUrl` system properties\n// Or configures using the `DEDALUS_API_KEY`, `DEDALUS_X_API_KEY`, `DEDALUS_AS_URL`, `DEDALUS_ORG_ID`, `DEDALUS_PROVIDER`, `DEDALUS_PROVIDER_KEY`, `DEDALUS_PROVIDER_MODEL` and `DEDALUS_BASE_URL` environment variables\nval client: DedalusClientAsync = DedalusOkHttpClientAsync.fromEnv()\n\nval params: ChatCompletionCreateParams = ChatCompletionCreateParams.builder()\n    .model("openai/gpt-5")\n    .build()\nval chatCompletion: ChatCompletion = client.chat().completions().create(params)\n```\n\nThe asynchronous client supports the same options as the synchronous one, except most methods are [suspending](https://kotlinlang.org/docs/coroutines-guide.html).\n\n## Streaming\n\nThe SDK defines methods that return response "chunk" streams, where each chunk can be individually     processed as soon as it arrives instead of waiting on the full response. Streaming methods generally     correspond to [SSE](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events) or     [JSONL](https://jsonlines.org) responses.\n\nSome of these methods may have streaming and non-streaming variants, but a streaming method will     always have a `Streaming` suffix in its name, even if it doesn\'t have a non-streaming variant.\n\nThese streaming methods return [`StreamResponse`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/core/http/StreamResponse.kt) for synchronous clients:\n\n```kotlin\nclient.chat().completions().createStreaming(params).use { response ->\n    response.asSequence().forEach { println(it) }\n    println("No more chunks!")\n}\n```\n\n## File uploads\n\nThe SDK defines methods that accept files.\n\nTo upload a file, pass a [`Path`](https://docs.oracle.com/javase/8/docs/api/java/nio/file/Path.html):\n\n```kotlin\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateParams\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateResponse\nimport java.nio.file.Paths\n\nval params: TranscriptionCreateParams = TranscriptionCreateParams.builder()\n    .model("model")\n    .file(Paths.get("/path/to/file"))\n    .build()\nval transcription: TranscriptionCreateResponse = client.audio().transcriptions().create(params)\n```\n\nOr an arbitrary [`InputStream`](https://docs.oracle.com/javase/8/docs/api/java/io/InputStream.html):\n\n```kotlin\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateParams\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateResponse\nimport java.net.URL\n\nval params: TranscriptionCreateParams = TranscriptionCreateParams.builder()\n    .model("model")\n    .file(URL("https://example.com//path/to/file").openStream())\n    .build()\nval transcription: TranscriptionCreateResponse = client.audio().transcriptions().create(params)\n```\n\nOr a `ByteArray`:\n\n```kotlin\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateParams\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateResponse\n\nval params: TranscriptionCreateParams = TranscriptionCreateParams.builder()\n    .model("model")\n    .file("content".toByteArray())\n    .build()\nval transcription: TranscriptionCreateResponse = client.audio().transcriptions().create(params)\n```\n\nNote that when passing a non-`Path` its filename is unknown so it will not be included in the request.     To manually set a filename, pass a [`MultipartField`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/core/Values.kt):\n\n```kotlin\nimport com.dedalus_sdk.api.core.MultipartField\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateParams\nimport com.dedalus_sdk.api.models.audio.transcriptions.TranscriptionCreateResponse\nimport java.io.InputStream\nimport java.net.URL\n\nval params: TranscriptionCreateParams = TranscriptionCreateParams.builder()\n    .model("model")\n    .file(MultipartField.builder<InputStream>()\n        .value(URL("https://example.com//path/to/file").openStream())\n        .filename("/path/to/file")\n        .build())\n    .build()\nval transcription: TranscriptionCreateResponse = client.audio().transcriptions().create(params)\n```\n\n## Binary responses\n\nThe SDK defines methods that return binary responses, which are used for API responses that shouldn\'t     necessarily be parsed, like non-JSON data.\n\nThese methods return [`HttpResponse`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/core/http/HttpResponse.kt):\n\n```kotlin\nimport com.dedalus_sdk.api.core.http.HttpResponse\nimport com.dedalus_sdk.api.models.audio.speech.SpeechCreateParams\n\nval params: SpeechCreateParams = SpeechCreateParams.builder()\n    .input("input")\n    .model(SpeechCreateParams.Model.TTS_1)\n    .voice(SpeechCreateParams.Voice.UnionMember1.ALLOY)\n    .build()\nval speech: HttpResponse = client.audio().speech().create(params)\n```\n\nTo save the response content to a file, use the     [`Files.copy(...)`](https://docs.oracle.com/javase/8/docs/api/java/nio/file/Files.html#copy-java.io.InputStream-java.nio.file.Path-java.nio.file.CopyOption...-)     method:\n\n```kotlin\nimport java.nio.file.Files\nimport java.nio.file.Paths\nimport java.nio.file.StandardCopyOption\n\nclient.audio().speech().create(params).use {\n    Files.copy(\n        it.body(),\n        Paths.get(path),\n        StandardCopyOption.REPLACE_EXISTING\n    )\n}\n```\n\nOr transfer the response content to any     [`OutputStream`](https://docs.oracle.com/javase/8/docs/api/java/io/OutputStream.html):\n\n```kotlin\nimport java.nio.file.Files\nimport java.nio.file.Paths\n\nclient.audio().speech().create(params).use {\n    it.body().transferTo(Files.newOutputStream(Paths.get(path)))\n}\n```\n\n## Raw responses\n\nThe SDK defines methods that deserialize responses into instances of Kotlin classes.       However, these methods don\'t provide access to the response headers, status code, or the raw response       body.\n\nTo access this data, prefix any HTTP method call on a client or service with `withRawResponse()`:\n\n```kotlin\nimport com.dedalus_sdk.api.core.http.Headers\nimport com.dedalus_sdk.api.core.http.HttpResponseFor\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams\n\nval params: ChatCompletionCreateParams = ChatCompletionCreateParams.builder()\n    .model("openai/gpt-5")\n    .build()\nval chatCompletion: HttpResponseFor<ChatCompletion> = client.chat().completions().withRawResponse().create(params)\n\nval statusCode: Int = chatCompletion.statusCode()\nval headers: Headers = chatCompletion.headers()\n```\n\nYou can still deserialize the response into an instance of a Kotlin class if needed:\n\n```kotlin\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion\n\nval parsedChatCompletion: ChatCompletion = chatCompletion.parse()\n```\n\n## Error handling\n\nThe SDK throws custom unchecked exception types:\n\n- [`DedalusServiceException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusServiceException.kt): Base class for HTTP errors. See this table for which exception       subclass is thrown for each HTTP status code:\n\n  | Status | Exception                                          |\n  | ------ | -------------------------------------------------- |\n  | 400    | [`BadRequestException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/BadRequestException.kt)           |\n  | 401    | [`UnauthorizedException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/UnauthorizedException.kt)         |\n  | 403    | [`PermissionDeniedException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/PermissionDeniedException.kt)     |\n  | 404    | [`NotFoundException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/NotFoundException.kt)             |\n  | 422    | [`UnprocessableEntityException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/UnprocessableEntityException.kt)  |\n  | 429    | [`RateLimitException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/RateLimitException.kt)            |\n  | 5xx    | [`InternalServerException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/InternalServerException.kt)       |\n  | others | [`UnexpectedStatusCodeException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/UnexpectedStatusCodeException.kt) |\n\n  [`SseException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/SseException.kt) is thrown for errors encountered during             [SSE streaming](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events) after a             successful initial HTTP response.\n\n- [`DedalusIoException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusIoException.kt): I/O networking errors.\n\n- [`DedalusRetryableException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusRetryableException.kt): Generic error indicating a failure that could be retried by the client.\n\n- [`DedalusInvalidDataException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusInvalidDataException.kt): Failure to interpret successfully parsed data. For example,       when accessing a property that\'s supposed to be required, but the API unexpectedly omitted it from the       response.\n\n- [`DedalusException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusException.kt): Base class for all exceptions. Most errors will result in one of the       previously mentioned ones, but completely generic errors may be thrown using the base class.\n\n\n\n## Logging\n\nEnable logging by setting the `DEDALUS_LOG` environment variable to   `info`:\n\n```sh\nexport DEDALUS_LOG=info\n```\n\nOr to `debug` for more verbose logging:\n\n```sh\nexport DEDALUS_LOG=debug\n```\n\nOr configure the client manually using the `logLevel` method:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.core.LogLevel\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .logLevel(LogLevel.INFO)\n    .build()\n```\n\n## ProGuard and R8\n\nAlthough the SDK uses reflection, it is still usable with     [ProGuard](https://github.com/Guardsquare/proguard) and     [R8](https://developer.android.com/topic/performance/app-optimization/enable-app-optimization) because     `dedalus-kotlin-core` is published with a     [configuration file](dedalus-kotlin-core/src/main/resources/META-INF/proguard/dedalus-kotlin-core.pro) containing     [keep rules](https://www.guardsquare.com/manual/configuration/usage).\n\nProGuard and R8 should automatically detect and use the published rules, but you can also manually copy     the keep rules if necessary.\n\n\n\n\n\n## Jackson\n\nThe SDK depends on [Jackson](https://github.com/FasterXML/jackson) for JSON     serialization/deserialization. It is compatible with version 2.13.4 or higher,     but depends on version 2.18.2 by default.\n\nThe SDK throws an exception if it detects an incompatible Jackson version at runtime (e.g. if the     default version was overridden in your Maven or Gradle config).\n\nIf the SDK threw an exception, but you\'re _certain_ the version is compatible, then disable the version     check using the `checkJacksonVersionCompatibility` on [`DedalusOkHttpClient`](dedalus-kotlin-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClient.kt) or     [`DedalusOkHttpClientAsync`](dedalus-kotlin-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClientAsync.kt).\n\n> [!CAUTION]\n> We make no guarantee that the SDK works correctly when the Jackson version check is disabled.\n\nAlso note that there are bugs in older Jackson versions that can affect the SDK. We don\'t work around all     Jackson bugs ([example](https://github.com/FasterXML/jackson-databind/issues/3240)) and expect users to     upgrade Jackson for those instead.\n\n## Network options\n\n### Retries\n\nThe SDK automatically retries 2 times by default, with a short exponential backoff between requests.\n\nOnly the following error types are retried:\n- Connection errors (for example, due to a network connectivity problem)\n- 408 Request Timeout\n- 409 Conflict\n- 429 Rate Limit\n- 5xx Internal\n\nThe API may also explicitly instruct the SDK to retry or not retry a request.\n\nTo set a custom number of retries, configure the client using the `maxRetries` method:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .maxRetries(4)\n    .build()\n```\n\n### Timeouts\n\nRequests time out after 1 minute by default.\n\nTo set a custom timeout, configure the method call using the `timeout` method:\n\n```kotlin\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion\n\nval chatCompletion: ChatCompletion = client.chat().completions().create(\n  params, RequestOptions.builder().timeout(Duration.ofSeconds(30)).build()\n)\n```\n\nOr configure the default for all method calls at the client level:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport java.time.Duration\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .timeout(Duration.ofSeconds(30))\n    .build()\n```\n\n### Proxies\n\nTo route requests through a proxy, configure the client using the `proxy` method:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport java.net.InetSocketAddress\nimport java.net.Proxy\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .proxy(Proxy(\n      Proxy.Type.HTTP, InetSocketAddress(\n        "https://example.com", 8080\n      )\n    ))\n    .build()\n```\n\nIf the proxy responds with `407 Proxy Authentication Required`, supply credentials by also   configuring `proxyAuthenticator`:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport com.dedalus_sdk.api.core.http.ProxyAuthenticator\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .proxy(...)\n    // Or a custom implementation of `ProxyAuthenticator`.\n    .proxyAuthenticator(ProxyAuthenticator.basic("username", "password"))\n    .build()\n```\n\n### Connection pooling\n\nTo customize the underlying OkHttp connection pool, configure the client using the   `maxIdleConnections` and `keepAliveDuration` methods:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\nimport java.time.Duration\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    .fromEnv()\n    // If `maxIdleConnections` is set, then `keepAliveDuration` must be set, and vice versa.\n    .maxIdleConnections(10)\n    .keepAliveDuration(Duration.ofMinutes(2))\n    .build()\n```\n\nIf both options are unset, OkHttp\'s default connection pool settings are used.\n\n### HTTPS\n\n> [!NOTE]\n> Most applications should not call these methods, and instead use the system defaults. The defaults include\n> special optimizations that can be lost if the implementations are modified.\n\nTo configure how HTTPS connections are secured, configure the client using the `sslSocketFactory`,   `trustManager`, and `hostnameVerifier` methods:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    .fromEnv()\n    // If `sslSocketFactory` is set, then `trustManager` must be set, and vice versa.\n    .sslSocketFactory(yourSSLSocketFactory)\n    .trustManager(yourTrustManager)\n    .hostnameVerifier(yourHostnameVerifier)\n    .build()\n```\n\n### Environments\n\nThe SDK sends requests to the production by default. To send requests to a different     environment, configure the client like so:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .development()\n    .build()\n```\n\n### Custom HTTP client\n\nThe SDK consists of three artifacts:\n- `dedalus-kotlin-core`\n  - Contains core SDK logic\n  - Does not depend on [OkHttp](https://square.github.io/okhttp)\n  - Exposes [`DedalusClient`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClient.kt), [`DedalusClientAsync`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientAsync.kt),             [`DedalusClientImpl`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientImpl.kt), and [`DedalusClientAsyncImpl`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientAsyncImpl.kt), all of which can             work with any HTTP client\n- `dedalus-kotlin-client-okhttp`\n  - Depends on [OkHttp](https://square.github.io/okhttp)\n  - Exposes [`DedalusOkHttpClient`](dedalus-kotlin-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClient.kt) and [`DedalusOkHttpClientAsync`](dedalus-kotlin-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClientAsync.kt), which             provide a way to construct [`DedalusClientImpl`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientImpl.kt) and             [`DedalusClientAsyncImpl`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientAsyncImpl.kt), respectively, using OkHttp\n- `dedalus-kotlin`\n  - Depends on and exposes the APIs of both `dedalus-kotlin-core` and `dedalus-kotlin-client-okhttp`\n  - Does not have its own logic\n\nThis structure allows replacing the SDK\'s default HTTP client without pulling in unnecessary dependencies.\n\n#### Customized [`OkHttpClient`](https://square.github.io/okhttp/3.x/okhttp/okhttp3/OkHttpClient.html)\n\n> [!TIP]\n> Try the available [network options](#network-options) before replacing the default client.\n\nTo use a customized `OkHttpClient`:\n\n1. Replace your [`dedalus-kotlin` dependency](#installation) with `dedalus-kotlin-core`\n2. Copy `dedalus-kotlin-client-okhttp`\'s [`OkHttpClient`](dedalus-kotlin-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/OkHttpClient.kt) class into your code and        customize it\n3. Construct [`DedalusClientImpl`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientImpl.kt) or [`DedalusClientAsyncImpl`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientAsyncImpl.kt), similarly to        [`DedalusOkHttpClient`](dedalus-kotlin-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClient.kt) or [`DedalusOkHttpClientAsync`](dedalus-kotlin-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClientAsync.kt), using your        customized client\n\n### Completely custom HTTP client\n\nTo use a completely custom HTTP client:\n\n1. Replace your [`dedalus-kotlin` dependency](#installation) with `dedalus-kotlin-core`\n2. Write a class that implements the [`HttpClient`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/core/http/HttpClient.kt) interface\n3. Construct [`DedalusClientImpl`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientImpl.kt) or [`DedalusClientAsyncImpl`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/client/DedalusClientAsyncImpl.kt), similarly to        [`DedalusOkHttpClient`](dedalus-kotlin-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClient.kt) or [`DedalusOkHttpClientAsync`](dedalus-kotlin-client-okhttp/src/main/kotlin/com/dedalus_sdk/api/client/okhttp/DedalusOkHttpClientAsync.kt), using your new        client class\n\n## Undocumented API functionality\n\nThe SDK is typed for convenient usage of the documented API. However, it also supports working with undocumented or not yet supported parts of the API.\n\n### Parameters\n\nTo set undocumented parameters, call the `putAdditionalHeader`, `putAdditionalQueryParam`, or       `putAdditionalBodyProperty` methods on any `Params` class:\n\n```kotlin\nimport com.dedalus_sdk.api.core.JsonValue\nimport com.dedalus_sdk.api.models.chat.completions.CompletionCreateParams\n\nval params: CompletionCreateParams = CompletionCreateParams.builder()\n    .putAdditionalHeader("Secret-Header", "42")\n    .putAdditionalQueryParam("secret_query_param", "42")\n    .putAdditionalBodyProperty("secretProperty", JsonValue.from("42"))\n    .build()\n```\n\nThese can be accessed on the built object later using the `_additionalHeaders()`,       `_additionalQueryParams()`, and `_additionalBodyProperties()` methods.\n\nTo set a documented parameter or property to an undocumented or not yet supported _value_, pass a       [`JsonValue`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/core/Values.kt) object to its setter:\n\n```kotlin\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams\nimport com.dedalus_sdk.api.models.chat.completions.CompletionCreateParams\n\nval params: CompletionCreateParams = CompletionCreateParams.builder()\n    .chatCompletionCreateParams(ChatCompletionCreateParams.builder()\n        .model("openai/gpt-5")\n        .build())\n    .build()\n```\n\nThe most straightforward way to create a [`JsonValue`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/core/Values.kt) is using its       `from(...)` method:\n\n```kotlin\nimport com.dedalus_sdk.api.core.JsonValue\n\n// Create primitive JSON values\nval nullValue: JsonValue = JsonValue.from(null)\nval booleanValue: JsonValue = JsonValue.from(true)\nval numberValue: JsonValue = JsonValue.from(42)\nval stringValue: JsonValue = JsonValue.from("Hello World!")\n\n// Create a JSON array value equivalent to `["Hello", "World"]`\nval arrayValue: JsonValue = JsonValue.from(listOf(\n  "Hello", "World"\n))\n\n// Create a JSON object value equivalent to `{ "a": 1, "b": 2 }`\nval objectValue: JsonValue = JsonValue.from(mapOf(\n  "a" to 1, "b" to 2\n))\n\n// Create an arbitrarily nested JSON equivalent to:\n// {\n//   "a": [1, 2],\n//   "b": [3, 4]\n// }\nval complexValue: JsonValue = JsonValue.from(mapOf(\n  "a" to listOf(\n    1, 2\n  ), "b" to listOf(\n    3, 4\n  )\n))\n```\n\nNormally a `Builder` class\'s `build` method will throw         [`IllegalStateException`](https://docs.oracle.com/javase/8/docs/api/java/lang/IllegalStateException.html)         if any required parameter or property is unset.\n\nTo forcibly omit a required parameter or property, pass [`JsonMissing`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/core/Values.kt):\n\n```kotlin\nimport com.dedalus_sdk.api.core.JsonMissing\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletionCreateParams\nimport com.dedalus_sdk.api.models.chat.completions.CompletionCreateParams\n\nval params: CompletionCreateParams = CompletionCreateParams.builder()\n    .chatCompletionCreateParams(ChatCompletionCreateParams.builder()\n        .model("openai/gpt-5")\n        .build())\n    .model(JsonMissing.of())\n    .build()\n```\n\n### Response properties\n\nTo access undocumented response properties, call the `_additionalProperties()` method:\n\n```kotlin\nimport com.dedalus_sdk.api.core.JsonBoolean\nimport com.dedalus_sdk.api.core.JsonNull\nimport com.dedalus_sdk.api.core.JsonNumber\nimport com.dedalus_sdk.api.core.JsonValue\n\nval additionalProperties: Map<String, JsonValue> = client.chat().completions().create(params)._additionalProperties()\nval secretPropertyValue: JsonValue = additionalProperties.get("secretProperty")\n\nval result = when (secretPropertyValue) {\n    is JsonNull -> "It\'s null!"\n    is JsonBoolean -> "It\'s a boolean!"\n    is JsonNumber -> "It\'s a number!"\n    // Other types include `JsonMissing`, `JsonString`, `JsonArray`, and `JsonObject`\n    else -> "It\'s something else!"\n}\n```\n\nTo access a property\'s raw JSON value, which may be undocumented, call its `_` prefixed method:\n\n```kotlin\nimport com.dedalus_sdk.api.core.JsonField\n\nval field: JsonField<Any> = client.chat().completions().create(params)._field()\n\nif (field.isMissing()) {\n  // The property is absent from the JSON response\n} else if (field.isNull()) {\n  // The property was set to literal null\n} else {\n  // Check if value was provided as a string\n  // Other methods include `asNumber()`, `asBoolean()`, etc.\n  val jsonString: String? = field.asString();\n\n  // Try to deserialize into a custom type\n  val myObject: MyClass = field.asUnknown()!!.convert(MyClass::class.java)\n}\n```\n\n### Response validation\n\nIn rare cases, the API may return a response that doesn\'t match the expected type. For example, the SDK     may expect a property to contain a `String`, but the API could return something else.\n\nBy default, the SDK will not throw an exception in this case. It will throw     [`DedalusInvalidDataException`](dedalus-kotlin-core/src/main/kotlin/com/dedalus_sdk/api/errors/DedalusInvalidDataException.kt) only if you directly access the property.\n\nValidating the response is _not_ forwards compatible with new types from the API for existing fields.\n\nIf you would still prefer to check that the response is completely well-typed upfront, then either call     `validate()`:\n\n```kotlin\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion\n\nval chatCompletion: ChatCompletion = client.chat().completions().create(params).validate()\n```\n\nOr configure the method call to validate the response using the `responseValidation` method:\n\n```kotlin\nimport com.dedalus_sdk.api.models.chat.completions.ChatCompletion\n\nval chatCompletion: ChatCompletion = client.chat().completions().create(\n  params, RequestOptions.builder().responseValidation(true).build()\n)\n```\n\nOr configure the default for all method calls at the client level:\n\n```kotlin\nimport com.dedalus_sdk.api.client.DedalusClient\nimport com.dedalus_sdk.api.client.okhttp.DedalusOkHttpClient\n\nval client: DedalusClient = DedalusOkHttpClient.builder()\n    .fromEnv()\n    .responseValidation(true)\n    .build()\n```\n\n## FAQ\n\n### Why don\'t you use plain `enum` classes?\n\nKotlin `enum` classes are not trivially   [forwards compatible](https://www.stainless.com/blog/making-java-enums-forwards-compatible). Using them in   the SDK could cause runtime exceptions if the API is updated to respond with a new enum value.\n\n### Why do you represent fields using `JsonField<T>` instead of just plain `T`?\n\nUsing `JsonField<T>` enables a few features:\n\n- Allowing usage of [undocumented API functionality](#undocumented-api-functionality)\n- Lazily [validating the API response against the expected shape](#response-validation)\n- Representing absent vs explicitly null values\n\n### Why don\'t you use [`data` classes](https://kotlinlang.org/docs/data-classes.html)?\n\nIt is not [backwards compatible to add new fields to a data class](https://kotlinlang.org/docs/api-guidelines-backward-compatibility.html#avoid-using-data-classes-in-your-api)   and we don\'t want to introduce a breaking change every time we add a field to a class.\n\n### Why don\'t you use checked exceptions?\n\nChecked exceptions are widely considered a mistake in the Java programming language. In fact, they were   omitted from Kotlin for this reason.\n\nChecked exceptions:\n\n- Are verbose to handle\n- Encourage error handling at the wrong level of abstraction, where nothing can be done about the error\n- Are tedious to propagate due to the [function coloring problem](https://journal.stuffwithstuff.com/2015/02/01/what-color-is-your-function)\n- Don\'t play well with lambdas (also due to the function coloring problem)\n\n## Semantic versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n2. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/dedalus-labs/dedalus-sdk-kotlin/issues) with questions, bugs, or suggestions.\n',
  },
  {
    language: 'php',
    content:
      "# Dedalus PHP API Library\n\nThe Dedalus PHP library provides convenient access to the Dedalus REST API from any PHP 8.1.0+ application.\n\n## Installation\n\nTo use this package, install via Composer by adding the following to your application's `composer.json`:\n\n```json\n{\n  \"repositories\": [\n    {\n      \"type\": \"vcs\",\n      \"url\": \"git@github.com:stainless-sdks/dedalus-sdk-php.git\"\n    }\n  ],\n  \"require\": {\n    \"org-placeholder/dedalus-sdk\": \"dev-main\"\n  }\n}\n```\n\n## Usage\n\n```php\n<?php\n\n$client = new Client(\n  apiKey: getenv('DEDALUS_API_KEY') ?: 'My API Key', environment: 'development'\n);\n\n$chatCompletion = $client->chat->completions->create(\n  model: 'openai/gpt-5-nano',\n  messages: [\n    [\n      'role' => 'system',\n      'content' => 'You are Stephen Dedalus. Respond in morose Joycean malaise.',\n    ],\n    ['role' => 'user', 'content' => 'Hello, how are you today?'],\n  ],\n);\n\nvar_dump($chatCompletion->id);\n```",
  },
  {
    language: 'python',
    content:
      '# Dedalus Python API library\n\n<!-- prettier-ignore -->\n[![PyPI version](https://img.shields.io/pypi/v/dedalus_labs.svg?label=pypi%20(stable))](https://pypi.org/project/dedalus_labs/)\n\nThe Dedalus Python library provides convenient access to the Dedalus REST API from any Python 3.9+\napplication. The library includes type definitions for all request params and response fields,\nand offers both synchronous and asynchronous clients powered by [httpx](https://github.com/encode/httpx).\n\n\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Dedalus MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=dedalus-labs-mcp&config=eyJuYW1lIjoiZGVkYWx1cy1sYWJzLW1jcCIsInRyYW5zcG9ydCI6Imh0dHAiLCJ1cmwiOiJodHRwczovL2RlZGFsdXMtc2RrLnN0bG1jcC5jb20iLCJoZWFkZXJzIjp7IngtZGVkYWx1cy1hcGkta2V5IjoiTXkgQVBJIEtleSIsIngtYXBpLWtleSI6Ik15IFggQVBJIEtleSJ9fQ)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dedalus-labs-mcp%22%2C%22type%22%3A%22http%22%2C%22url%22%3A%22https%3A%2F%2Fdedalus-sdk.stlmcp.com%22%2C%22headers%22%3A%7B%22x-dedalus-api-key%22%3A%22My%20API%20Key%22%2C%22x-api-key%22%3A%22My%20X%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n## Documentation\n\nThe REST API documentation can be found on [docs.dedaluslabs.ai](https://docs.dedaluslabs.ai). The full API of this library can be found in [api.md](api.md).\n\n## Installation\n\n```sh\n# install from PyPI\npip install dedalus_labs\n```\n\n## Usage\n\nThe full API of this library can be found in [api.md](api.md).\n\n```python\nimport os\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n    # defaults to "production".\n    environment="development",\n)\n\nchat_completion = client.chat.completions.create(\n    model="openai/gpt-5-nano",\n    messages=[{\n        "role": "system",\n        "content": "You are Stephen Dedalus. Respond in morose Joycean malaise.",\n    }, {\n        "role": "user",\n        "content": "Hello, how are you today?",\n    }],\n)\nprint(chat_completion.id)\n```\n\nWhile you can provide an `api_key` keyword argument,\nwe recommend using [python-dotenv](https://pypi.org/project/python-dotenv/)\nto add `DEDALUS_API_KEY="My API Key"` to your `.env` file\nso that your API Key is not stored in source control.\n\n## Async usage\n\nSimply import `AsyncDedalus` instead of `Dedalus` and use `await` with each API call:\n\n```python\nimport os\nimport asyncio\nfrom dedalus_labs import AsyncDedalus\n\nclient = AsyncDedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n    # defaults to "production".\n    environment="development",\n)\n\nasync def main() -> None:\n  chat_completion = await client.chat.completions.create(\n      model="openai/gpt-5-nano",\n      messages=[{\n          "role": "system",\n          "content": "You are Stephen Dedalus. Respond in morose Joycean malaise.",\n      }, {\n          "role": "user",\n          "content": "Hello, how are you today?",\n      }],\n  )\n  print(chat_completion.id)\n\nasyncio.run(main())\n```\n\nFunctionality between the synchronous and asynchronous clients is otherwise identical.\n\n### With aiohttp\n\nBy default, the async client uses `httpx` for HTTP requests. However, for improved concurrency performance you may also use `aiohttp` as the HTTP backend.\n\nYou can enable this by installing `aiohttp`:\n\n```sh\n# install from PyPI\npip install dedalus_labs[aiohttp]\n```\n\nThen you can enable it by instantiating the client with `http_client=DefaultAioHttpClient()`:\n\n```python\nimport os\nimport asyncio\nfrom dedalus_labs import DefaultAioHttpClient\nfrom dedalus_labs import AsyncDedalus\n\nasync def main() -> None:\n  async with AsyncDedalus(\n    api_key=os.environ.get("DEDALUS_API_KEY"),  # This is the default and can be omitted\n    http_client=DefaultAioHttpClient(),\n) as client:\n    chat_completion = await client.chat.completions.create(\n        model="openai/gpt-5-nano",\n        messages=[{\n            "role": "system",\n            "content": "You are Stephen Dedalus. Respond in morose Joycean malaise.",\n        }, {\n            "role": "user",\n            "content": "Hello, how are you today?",\n        }],\n    )\n    print(chat_completion.id)\n\nasyncio.run(main())\n```\n\n## Streaming responses\n\nWe provide support for streaming responses using Server Side Events (SSE).\n\n```python\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus()\n\nstream = client.chat.completions.create(\n    model="openai/gpt-5-nano",\n    stream=True,\n    messages=[{\n        "role": "system",\n        "content": "You are Stephen Dedalus. Respond in morose Joycean malaise.",\n    }, {\n        "role": "user",\n        "content": "What do you think of artificial intelligence?",\n    }],\n)\nfor chat_completion in stream:\n  print(chat_completion.id)\n```\n\nThe async client uses the exact same interface.\n\n```python\nfrom dedalus_labs import AsyncDedalus\n\nclient = AsyncDedalus()\n\nstream = await client.chat.completions.create(\n    model="openai/gpt-5-nano",\n    stream=True,\n    messages=[{\n        "role": "system",\n        "content": "You are Stephen Dedalus. Respond in morose Joycean malaise.",\n    }, {\n        "role": "user",\n        "content": "What do you think of artificial intelligence?",\n    }],\n)\nasync for chat_completion in stream:\n  print(chat_completion.id)\n```\n\n## Using types\n\nNested request parameters are [TypedDicts](https://docs.python.org/3/library/typing.html#typing.TypedDict). Responses are [Pydantic models](https://docs.pydantic.dev) which also provide helper methods for things like:\n\n- Serializing back into JSON, `model.to_json()`\n- Converting to a dictionary, `model.to_dict()`\n\nTyped requests and responses provide autocomplete and documentation within your editor. If you would like to see type errors in VS Code to help catch bugs earlier, set `python.analysis.typeCheckingMode` to `basic`.\n\n\n\n## Nested params\n\nNested parameters are dictionaries, typed using `TypedDict`, for example:\n\n```python\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus()\n\nchat_completion = client.chat.completions.create(\n    model="openai/gpt-5",\n    audio={\n        "format": "mp3",\n        "voice": "alloy",\n    },\n)\nprint(chat_completion.audio)\n```\n\n## File uploads\n\nRequest parameters that correspond to file uploads can be passed as `bytes`, or a [`PathLike`](https://docs.python.org/3/library/os.html#os.PathLike) instance or a tuple of `(filename, contents, media type)`.\n\n```python\nfrom pathlib import Path\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus()\n\nclient.audio.transcriptions.create(\n    file=Path("/path/to/file"),\n    model="model",\n)\n```\n\nThe async client uses the exact same interface. If you pass a [`PathLike`](https://docs.python.org/3/library/os.html#os.PathLike) instance, the file contents will be read asynchronously automatically.\n\n## Handling errors\n\nWhen the library is unable to connect to the API (for example, due to network connection problems or a timeout), a subclass of `dedalus_labs.APIConnectionError` is raised.\n\nWhen the API returns a non-success status code (that is, 4xx or 5xx\nresponse), a subclass of `dedalus_labs.APIStatusError` is raised, containing `status_code` and `response` properties.\n\nAll errors inherit from `dedalus_labs.APIError`.\n\n```python\nimport dedalus_labs\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus()\n\ntry:\n    client.chat.completions.create(\n        model="openai/gpt-5-nano",\n        messages=[{\n            "role": "system",\n            "content": "You are Stephen Dedalus. Respond in morose Joycean malaise.",\n        }, {\n            "role": "user",\n            "content": "Hello, how are you today?",\n        }],\n    )\nexcept dedalus_labs.APIConnectionError as e:\n    print("The server could not be reached")\n    print(e.__cause__) # an underlying Exception, likely raised within httpx.\nexcept dedalus_labs.RateLimitError as e:\n    print("A 429 status code was received; we should back off a bit.")\nexcept dedalus_labs.APIStatusError as e:\n    print("Another non-200-range status code was received")\n    print(e.status_code)\n    print(e.response)\n```\n\nError codes are as follows:\n\n| Status Code | Error Type                 |\n| ----------- | -------------------------- |\n| 400         | `BadRequestError`          |\n| 401         | `AuthenticationError`      |\n| 403         | `PermissionDeniedError`    |\n| 404         | `NotFoundError`            |\n| 422         | `UnprocessableEntityError` |\n| 429         | `RateLimitError`           |\n| >=500       | `InternalServerError`      |\n| N/A         | `APIConnectionError`       |\n\n### Retries\n\nCertain errors are automatically retried 2 times by default, with a short exponential backoff.\nConnection errors (for example, due to a network connectivity problem), 408 Request Timeout, 409 Conflict,\n429 Rate Limit, and >=500 Internal errors are all retried by default.\n\nYou can use the `max_retries` option to configure or disable retry settings:\n\n```python\nfrom dedalus_labs import Dedalus\n\n# Configure the default for all requests:\nclient = Dedalus(\n    # default is 2\n    max_retries=0,\n)\n\n# Or, configure per-request:\nclient.with_options(max_retries = 5).chat.completions.create(\n    model="openai/gpt-5-nano",\n    messages=[{\n        "role": "system",\n        "content": "You are Stephen Dedalus. Respond in morose Joycean malaise.",\n    }, {\n        "role": "user",\n        "content": "Hello, how are you today?",\n    }],\n)\n```\n\n### Timeouts\n\nBy default requests time out after 1 minute. You can configure this with a `timeout` option,\nwhich accepts a float or an [`httpx.Timeout`](https://www.python-httpx.org/advanced/timeouts/#fine-tuning-the-configuration) object:\n\n```python\nfrom dedalus_labs import Dedalus\n\n# Configure the default for all requests:\nclient = Dedalus(\n    # 20 seconds (default is 1 minute)\n    timeout=20.0,\n)\n\n# More granular control:\nclient = Dedalus(\n    timeout=httpx.Timeout(60.0, read=5.0, write=10.0, connect=2.0),\n)\n\n# Override per-request:\nclient.with_options(timeout = 5.0).chat.completions.create(\n    model="openai/gpt-5-nano",\n    messages=[{\n        "role": "system",\n        "content": "You are Stephen Dedalus. Respond in morose Joycean malaise.",\n    }, {\n        "role": "user",\n        "content": "Hello, how are you today?",\n    }],\n)\n```\n\nOn timeout, an `APITimeoutError` is thrown.\n\nNote that requests that time out are [retried twice by default](#retries).\n\n## Default Headers\n\nWe automatically send the following headers with all requests.\n\n| Header          | Value         |\n| --------------- | ------------- |\n| `User-Agent`    | `Dedalus-SDK` |\n| `X-SDK-Version` | `1.0.0`       |\n\nIf you need to, you can override these headers by setting default headers per-request or on the client object.\n\n```python\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus(\n    default_headers={\n        "User-Agent": "My-Custom-Value"\n    },\n)\n```\n\n## Advanced\n\n### Logging\n\nWe use the standard library [`logging`](https://docs.python.org/3/library/logging.html) module.\n\nYou can enable logging by setting the environment variable `DEDALUS_LOG` to `info`.\n\n```shell\n$ export DEDALUS_LOG=info\n```\n\nOr to `debug` for more verbose logging.\n\n### How to tell whether `None` means `null` or missing\n\nIn an API response, a field may be explicitly `null`, or missing entirely; in either case, its value is `None` in this library. You can differentiate the two cases with `.model_fields_set`:\n\n```py\nif response.my_field is None:\n  if \'my_field\' not in response.model_fields_set:\n    print(\'Got json like {}, without a "my_field" key present at all.\')\n  else:\n    print(\'Got json like {"my_field": null}.\')\n```\n\n### Accessing raw response data (e.g. headers)\n\nThe "raw" Response object can be accessed by prefixing `.with_raw_response.` to any HTTP method call, e.g.,\n\n```py\nfrom dedalus_labs import Dedalus\n\nclient = Dedalus()\nresponse = client.chat.completions.with_raw_response.create(\n    model="openai/gpt-5-nano",\n    messages=[{\n        "role": "system",\n        "content": "You are Stephen Dedalus. Respond in morose Joycean malaise.",\n    }, {\n        "role": "user",\n        "content": "Hello, how are you today?",\n    }],\n)\nprint(response.headers.get(\'X-My-Header\'))\n\ncompletion = response.parse()  # get the object that `chat.completions.create()` would have returned\nprint(completion.id)\n```\n\nThese methods return an [`APIResponse`](https://github.com/dedalus-labs/dedalus-sdk-python/tree/main/src/dedalus_labs/_response.py) object.\n\nThe async client returns an [`AsyncAPIResponse`](https://github.com/dedalus-labs/dedalus-sdk-python/tree/main/src/dedalus_labs/_response.py) with the same structure, the only difference being `await`able methods for reading the response content.\n\n#### `.with_streaming_response`\n\nThe above interface eagerly reads the full response body when you make the request, which may not always be what you want.\n\nTo stream the response body, use `.with_streaming_response` instead, which requires a context manager and only reads the response body once you call `.read()`, `.text()`, `.json()`, `.iter_bytes()`, `.iter_text()`, `.iter_lines()` or `.parse()`. In the async client, these are async methods.\n\n```python\nwith client.chat.completions.with_streaming_response.create(\n    model="openai/gpt-5-nano",\n    messages=[{\n        "role": "system",\n        "content": "You are Stephen Dedalus. Respond in morose Joycean malaise.",\n    }, {\n        "role": "user",\n        "content": "Hello, how are you today?",\n    }],\n) as response :\n    print(response.headers.get(\'X-My-Header\'))\n\n    for line in response.iter_lines():\n      print(line)\n```\n\nThe context manager is required so that the response will reliably be closed.\n\n### Making custom/undocumented requests\n\nThis library is typed for convenient access to the documented API.\n\nIf you need to access undocumented endpoints, params, or response properties, the library can still be used.\n\n#### Undocumented endpoints\n\nTo make requests to undocumented endpoints, you can make requests using `client.get`, `client.post`, and other\nhttp verbs. Options on the client will be respected (such as retries) when making this request.\n\n```py\nimport httpx\n\nresponse = client.post(\n    "/foo",\n    cast_to=httpx.Response,\n    body={"my_param": True},\n)\n\nprint(response.headers.get("x-foo"))\n```\n\n#### Undocumented request params\n\nIf you want to explicitly send an extra param, you can do so with the `extra_query`, `extra_body`, and `extra_headers` request\noptions.\n\n#### Undocumented response properties\n\nTo access undocumented response properties, you can access the extra fields like `response.unknown_prop`. You\ncan also get all the extra fields on the Pydantic model as a dict with\n[`response.model_extra`](https://docs.pydantic.dev/latest/api/base_model/#pydantic.BaseModel.model_extra).\n\n### Configuring the HTTP client\n\nYou can directly override the [httpx client](https://www.python-httpx.org/api/#client) to customize it for your use case, including:\n\n- Support for [proxies](https://www.python-httpx.org/advanced/proxies/)\n- Custom [transports](https://www.python-httpx.org/advanced/transports/)\n- Additional [advanced](https://www.python-httpx.org/advanced/clients/) functionality\n\n```python\nimport httpx\nfrom dedalus_labs import Dedalus, DefaultHttpxClient\n\nclient = Dedalus(\n    # Or use the `DEDALUS_BASE_URL` env var\n    base_url="http://my.test.server.example.com:8083",\n    http_client=DefaultHttpxClient(proxy="http://my.test.proxy.example.com", transport=httpx.HTTPTransport(local_address="0.0.0.0")),\n)\n```\n\nYou can also customize the client on a per-request basis by using `with_options()`:\n\n```python\nclient.with_options(http_client=DefaultHttpxClient(...))\n```\n\n### Managing HTTP resources\n\nBy default the library closes underlying HTTP connections whenever the client is [garbage collected](https://docs.python.org/3/reference/datamodel.html#object.__del__). You can manually close the client using the `.close()` method if desired, or with a context manager that closes when exiting.\n\n```py\nfrom dedalus_labs import Dedalus\n\nwith Dedalus() as client:\n  # make requests here\n  ...\n\n# HTTP client is now closed\n```\n\n## Versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes that only affect static types, without breaking runtime behavior.\n2. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n3. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/dedalus-labs/dedalus-sdk-python/issues) with questions, bugs, or suggestions.\n\n### Determining the installed version\n\nIf you\'ve upgraded to the latest version but aren\'t seeing any new features you were expecting then your python environment is likely still using an older version.\n\nYou can determine the version that is being used at runtime with:\n\n```py\nimport dedalus_labs\nprint(dedalus_labs.__version__)\n```\n\n## Requirements\n\nPython 3.9 or higher.\n\n## Contributing\n\nSee [the contributing documentation](./CONTRIBUTING.md).\n',
  },
  {
    language: 'ruby',
    content:
      '# Dedalus Ruby API library\n\nThe Dedalus Ruby library provides convenient access to the Dedalus REST API from any Ruby 3.2.0+ application. It ships with comprehensive types & docstrings in Yard, RBS, and RBI – [see below](https://github.com/dedalus-labs/dedalus-sdk-ruby#Sorbet) for usage with Sorbet. The standard library\'s `net/http` is used as the HTTP transport, with connection pooling via the `connection_pool` gem.\n\n\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Dedalus MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=dedalus-labs-mcp&config=eyJuYW1lIjoiZGVkYWx1cy1sYWJzLW1jcCIsInRyYW5zcG9ydCI6Imh0dHAiLCJ1cmwiOiJodHRwczovL2RlZGFsdXMtc2RrLnN0bG1jcC5jb20iLCJoZWFkZXJzIjp7IngtZGVkYWx1cy1hcGkta2V5IjoiTXkgQVBJIEtleSIsIngtYXBpLWtleSI6Ik15IFggQVBJIEtleSJ9fQ)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dedalus-labs-mcp%22%2C%22type%22%3A%22http%22%2C%22url%22%3A%22https%3A%2F%2Fdedalus-sdk.stlmcp.com%22%2C%22headers%22%3A%7B%22x-dedalus-api-key%22%3A%22My%20API%20Key%22%2C%22x-api-key%22%3A%22My%20X%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n## Documentation\n\nDocumentation for releases of this gem can be found [on RubyDoc](https://gemdocs.org/gems/dedalus-sdk).\n\nThe REST API documentation can be found on [docs.dedaluslabs.ai](https://docs.dedaluslabs.ai).\n\n## Installation\n\nTo use this gem, install via Bundler by adding the following to your application\'s `Gemfile`:\n\n<!-- x-release-please-start-version -->\n\n```ruby\ngem "dedalus-sdk", "~> 0.0.1"\n```\n\n<!-- x-release-please-end -->\n\n## Usage\n\n```ruby\nrequire "bundler/setup"\nrequire "dedalus_sdk"\n\ndedalus = DedalusSDK::Client.new(\n  api_key: ENV["DEDALUS_API_KEY"], # This is the default and can be omitted\n  environment: "development" # defaults to "production"\n)\n\nchat_completion = dedalus.chat.completions.create(\n  model: "openai/gpt-5-nano",\n  messages: [\n    {role: "system", content: "You are Stephen Dedalus. Respond in morose Joycean malaise."},\n    {role: "user", content: "Hello, how are you today?"}\n  ]\n)\n\nputs(chat_completion.id)\n```\n\n### Streaming\n\nWe provide support for streaming responses using Server-Sent Events (SSE).\n\n```ruby\nstream = dedalus.chat.completions.create_streaming(\n  model: "openai/gpt-5-nano",\n  messages: [\n    {role: "system", content: "You are Stephen Dedalus. Respond in morose Joycean malaise."},\n    {role: "user", content: "What do you think of artificial intelligence?"}\n  ]\n)\n\nstream.each do |completion|\n  puts(completion.id)\nend\n```\n\n\n\n### File uploads\n\nRequest parameters that correspond to file uploads can be passed as raw contents, a [`Pathname`](https://rubyapi.org/3.2/o/pathname) instance, [`StringIO`](https://rubyapi.org/3.2/o/stringio), or more.\n\n```ruby\nrequire "pathname"\n\n# Use `Pathname` to send the filename and/or avoid paging a large file into memory:\ntranscription = dedalus.audio.transcriptions.create(file: Pathname("/path/to/file"))\n\n# Alternatively, pass file contents or a `StringIO` directly:\ntranscription = dedalus.audio.transcriptions.create(file: File.read("/path/to/file"))\n\n# Or, to control the filename and/or content type:\nfile = DedalusSDK::FilePart.new(File.read("/path/to/file"), filename: "/path/to/file", content_type: "…")\ntranscription = dedalus.audio.transcriptions.create(file: file)\n\nputs(transcription)\n```\n\nNote that you can also pass a raw `IO` descriptor, but this disables retries, as the library can\'t be sure if the descriptor is a file or pipe (which cannot be rewound).\n\n### Handling errors\n\nWhen the library is unable to connect to the API, or if the API returns a non-success status code (i.e., 4xx or 5xx response), a subclass of `DedalusSDK::Errors::APIError` will be thrown:\n\n```ruby\nbegin\n  completion = dedalus.chat.completions.create(\n    model: "openai/gpt-5-nano",\n    messages: [\n      {role: "system", content: "You are Stephen Dedalus. Respond in morose Joycean malaise."},\n      {role: "user", content: "Hello, how are you today?"}\n    ]\n  )\nrescue DedalusSDK::Errors::APIConnectionError => e\n  puts("The server could not be reached")\n  puts(e.cause)  # an underlying Exception, likely raised within `net/http`\nrescue DedalusSDK::Errors::RateLimitError => e\n  puts("A 429 status code was received; we should back off a bit.")\nrescue DedalusSDK::Errors::APIStatusError => e\n  puts("Another non-200-range status code was received")\n  puts(e.status)\nend\n```\n\nError codes are as follows:\n\n| Cause            | Error Type                 |\n| ---------------- | -------------------------- |\n| HTTP 400         | `BadRequestError`          |\n| HTTP 401         | `AuthenticationError`      |\n| HTTP 403         | `PermissionDeniedError`    |\n| HTTP 404         | `NotFoundError`            |\n| HTTP 409         | `ConflictError`            |\n| HTTP 422         | `UnprocessableEntityError` |\n| HTTP 429         | `RateLimitError`           |\n| HTTP >= 500      | `InternalServerError`      |\n| Other HTTP error | `APIStatusError`           |\n| Timeout          | `APITimeoutError`          |\n| Network error    | `APIConnectionError`       |\n\n### Retries\n\nCertain errors will be automatically retried 2 times by default, with a short exponential backoff.\n\nConnection errors (for example, due to a network connectivity problem), 408 Request Timeout, 409 Conflict, 429 Rate Limit, >=500 Internal errors, and timeouts will all be retried by default.\n\nYou can use the `max_retries` option to configure or disable this:\n\n```ruby\n# Configure the default for all requests:\ndedalus = DedalusSDK::Client.new(\n  max_retries: 0 # default is 2\n)\n\n# Or, configure per-request:\ndedalus.chat.completions.create(\n  model: "openai/gpt-5-nano",\n  messages: [\n    {role: "system", content: "You are Stephen Dedalus. Respond in morose Joycean malaise."},\n    {role: "user", content: "Hello, how are you today?"}\n  ],\n  request_options: {max_retries: 5}\n)\n```\n\n### Timeouts\n\nBy default, requests will time out after 60 seconds. You can use the timeout option to configure or disable this:\n\n```ruby\n# Configure the default for all requests:\ndedalus = DedalusSDK::Client.new(\n  timeout: nil # default is 60\n)\n\n# Or, configure per-request:\ndedalus.chat.completions.create(\n  model: "openai/gpt-5-nano",\n  messages: [\n    {role: "system", content: "You are Stephen Dedalus. Respond in morose Joycean malaise."},\n    {role: "user", content: "Hello, how are you today?"}\n  ],\n  request_options: {timeout: 5}\n)\n```\n\nOn timeout, `DedalusSDK::Errors::APITimeoutError` is raised.\n\nNote that requests that time out are retried by default.\n\n## Advanced concepts\n\n### BaseModel\n\nAll parameter and response objects inherit from `DedalusSDK::Internal::Type::BaseModel`, which provides several conveniences, including:\n\n1. All fields, including unknown ones, are accessible with `obj[:prop]` syntax, and can be destructured with `obj => {prop: prop}` or pattern-matching syntax.\n\n2. Structural equivalence for equality; if two API calls return the same values, comparing the responses with == will return true.\n\n3. Both instances and the classes themselves can be pretty-printed.\n\n4. Helpers such as `#to_h`, `#deep_to_h`, `#to_json`, and `#to_yaml`.\n\n### Making custom or undocumented requests\n\n#### Undocumented properties\n\nYou can send undocumented parameters to any endpoint, and read undocumented response properties, like so:\n\nNote: the `extra_` parameters of the same name overrides the documented parameters.\n\n```ruby\nchat_completion =\n  dedalus.chat.completions.create(\n    model: "openai/gpt-5-nano",\n    messages: [\n      {role: "system", content: "You are Stephen Dedalus. Respond in morose Joycean malaise."},\n      {role: "user", content: "Hello, how are you today?"}\n    ],\n    request_options: {\n      extra_query: {my_query_parameter: value},\n      extra_body: {my_body_parameter: value},\n      extra_headers: {"my-header": value}\n    }\n  )\n\nputs(chat_completion[:my_undocumented_property])\n```\n\n#### Undocumented request params\n\nIf you want to explicitly send an extra param, you can do so with the `extra_query`, `extra_body`, and `extra_headers` under the `request_options:` parameter when making a request, as seen in the examples above.\n\n#### Undocumented endpoints\n\nTo make requests to undocumented endpoints while retaining the benefit of auth, retries, and so on, you can make requests using `client.request`, like so:\n\n```ruby\nresponse = client.request(\n  method: :post,\n  path: \'/undocumented/endpoint\',\n  query: {"dog": "woof"},\n  headers: {"useful-header": "interesting-value"},\n  body: {"hello": "world"}\n)\n```\n\n### Concurrency & connection pooling\n\nThe `DedalusSDK::Client` instances are threadsafe, but are only are fork-safe when there are no in-flight HTTP requests.\n\nEach instance of `DedalusSDK::Client` has its own HTTP connection pool with a default size of 99. As such, we recommend instantiating the client once per application in most settings.\n\nWhen all available connections from the pool are checked out, requests wait for a new connection to become available, with queue time counting towards the request timeout.\n\nUnless otherwise specified, other classes in the SDK do not have locks protecting their underlying data structure.\n\n## Sorbet\n\nThis library provides comprehensive [RBI](https://sorbet.org/docs/rbi) definitions, and has no dependency on sorbet-runtime.\n\nYou can provide typesafe request parameters like so:\n\n```ruby\ndedalus.chat.completions.create(\n  model: "openai/gpt-5-nano",\n  messages: [\n    DedalusSDK::Chat::ChatCompletionSystemMessageParam.new(\n      content: "You are Stephen Dedalus. Respond in morose Joycean malaise."\n    ),\n    DedalusSDK::Chat::ChatCompletionUserMessageParam.new(content: "Hello, how are you today?")\n  ]\n)\n```\n\nOr, equivalently:\n\n```ruby\n# Hashes work, but are not typesafe:\ndedalus.chat.completions.create(\n  model: "openai/gpt-5-nano",\n  messages: [\n    {role: "system", content: "You are Stephen Dedalus. Respond in morose Joycean malaise."},\n    {role: "user", content: "Hello, how are you today?"}\n  ]\n)\n\n# You can also splat a full Params class:\nparams = DedalusSDK::Chat::CompletionCreateParams.new(\n  model: "openai/gpt-5-nano",\n  messages: [\n    DedalusSDK::Chat::ChatCompletionSystemMessageParam.new(\n      content: "You are Stephen Dedalus. Respond in morose Joycean malaise."\n    ),\n    DedalusSDK::Chat::ChatCompletionUserMessageParam.new(content: "Hello, how are you today?")\n  ]\n)\ndedalus.chat.completions.create(**params)\n```\n\n### Enums\n\nSince this library does not depend on `sorbet-runtime`, it cannot provide [`T::Enum`](https://sorbet.org/docs/tenum) instances. Instead, we provide "tagged symbols" instead, which is always a primitive at runtime:\n\n```ruby\n# :float\nputs(DedalusSDK::CreateEmbeddingRequest::EncodingFormat::FLOAT)\n\n# Revealed type: `T.all(DedalusSDK::CreateEmbeddingRequest::EncodingFormat, Symbol)`\nT.reveal_type(DedalusSDK::CreateEmbeddingRequest::EncodingFormat::FLOAT)\n```\n\nEnum parameters have a "relaxed" type, so you can either pass in enum constants or their literal value:\n\n```ruby\n# Using the enum constants preserves the tagged type information:\ndedalus.embeddings.create(\n  encoding_format: DedalusSDK::CreateEmbeddingRequest::EncodingFormat::FLOAT,\n  # …\n)\n\n# Literal values are also permissible:\ndedalus.embeddings.create(\n  encoding_format: :float,\n  # …\n)\n```\n\n## Versioning\n\nThis package follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions. As the library is in initial development and has a major version of `0`, APIs may change at any time.\n\nThis package considers improvements to the (non-runtime) `*.rbi` and `*.rbs` type definitions to be non-breaking changes.\n\n## Requirements\n\nRuby 3.2.0 or higher.\n\n## Contributing\n\nSee [the contributing documentation](https://github.com/dedalus-labs/dedalus-sdk-ruby/tree/main/CONTRIBUTING.md).\n',
  },
  {
    language: 'typescript',
    content:
      "# Dedalus TypeScript API Library\n\n[![NPM version](https://img.shields.io/npm/v/dedalus-labs.svg?label=npm%20(stable))](https://npmjs.org/package/dedalus-labs) ![npm bundle size](https://img.shields.io/bundlephobia/minzip/dedalus-labs)\n\nThis library provides convenient access to the Dedalus REST API from server-side TypeScript or JavaScript.\n\n\n\nThe REST API documentation can be found on [docs.dedaluslabs.ai](https://docs.dedaluslabs.ai). The full API of this library can be found in [api.md](api.md).\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Dedalus MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=dedalus-labs-mcp&config=eyJuYW1lIjoiZGVkYWx1cy1sYWJzLW1jcCIsInRyYW5zcG9ydCI6Imh0dHAiLCJ1cmwiOiJodHRwczovL2RlZGFsdXMtc2RrLnN0bG1jcC5jb20iLCJoZWFkZXJzIjp7IngtZGVkYWx1cy1hcGkta2V5IjoiTXkgQVBJIEtleSIsIngtYXBpLWtleSI6Ik15IFggQVBJIEtleSJ9fQ)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dedalus-labs-mcp%22%2C%22type%22%3A%22http%22%2C%22url%22%3A%22https%3A%2F%2Fdedalus-sdk.stlmcp.com%22%2C%22headers%22%3A%7B%22x-dedalus-api-key%22%3A%22My%20API%20Key%22%2C%22x-api-key%22%3A%22My%20X%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n## Installation\n\n```sh\nnpm install dedalus-labs\n```\n\n\n\n## Usage\n\nThe full API of this library can be found in [api.md](api.md).\n\n<!-- prettier-ignore -->\n```js\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n  environment: 'development', // defaults to 'production'\n});\n\nconst chatCompletion = await client.chat.completions.create({\n  model: 'openai/gpt-5-nano',\n  messages: [\n    { role: 'system', content: 'You are Stephen Dedalus. Respond in morose Joycean malaise.' },\n    { role: 'user', content: 'Hello, how are you today?' },\n  ],\n});\n\nconsole.log(chatCompletion.id);\n```\n\n## Streaming responses\n\nWe provide support for streaming responses using Server Sent Events (SSE).\n\n```ts\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst stream = await client.chat.completions.create({\n  model: 'openai/gpt-5-nano',\n  stream: true,\n  messages: [\n    { role: 'system', content: 'You are Stephen Dedalus. Respond in morose Joycean malaise.' },\n    { role: 'user', content: 'What do you think of artificial intelligence?' },\n  ],\n});\nfor await (const chatCompletionChunk of stream) {\n  console.log(chatCompletionChunk.id);\n}\n```\n\nIf you need to cancel a stream, you can `break` from the loop\nor call `stream.controller.abort()`.\n\n### Request & Response types\n\nThis library includes TypeScript definitions for all request params and response fields. You may import and use them like so:\n\n<!-- prettier-ignore -->\n```ts\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  apiKey: process.env['DEDALUS_API_KEY'], // This is the default and can be omitted\n  environment: 'development', // defaults to 'production'\n});\n\nconst params: Dedalus.Chat.CompletionCreateParams = {\n  model: 'openai/gpt-5-nano',\n  messages: [\n    { role: 'system', content: 'You are Stephen Dedalus. Respond in morose Joycean malaise.' },\n    { role: 'user', content: 'Hello, how are you today?' },\n  ],\n};\nconst chatCompletion: Dedalus.Chat.ChatCompletion = await client.chat.completions.create(params);\n```\n\nDocumentation for each method, request param, and response field are available in docstrings and will appear on hover in most modern editors.\n\n## File uploads\n\nRequest parameters that correspond to file uploads can be passed in many different forms:\n- `File` (or an object with the same structure)\n- a `fetch` `Response` (or an object with the same structure)\n- an `fs.ReadStream`\n- the return value of our `toFile` helper\n\n```ts\nimport fs from 'fs';\nimport Dedalus, { toFile } from 'dedalus-labs';\n\nconst client = new Dedalus();\n\n// If you have access to Node `fs` we recommend using `fs.createReadStream()`:\nawait client.audio.transcriptions.create({\n  file: fs.createReadStream('/path/to/file'),\n  model: 'model',\n});\n\n// Or if you have the web `File` API you can pass a `File` instance:\nawait client.audio.transcriptions.create({ file: new File(['my bytes'], 'file'), model: 'model' });\n\n// You can also pass a `fetch` `Response`:\nawait client.audio.transcriptions.create({\n  file: await fetch('https://somesite/file'),\n  model: 'model',\n});\n\n// Finally, if none of the above are convenient, you can use our `toFile` helper:\nawait client.audio.transcriptions.create({\n  file: await toFile(Buffer.from('my bytes'), 'file'),\n  model: 'model',\n});\nawait client.audio.transcriptions.create({\n  file: await toFile(new Uint8Array([0, 1, 2]), 'file'),\n  model: 'model',\n});\n```\n\n\n\n## Handling errors\n\nWhen the library is unable to connect to the API,\nor if the API returns a non-success status code (i.e., 4xx or 5xx response),\na subclass of `APIError` will be thrown:\n\n<!-- prettier-ignore -->\n```ts\nconst chatCompletion = await client.chat.completions\n  .create({\n    model: 'openai/gpt-5-nano',\n    messages: [\n      { role: 'system', content: 'You are Stephen Dedalus. Respond in morose Joycean malaise.' },\n      { role: 'user', content: 'Hello, how are you today?' },\n    ],\n  })\n  .catch(async (err) => {\n    if (err instanceof Dedalus.APIError) {\n      console.log(err.status); // 400\n      console.log(err.name); // BadRequestError\n      console.log(err.headers); // {server: 'nginx', ...}\n    } else {\n      throw err;\n    }\n  });\n```\n\nError codes are as follows:\n\n| Status Code | Error Type                 |\n| ----------- | -------------------------- |\n| 400         | `BadRequestError`          |\n| 401         | `AuthenticationError`      |\n| 403         | `PermissionDeniedError`    |\n| 404         | `NotFoundError`            |\n| 422         | `UnprocessableEntityError` |\n| 429         | `RateLimitError`           |\n| >=500       | `InternalServerError`      |\n| N/A         | `APIConnectionError`       |\n\n### Retries\n\nCertain errors will be automatically retried 2 times by default, with a short exponential backoff.\nConnection errors (for example, due to a network connectivity problem), 408 Request Timeout, 409 Conflict,\n429 Rate Limit, and >=500 Internal errors will all be retried by default.\n\nYou can use the `maxRetries` option to configure or disable this:\n\n<!-- prettier-ignore -->\n```js\n// Configure the default for all requests:\nconst client = new Dedalus({\n  maxRetries: 0, // default is 2\n});\n\n// Or, configure per-request:\nawait client.chat.completions.create({ model: 'openai/gpt-5-nano', messages: [{ role: 'system', content: 'You are Stephen Dedalus. Respond in morose Joycean malaise.' }, { role: 'user', content: 'Hello, how are you today?' }] }, {\n  maxRetries: 5,\n});\n```\n\n### Timeouts\n\nRequests time out after 1 minute by default. You can configure this with a `timeout` option:\n\n<!-- prettier-ignore -->\n```ts\n// Configure the default for all requests:\nconst client = new Dedalus({\n  timeout: 20 * 1000, // 20 seconds (default is 1 minute)\n});\n\n// Override per-request:\nawait client.chat.completions.create({ model: 'openai/gpt-5-nano', messages: [{ role: 'system', content: 'You are Stephen Dedalus. Respond in morose Joycean malaise.' }, { role: 'user', content: 'Hello, how are you today?' }] }, {\n  timeout: 5 * 1000,\n});\n```\n\nOn timeout, an `APIConnectionTimeoutError` is thrown.\n\nNote that requests which time out will be [retried twice by default](#retries).\n\n\n\n## Default Headers\n\nWe automatically send the following headers with all requests.\n\n| Header          | Value         |\n| --------------- | ------------- |\n| `User-Agent`    | `Dedalus-SDK` |\n| `X-SDK-Version` | `1.0.0`       |\n\nIf you need to, you can override these headers by setting default headers on a per-request basis.\n\n```ts\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus();\n\nconst chatCompletion = await client.chat.completions.create(\n  {\n    model: 'openai/gpt-5-nano',\n    messages: [\n      { role: 'system', content: 'You are Stephen Dedalus. Respond in morose Joycean malaise.' },\n      { role: 'user', content: 'Hello, how are you today?' },\n    ],\n  },\n  { headers: { 'User-Agent': 'My-Custom-Value' } },\n);\n```\n\n## Advanced Usage\n\n### Accessing raw Response data (e.g., headers)\n\nThe \"raw\" `Response` returned by `fetch()` can be accessed through the `.asResponse()` method on the `APIPromise` type that all methods return.\nThis method returns as soon as the headers for a successful response are received and does not consume the response body, so you are free to write custom parsing or streaming logic.\n\nYou can also use the `.withResponse()` method to get the raw `Response` along with the parsed data.\nUnlike `.asResponse()` this method consumes the body, returning once it is parsed.\n\n<!-- prettier-ignore -->\n```ts\nconst client = new Dedalus();\n\nconst response = await client.chat.completions\n  .create({\n    model: 'openai/gpt-5-nano',\n    messages: [\n      { role: 'system', content: 'You are Stephen Dedalus. Respond in morose Joycean malaise.' },\n      { role: 'user', content: 'Hello, how are you today?' },\n    ],\n  })\n  .asResponse();\nconsole.log(response.headers.get('X-My-Header'));\nconsole.log(response.statusText); // access the underlying Response object\n\nconst { data: chatCompletion, response: raw } = await client.chat.completions\n  .create({\n    model: 'openai/gpt-5-nano',\n    messages: [\n      { role: 'system', content: 'You are Stephen Dedalus. Respond in morose Joycean malaise.' },\n      { role: 'user', content: 'Hello, how are you today?' },\n    ],\n  })\n  .withResponse();\nconsole.log(raw.headers.get('X-My-Header'));\nconsole.log(chatCompletion.id);\n```\n\n### Logging\n\n> [!IMPORTANT]\n> All log messages are intended for debugging only. The format and content of log messages\n> may change between releases.\n\n#### Log levels\n\nThe log level can be configured in two ways:\n\n1. Via the `DEDALUS_LOG` environment variable\n2. Using the `logLevel` client option (overrides the environment variable if set)\n\n```ts\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  logLevel: 'debug', // Show all log messages\n});\n```\n\nAvailable log levels, from most to least verbose:\n\n- `'debug'` - Show debug messages, info, warnings, and errors\n- `'info'` - Show info messages, warnings, and errors\n- `'warn'` - Show warnings and errors (default)\n- `'error'` - Show only errors\n- `'off'` - Disable all logging\n\nAt the `'debug'` level, all HTTP requests and responses are logged, including headers and bodies.\nSome authentication-related headers are redacted, but sensitive data in request and response bodies\nmay still be visible.\n\n#### Custom logger\n\nBy default, this library logs to `globalThis.console`. You can also provide a custom logger.\nMost logging libraries are supported, including [pino](https://www.npmjs.com/package/pino), [winston](https://www.npmjs.com/package/winston), [bunyan](https://www.npmjs.com/package/bunyan), [consola](https://www.npmjs.com/package/consola), [signale](https://www.npmjs.com/package/signale), and [@std/log](https://jsr.io/@std/log). If your logger doesn't work, please open an issue.\n\nWhen providing a custom logger, the `logLevel` option still controls which messages are emitted, messages\nbelow the configured level will not be sent to your logger.\n\n```ts\nimport Dedalus from 'dedalus-labs';\nimport pino from 'pino';\n\nconst logger = pino();\n\nconst client = new Dedalus({\n  logger: logger.child({ name: 'Dedalus' }),\n  logLevel: 'debug', // Send all messages to pino, allowing it to filter\n});\n```\n\n### Making custom/undocumented requests\n\nThis library is typed for convenient access to the documented API. If you need to access undocumented\nendpoints, params, or response properties, the library can still be used.\n\n#### Undocumented endpoints\n\nTo make requests to undocumented endpoints, you can use `client.get`, `client.post`, and other HTTP verbs.\nOptions on the client, such as retries, will be respected when making these requests.\n\n```ts\nawait client.post('/some/path', {\n  body: { some_prop: 'foo' },\n  query: { some_query_arg: 'bar' },\n});\n```\n\n#### Undocumented request params\n\nTo make requests using undocumented parameters, you may use `// @ts-expect-error` on the undocumented\nparameter. This library doesn't validate at runtime that the request matches the type, so any extra values you\nsend will be sent as-is.\n\n```ts\nclient.chat.completions.create({\n  // ...\n  // @ts-expect-error baz is not yet public\n  baz: 'undocumented option',\n});\n```\n\nFor requests with the `GET` verb, any extra params will be in the query, all other requests will send the\nextra param in the body.\n\nIf you want to explicitly send an extra argument, you can do so with the `query`, `body`, and `headers` request\noptions.\n\n#### Undocumented response properties\n\nTo access undocumented response properties, you may access the response object with `// @ts-expect-error` on\nthe response object, or cast the response object to the requisite type. Like the request params, we do not\nvalidate or strip extra properties from the response from the API.\n\n### Customizing the fetch client\n\nBy default, this library expects a global `fetch` function is defined.\n\nIf you want to use a different `fetch` function, you can either polyfill the global:\n\n```ts\nimport fetch from 'my-fetch';\n\nglobalThis.fetch = fetch;\n```\n\nOr pass it to the client:\n\n```ts\nimport Dedalus from 'dedalus-labs';\nimport fetch from 'my-fetch';\n\nconst client = new Dedalus({ fetch });\n```\n\n### Fetch options\n\nIf you want to set custom `fetch` options without overriding the `fetch` function, you can provide a `fetchOptions` object when instantiating the client or making a request. (Request-specific options override client options.)\n\n```ts\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  fetchOptions: {\n    // `RequestInit` options\n  },\n});\n```\n\n#### Configuring proxies\n\nTo modify proxy behavior, you can provide custom `fetchOptions` that add runtime-specific proxy\noptions to requests:\n\n<img src=\"https://raw.githubusercontent.com/stainless-api/sdk-assets/refs/heads/main/node.svg\" align=\"top\" width=\"18\" height=\"21\"> **Node** <sup>[[docs](https://github.com/nodejs/undici/blob/main/docs/docs/api/ProxyAgent.md#example---proxyagent-with-fetch)]</sup>\n\n```ts\nimport Dedalus from 'dedalus-labs';\nimport * as undici from 'undici';\n\nconst proxyAgent = new undici.ProxyAgent('http://localhost:8888');\nconst client = new Dedalus({\n  fetchOptions: {\n    dispatcher: proxyAgent,\n  },\n});\n```\n\n<img src=\"https://raw.githubusercontent.com/stainless-api/sdk-assets/refs/heads/main/bun.svg\" align=\"top\" width=\"18\" height=\"21\"> **Bun** <sup>[[docs](https://bun.sh/guides/http/proxy)]</sup>\n\n```ts\nimport Dedalus from 'dedalus-labs';\n\nconst client = new Dedalus({\n  fetchOptions: {\n    proxy: 'http://localhost:8888',\n  },\n});\n```\n\n<img src=\"https://raw.githubusercontent.com/stainless-api/sdk-assets/refs/heads/main/deno.svg\" align=\"top\" width=\"18\" height=\"21\"> **Deno** <sup>[[docs](https://docs.deno.com/api/deno/~/Deno.createHttpClient)]</sup>\n\n```ts\nimport Dedalus from 'npm:dedalus-labs';\n\nconst httpClient = Deno.createHttpClient({ proxy: { url: 'http://localhost:8888' } });\nconst client = new Dedalus({\n  fetchOptions: {\n    client: httpClient,\n  },\n});\n```\n\n## Frequently Asked Questions\n\n## Semantic versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes that only affect static types, without breaking runtime behavior.\n2. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n3. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/dedalus-labs/dedalus-sdk-typescript/issues) with questions, bugs, or suggestions.\n\n## Requirements\n\nTypeScript >= 4.9 is supported.\n\nThe following runtimes are supported:\n\n- Web browsers (Up-to-date Chrome, Firefox, Safari, Edge, and more)\n- Node.js 20 LTS or later ([non-EOL](https://endoflife.date/nodejs)) versions.\n- Deno v1.28.0 or higher.\n- Bun 1.0 or later.\n- Cloudflare Workers.\n- Vercel Edge Runtime.\n- Jest 28 or greater with the `\"node\"` environment (`\"jsdom\"` is not supported at this time).\n- Nitro v2.6 or greater.\n\nNote that React Native is not supported at this time.\n\nIf you are interested in other runtime environments, please open or upvote an issue on GitHub.\n\n## Contributing\n\nSee [the contributing documentation](./CONTRIBUTING.md).\n",
  },
];

const INDEX_OPTIONS = {
  fields: [
    'name',
    'endpoint',
    'summary',
    'description',
    'qualified',
    'stainlessPath',
    'content',
    'sectionContext',
  ],
  storeFields: ['kind', '_original'],
  searchOptions: {
    prefix: true,
    fuzzy: 0.1,
    boost: {
      name: 5,
      stainlessPath: 3,
      endpoint: 3,
      qualified: 3,
      summary: 2,
      content: 1,
      description: 1,
    } as Record<string, number>,
  },
};

/**
 * Self-contained local search engine backed by MiniSearch.
 * Method data is embedded at SDK build time; prose documents
 * can be loaded from an optional docs directory at runtime.
 */
export class LocalDocsSearch {
  private methodIndex: MiniSearch<MiniSearchDocument>;
  private proseIndex: MiniSearch<MiniSearchDocument>;

  private constructor() {
    this.methodIndex = new MiniSearch<MiniSearchDocument>(INDEX_OPTIONS);
    this.proseIndex = new MiniSearch<MiniSearchDocument>(INDEX_OPTIONS);
  }

  static async create(opts?: { docsDir?: string }): Promise<LocalDocsSearch> {
    const instance = new LocalDocsSearch();
    instance.indexMethods(EMBEDDED_METHODS);
    for (const readme of EMBEDDED_READMES) {
      instance.indexProse(readme.content, `readme:${readme.language}`);
    }
    if (opts?.docsDir) {
      await instance.loadDocsDirectory(opts.docsDir);
    }
    return instance;
  }

  search(props: {
    query: string;
    language?: string;
    detail?: string;
    maxResults?: number;
    maxLength?: number;
  }): SearchResult {
    const { query, language = 'typescript', detail = 'default', maxResults = 5, maxLength = 100_000 } = props;

    const useMarkdown = detail === 'verbose' || detail === 'high';

    // Search both indices and merge results by score.
    // Filter prose hits so language-tagged content (READMEs and docs with
    // frontmatter) only matches the requested language.
    const methodHits = this.methodIndex
      .search(query)
      .map((hit) => ({ ...hit, _kind: 'http_method' as const }));
    const proseHits = this.proseIndex
      .search(query)
      .filter((hit) => {
        const source = ((hit as Record<string, unknown>)['_original'] as ProseChunk | undefined)?.source;
        if (!source) return true;
        // Check for language-tagged sources: "readme:<lang>" or "lang:<lang>:<filename>"
        let taggedLang: string | undefined;
        if (source.startsWith('readme:')) taggedLang = source.slice('readme:'.length);
        else if (source.startsWith('lang:')) taggedLang = source.split(':')[1];
        if (!taggedLang) return true;
        return taggedLang === language || (language === 'javascript' && taggedLang === 'typescript');
      })
      .map((hit) => ({ ...hit, _kind: 'prose' as const }));
    const merged = [...methodHits, ...proseHits].sort((a, b) => b.score - a.score);
    const top = merged.slice(0, maxResults);

    const fullResults: (string | Record<string, unknown>)[] = [];

    for (const hit of top) {
      const original = (hit as Record<string, unknown>)['_original'];
      if (hit._kind === 'http_method') {
        const m = original as MethodEntry;
        if (useMarkdown && m.markdown) {
          fullResults.push(m.markdown);
        } else {
          // Use per-language data when available, falling back to the
          // top-level fields (which are TypeScript-specific in the
          // legacy codepath).
          const langData = m.perLanguage?.[language];
          fullResults.push({
            method: langData?.method ?? m.qualified,
            summary: m.summary,
            description: m.description,
            endpoint: `${m.httpMethod.toUpperCase()} ${m.endpoint}`,
            ...(langData?.example ? { example: langData.example } : {}),
            ...(m.params ? { params: m.params } : {}),
            ...(m.response ? { response: m.response } : {}),
          });
        }
      } else {
        const c = original as ProseChunk;
        fullResults.push({
          content: c.content,
          ...(c.source ? { source: c.source } : {}),
        });
      }
    }

    let totalLength = 0;
    const results: (string | Record<string, unknown>)[] = [];
    for (const result of fullResults) {
      const len = typeof result === 'string' ? result.length : JSON.stringify(result).length;
      totalLength += len;
      if (totalLength > maxLength) break;
      results.push(result);
    }

    if (results.length < fullResults.length) {
      results.unshift(`Truncated; showing ${results.length} of ${fullResults.length} results.`);
    }

    return { results };
  }

  private indexMethods(methods: MethodEntry[]): void {
    const docs: MiniSearchDocument[] = methods.map((m, i) => ({
      id: `method-${i}`,
      kind: 'http_method' as const,
      name: m.name,
      endpoint: m.endpoint,
      summary: m.summary,
      description: m.description,
      qualified: m.qualified,
      stainlessPath: m.stainlessPath,
      _original: m as unknown as Record<string, unknown>,
    }));
    if (docs.length > 0) {
      this.methodIndex.addAll(docs);
    }
  }

  private async loadDocsDirectory(docsDir: string): Promise<void> {
    let entries;
    try {
      entries = await fs.readdir(docsDir, { withFileTypes: true });
    } catch (err) {
      getLogger().warn({ err, docsDir }, 'Could not read docs directory');
      return;
    }

    const files = entries
      .filter((e) => e.isFile())
      .filter((e) => e.name.endsWith('.md') || e.name.endsWith('.markdown') || e.name.endsWith('.json'));

    for (const file of files) {
      try {
        const filePath = path.join(docsDir, file.name);
        const content = await fs.readFile(filePath, 'utf-8');

        if (file.name.endsWith('.json')) {
          const texts = extractTexts(JSON.parse(content));
          if (texts.length > 0) {
            this.indexProse(texts.join('\n\n'), file.name);
          }
        } else {
          // Parse optional YAML frontmatter for language tagging.
          // Files with a "language" field in frontmatter will only
          // surface in searches for that language.
          //
          // Example:
          //   ---
          //   language: python
          //   ---
          //   # Error handling in Python
          //   ...
          const frontmatter = parseFrontmatter(content);
          const source = frontmatter.language ? `lang:${frontmatter.language}:${file.name}` : file.name;
          this.indexProse(content, source);
        }
      } catch (err) {
        getLogger().warn({ err, file: file.name }, 'Failed to index docs file');
      }
    }
  }

  private indexProse(markdown: string, source: string): void {
    const chunks = chunkMarkdown(markdown);
    const baseId = this.proseIndex.documentCount;

    const docs: MiniSearchDocument[] = chunks.map((chunk, i) => ({
      id: `prose-${baseId + i}`,
      kind: 'prose' as const,
      content: chunk.content,
      ...(chunk.sectionContext != null ? { sectionContext: chunk.sectionContext } : {}),
      _original: { ...chunk, source } as unknown as Record<string, unknown>,
    }));

    if (docs.length > 0) {
      this.proseIndex.addAll(docs);
    }
  }
}

/** Lightweight markdown chunker — splits on headers, chunks by word count. */
function chunkMarkdown(markdown: string): { content: string; tag: string; sectionContext?: string }[] {
  // Strip YAML frontmatter
  const stripped = markdown.replace(/^---\n[\s\S]*?\n---\n?/, '');
  const lines = stripped.split('\n');

  const chunks: { content: string; tag: string; sectionContext?: string }[] = [];
  const headers: string[] = [];
  let current: string[] = [];

  const flush = () => {
    const text = current.join('\n').trim();
    if (!text) return;
    const sectionContext = headers.length > 0 ? headers.join(' > ') : undefined;
    // Split into ~200-word chunks
    const words = text.split(/\s+/);
    for (let i = 0; i < words.length; i += 200) {
      const slice = words.slice(i, i + 200).join(' ');
      if (slice) {
        chunks.push({ content: slice, tag: 'p', ...(sectionContext != null ? { sectionContext } : {}) });
      }
    }
    current = [];
  };

  for (const line of lines) {
    const headerMatch = line.match(/^(#{1,6})\s+(.+)/);
    if (headerMatch) {
      flush();
      const level = headerMatch[1]!.length;
      const text = headerMatch[2]!.trim();
      while (headers.length >= level) headers.pop();
      headers.push(text);
    } else {
      current.push(line);
    }
  }
  flush();

  return chunks;
}

/** Recursively extracts string values from a JSON structure. */
function extractTexts(data: unknown, depth = 0): string[] {
  if (depth > 10) return [];
  if (typeof data === 'string') return data.trim() ? [data] : [];
  if (Array.isArray(data)) return data.flatMap((item) => extractTexts(item, depth + 1));
  if (typeof data === 'object' && data !== null) {
    return Object.values(data).flatMap((v) => extractTexts(v, depth + 1));
  }
  return [];
}

/** Parses YAML frontmatter from a markdown string, extracting the language field if present. */
function parseFrontmatter(markdown: string): { language?: string } {
  const match = markdown.match(/^---\n([\s\S]*?)\n---/);
  if (!match) return {};
  const body = match[1] ?? '';
  const langMatch = body.match(/^language:\s*(.+)$/m);
  return langMatch ? { language: langMatch[1]!.trim() } : {};
}
